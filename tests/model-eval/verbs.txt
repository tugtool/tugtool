# Verbs a session headline may open with, in the plain command form.
#
# A closed list on purpose: a headline opening with a word that is not here
# scores as a miss so a human reads it. When the model earns a good verb that
# is missing, add it — that is how this file is meant to grow.
add audit author
bind build bundle
cache chase check clip close compose cover cut
debug derive detect diagnose draft drive drop dump
emit enforce evaluate expand explain explore extend
fetch filter find fix flush fold format
gate generate
handle harden hunt
implement improve index inject insert inspect install instrument investigate
land link load log
map measure merge migrate mount move
normalize
open
parse patch persist pin plan port probe profile prune publish push
query queue
raise read rebuild record reduce refactor rename render repair replace
reset resolve restore resume retry rewrite route
salvage sample save scan scope score seal seed send serve set setup ship
show silence simplify skip solve sort spawn split stabilize start stop
store stream strip summarize sweep switch sync
teach test throttle tighten trace track trigger trim tune
unify update
validate verify vet
walk watch wire wrap write

# past forms
#
# What a retrospective opens with, once a stretch of work has settled. The
# strip gives an intent and a retrospective the same pixels, so the tense is
# the only thing telling them apart — which is why this is a scored rule and
# not a stylistic note. Grown exactly like the plain list above: when the
# model earns a past form that is missing, add it.
added audited authored
bound built bundled
cached chased checked clipped closed composed covered cut
debugged derived detected diagnosed drafted drove dropped dumped
emitted enforced evaluated expanded explained explored extended
fetched filtered found fixed flushed folded formatted
gated generated
handled hardened hunted
implemented improved indexed injected inserted inspected installed instrumented investigated
landed linked loaded logged
mapped measured merged migrated mounted moved
normalized
opened
parsed patched persisted pinned planned ported probed profiled pruned published pushed
queried queued
raised read rebuilt recorded reduced refactored renamed rendered repaired replaced
reset resolved restored resumed retried rewrote routed
salvaged sampled saved scanned scoped scored sealed seeded sent served set shipped
showed silenced simplified skipped solved sorted spawned split stabilized started stopped
stored streamed stripped summarized swept switched synced
taught tested throttled tightened traced tracked triggered trimmed tuned
unified updated
validated verified vetted
walked watched wired wrapped wrote

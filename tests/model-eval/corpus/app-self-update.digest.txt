What the user asked for:
- OK. We need to come up with a way for the release version of Tug.app that a user installs from the downloaded DMG can update itself. I *do not* want to invent anything new to make this feature happen. Between Swift, Rust, and Typescript, th…
- do the fixups!
- got interrupted. please continue.
- OK. If we're going to allow Sparkle to quit a running version of Tug, then we must *significantly harden* the auto-interrupt/auto-save on app termination features in Tug.app. The OS gives us hooks we can subscribe to and run code in, but I …
- Indeed. I joined this work back to main. Now, I'd like

What the session has been doing:
- Bash(just instances 2>&1 | tail -3)
- Bash(tugutil draft set --owner dash:quit-hardening --message "qui)
- TaskUpdate(Two of four passes run with log evidence (tugcast-outage ret)
- Bash(ls && echo "--- justfiles ---" && ls -la justfile* 2>/dev/nu)
- Bash(echo "=== scripts ===" && ls scripts/ 2>/dev/null; echo "===)
- Bash(wc -l Justfile .github/workflows/release.yml .github/workflo)
- Bash(echo "=== MARKETING_VERSION / CURRENT_PROJECT ===" && grep -)
- Read(version.sh)
- Read(release.yml)
- Read(make-appcast.sh)
- Bash(echo "=== Cargo.toml version ===" && grep -m1 '^version = ' )
- Bash(grep -n "^[a-z][a-z0-9-]*:\|^# " Justfile | sed -n '1,240p')

What it is doing right now:
- Bash(echo "=== tags ===" && git tag | tail -20 && echo "=== recen)
- Bash(echo "=== tag count / latest ===" && git tag | wc -l && git )
- Bash(grep -n "version\|VERSION" Justfile | head -40)
- Read(nightly.yml)
- Bash(grep -n "VERSION\|TUG_BUILD_NUMBER\|products/\|zip\|nightly")
- Bash(ls tuglaws/ && echo "=== release/update docs ===" && grep -r)
- Bash(cd tugrust && git log -1 --format=%h && echo "=== does gener)
- Bash(gh secret list 2>&1 | head -20)

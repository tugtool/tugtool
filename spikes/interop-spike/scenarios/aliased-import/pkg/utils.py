# Aliased import scenario: pkg/utils.py
# This module defines the function that will be imported with an alias

def process_data(data):
    """Process the input data."""
    return [x * 2 for x in data]

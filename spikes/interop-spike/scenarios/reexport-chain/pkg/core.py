# Reexport chain scenario: pkg/core.py
# The original definition - this is where the function is actually defined

def process_data(data):
    """Process the input data."""
    return [x * 2 for x in data]

# Design Decisions

*Each decision records a non-obvious choice and its rationale. Decisions are referenced from [tuglaws.md](tuglaws.md) as `[D##]` and laws are referenced here as `[L##]`.*

---

## Theme & Token Architecture

**D01.** Theme definitions (color choices + recipe reference) are stored as `.json` files in `tugdeck/themes/` (shipped) and `~/.tugtool/themes/` (authored), not as TypeScript constants. Enables runtime loading without rebuilding and the Prototype pattern for new theme authoring. [L06]

**D02.** Two-directory storage with unique names. Shipped themes in `tugdeck/themes/` (version-controlled, read-only). Authored themes in `~/.tugtool/themes/` (user data, not in repo). Names are unique across both directories — middleware checks authored first, then shipped, but uniqueness means no shadowing.

**D03.** Every theme is a peer — no base layer, no override cascade. Switching themes copies the selected theme's complete CSS file into `styles/tug-active-theme.css`. For brio, the source is `styles/themes/brio.css`. For harmony, the source is `styles/themes/harmony.css`. The active theme file is always complete; it is never empty. [L06]

**D04.** `contrastSearch()`, `darkRecipe()`, `lightRecipe()`, and `RECIPE_REGISTRY` are defined in `theme-engine.ts`. `RECIPE_REGISTRY` is the only derivation dispatch path — all `deriveTheme()` calls route through it.

**D86.** The `formulas?: DerivationFormulas` escape hatch is removed from `ThemeRecipe`. The recipe function is the only derivation path. `RECIPE_REGISTRY` is the extension point for new recipe variants.

**D87.** The theme generator card follows Mac document conventions: New (Prototype pattern, copy existing theme), Open (load from available themes), auto-save (500ms debounce to disk), Apply (inject CSS app-wide). Shipped themes open read-only. No explicit Save button.

**D88.** All theme loading goes through the Vite dev middleware. `ThemeName` is a plain `string` (not a hardcoded union). `themeCSSMap` is populated dynamically at startup via `GET /__themes/list`. Supports arbitrary authored themes without code changes.

**D89.** `canvasColorHex()` accepts derived canvas surface params (`hue`, `tone`, `intensity`) extracted from `ThemeOutput.formulas` after running `deriveTheme()`. The raw JSON `surface.canvas.intensity` differs from the derived `surfaceCanvasIntensity`; callers must use the derived value. The `CANVAS_COLORS` lookup table is removed.

**D90.** A theme's `recipe` field (`"dark"` or `"light"`) is set once at theme creation (copied from the prototype) and is immutable. The generator card displays recipe as a read-only label, not a toggle.

**D91.** The Swift Theme submenu uses `NSMenuDelegate.menuNeedsUpdate(_:)` to populate items dynamically from a cached theme list. The web view pushes updated theme lists to Swift via the `themeListUpdated` bridge message. Eliminates hardcoded menu items and per-theme `@objc` handlers.

**D92.** Bluenote is removed from the entire codebase — Swift menu, action dispatch, theme provider, and any CSS files.

**D70.** Color palette is OKLCH-based. 24 hue families with intensity/tone axes, 5 convenience presets per hue, neutral ramp, P3 gamut support, pure CSS formulas. [L15]

**D71.** Four-prefix token naming: `--tugc-{hue}[-preset]` (palette), `--tug7-*` (seven-slot semantic surface and element tokens), `--tugx-<component>-*` (component alias), `--tug-*` (scale/dimension). [L17, L18]

**D72.** Global dimension scale via `--tug-zoom` multiplier on `:root`.

**D73.** Global timing via `--tug-timing` (duration multiplier) and `--tug-motion` (binary on/off toggle) on `:root`.

**D75.** Achromatic neutral ramp (`--tug-neutral-*`). Alpha via CSS relative color syntax, not separate opacity tokens.

**D80.** `--tug-color()` notation expands to `oklch()` at build time via PostCSS plugin (`postcss-tug-color.ts`). Theme files use the notation; browsers never see it.

**D81.** Token pairings are machine-auditable. Every foreground-on-background relationship is extractable from CSS — either via same-rule `background-color` or via `@tug-renders-on` annotation. `audit-tokens lint` enforces zero violations. [L16]

**D82.** Four semantic contrast roles govern text legibility: `content` (75), `control` (60), `display` (60), `informational` (60). Each role maps to a hue slot derived from the recipe's text and display specs. The pairing map assigns every foreground token a contrast role for threshold enforcement. [L15, L18]

**D83.** Five contrast roles with minimum thresholds: `content` (75), `control` (60), `display` (60), `informational` (60), `decorative` (15). All readable text >= 60. [L16]

**D84.** Theme application uses stylesheet injection (`<style id="tug-theme-override">`), not body class toggling. [L06]

**D85.** Optional palette entries use `var()` fallbacks so themes can omit slots gracefully. [L15]

---

## Component Model

**D05.** Three component kinds: *wrappers* (thin Radix adapters), *compositions* (assemble multiple primitives), *originals* (built from scratch).

**D06.** `components/tugways/` is the public component API surface.

**D07.** Components are module-scope functions composed via JSX nesting, not class hierarchies or render props.

**D08.** TugButton has two modes: *direct-action* (`onClick` prop) and *chain-action* (`action` prop dispatches into responder chain). [L11]

**D15.** TugPane is composition, not inheritance. It assembles chrome (title bar, tab strip, accessory, content region) around the active Card's content, rendered via `CardHost`. [L09]

**D16.** Card data access via `useCardData()` hook, not render props.

**D17.** Cards compute dynamic min-size from content and report via `onMinSizeChange`.

**D22.** Component Gallery card serves as living inventory — 21 tabbed demos covering all tugways components. [L10]

**D39.** Default button: responder chain designates one button per scope. Enter key routes to it. [L11]

**D43.** Component Gallery is a proper card with tabs, not a floating panel. [L10]

**D105.** "Collapse long content" splits into two primitives along one axis: *does the hidden content stay mounted?* **Logical fold** (`BlockFoldCue` + `useBlockFoldState`) is for known-list bodies — file lines, terminal output, diff hunks — where collapsing must *unmount* the hidden portion for performance (don't mount a CM6 editor for a folded file) and coordinate with the transcript scroller (don't slam the viewport when a huge body folds mid-scroll). It thresholds by item count and is coupled, deliberately, to the tool-block chrome and scroller contexts. **Visual clamp** (`TugClamp`) is for arbitrary content that is cheap to render in full but visually too tall — a wrapped command, an error message, a markdown blurb — where there is no perf reason to unmount and no scroller to coordinate with. It *measures* the rendered height (the visual line count isn't known ahead of wrapping), caps it behind a `data-expanded` + CSS window with a Show more/less reveal, and keeps everything mounted. The two share only the `ChevronsDown/Up` vocabulary, never the machinery — so neither is forced to straddle both jobs. Reach for `TugClamp` in dialogs and inline surfaces; reach for the fold system inside a tool-block body. Tree-shaped per-node collapses (JSON nodes, diff hunks, search-result files) are a third, separate thing and belong to neither. [L06]

---

## Responder Chain & Actions

**D09.** Responder chain operates entirely outside React state. `ResponderChainManager` is a plain TypeScript class. [L01, L02, L07]

**D10.** Four-stage key processing pipeline: keybinding resolution, responder dispatch, default handling, browser passthrough.

**D11.** Two-level action validation: `canHandle` (fast routing check) + `validateAction` (semantic validation with current state). [L07]

**D61.** `ActionEvent` carries typed payload, sender identity, and phase. [L11]

**D62.** Two dispatch modes: *nil-target* (chain walks until a responder handles it) and *explicit-target* (dispatched to a specific responder). [L11]

**D63.** Controls (buttons, sliders, pickers) dispatch ActionEvents into the chain but never register as responder nodes. [L11]

---

## State & Mutation Zones

**D13.** DOM utility hooks for appearance zone: `useCSSVar`, `useDOMClass`, `useDOMStyle`. All bypass React state. [L06]

**D40.** DeckManager is a subscribable store. Implements `subscribe()`/`getSnapshot()` for `useSyncExternalStore`. Exactly one `root.render()` call, at construction. [L01, L02]

**D41.** `useResponder` uses `useLayoutEffect` for registration so responder nodes are wired before any events fire. [L03]

**D42.** No repeated `root.render()` from external code. All state changes flow through `notify()` and `useSyncExternalStore`. [L01]

**D64.** Mutation transactions: `begin` (snapshot CSS inline styles) → `preview` (live CSS mutations) → `commit`/`cancel`. [L08]

**D65.** Transaction previews operate in appearance zone only. Commit handler may cross into store/React state. [L06, L08]

**D66.** Style cascade reader (`StyleCascadeReader`) provides `getDeclared()` with four source layers: `token`, `class`, `inline`, `preview`.

---

## Cards & Layout

**D27.** *(Retired by [D128].)* Window-shade collapse: double-click title bar toggles card between full and title-bar-only. State stored in `CardState.collapsed`. Removed outright when the width presets landed — it was pointer-only (no command, no chord, no menu item), and the chevron's title-bar slot went to the width popup, which is a control the same cluster had room for and the keyboard can reach. Old blobs carrying `collapsed: true` deserialize with the field dropped and the pane expanded.

**D30.** Tab bar visible only when card has multiple tabs.

**D31.** Tabs are a TugPane chrome feature — a UI affordance that appears when a Pane holds more than one Card. Not a data concept. [L09, L10, L25]

**D44.** Progressive tab overflow: three stages — all visible, inactive tabs collapse to icon-only, overflow into dropdown.

**D45.** Card-as-tab merge: dropping a card onto another card's tab bar merges it as a new tab. `detachTab()` reverses.

**D49.** Per-tab state bag preserves `scroll`, `selection`, and `content` across tab switches and reloads.

**D50.** `useCardStatePreservation` hook: card content registers `onSave`/`onRestore` callbacks. Uses `useLayoutEffect` for registration. [L03]

**D95.** A content-owning + engine card has exactly one text-entry surface — the engine's editor (`tug-prompt-entry` for a session card) — and activation focus has exactly one destination: the engine. A content-owning card (`bag.content !== undefined` — every session card is one) carries no `data-tug-focus-key` / `data-tug-state-key` element of its own, so `captureFocus` only ever classifies its focus as `engine` or `none`; the framework-axis save site leaves `bag.focus` absent in both cases. On restore, the single-channel dispatcher `resolveBagFocus` finds no framework-axis snapshot but an engine-managed `componentId`, and routes to the `engine` resolution — `applyBagFocus` invokes the engine's registered `paintMirrorAsActive` hook (`store.registerEngineHooks` / `store.invokeEnginePaintMirrorAsActive`). The engine is a *callable*, not an autonomous claimant: it no longer claims focus from `onCardActivated`. Focusable-but-not-entry content inside the card (a read-only `TugCodeView` viewport, a block button) carries no focus marker, so `captureFocus` classifies it `none` and activation focus correctly returns to the engine — a viewer is not a text-entry surface, and transient selection-to-copy focus is not preserved across activation. The general framework focus axis (`data-tug-focus-key` / `data-tug-state-key`, the `dom` / `form-control` `FocusSnapshot` kinds) still serves non-engine cards (form-control cards, settings cards); it is simply never populated *inside* a content-owning + engine card. See [state-preservation.md § Focus dispatch model](state-preservation.md#focus-dispatch-model), [component-authoring.md § Focus in content-owning cards](component-authoring.md), `card-host.tsx` (`captureFocus`), and `focus-transfer.ts` (`resolveBagFocus` / `applyBagFocus`). Established by Phase E.11 (single-channel dispatcher) and Phase E.12 (single-text-entry rule; per-block Find removed). Supersedes the pre-E.11 framing of this decision (the engine-vs-framework "two axes" model with `kind: "component-owned"` and `resolveActivationTarget`). Previously documented inline as `[D07]` in `card-host.tsx` comments — that local tag has been normalized to reference this decision (the canonical D07 is the JSX-composition rule above; the focus-boundary rule is D95). [L23]

**D51.** Focused card ID persisted in `DeckState.focusedCardId` for reload restoration.

**D52.** Collapsed state persisted in `CardState.collapsed`.

**D121.** **Layout imposition: a slot is a position anchor, not a rect — the imposer places panes and never sizes them.** The deck's `imposition` is a required `{ kind?, contentWidth?, sidebars }` record on `DeckState`: `kind` (`"one-up"` through `"six-up"`, absent = off) defines numbered **slots**, `contentWidth` names the width content cards read at ([D128]), and `sidebars[componentId] = { side, pinned? }` says which edge each sidebar card holds and whether it is standing there. (The legacy `{ lens, lensPinned }` pair reads as `sidebars.lens` — the blob stayed at v4, defensively parsed.) All of them are layout decisions, so all of them are made in one place — the Lens **Layouts** section, whose every option is a scale picture of the resulting deck; there is no app-wide sidebar-side preference. Slots are laid across the **span** — the canvas minus each side's rail width and one imposition gap — so an imposed position is never under a rail even though a free pane may still be dragged there. **A sidebar is itself imposed, as the arrangement's fixed end rather than one of its slots** (`imposeSidebarStyle`): it holds its side at a constant pin with the imposition gap on all four sides, keeps its own width, and never overlaps. It has to be the fixed end — a slotted card can end up overlapped when the deck is crowded, and a card sliding under the Lens is the one outcome the arrangement forbids. **The Lens is draggable, and dragging it by its title bar is what takes it off the pin** (`lensPinned: false`): it becomes an ordinary free pane and the arrangement spans the whole canvas exactly as it does when the Lens is closed. Any choice in the Layouts section puts it back — naming the side, or naming the kind, is the gesture that says the Lens belongs at the end of the arrangement — as does closing and reopening it. Widening the Lens by its deck-facing edge does **not** unpin it: the arrangement keeps laying out against it, live. A sidebar pane carries no marker of its own: it is the pane hosting a card whose registration declares `layoutRole: "sidebar"`, derived by `findSidebarPanes` ([D128]). A slot is an anchor at a fixed fraction of the band: a pane of width *w* in slot *k* of an *N*-slot imposition sits `k / (N − 1) × max(0, band − w)` from the packing edge, and stretches to full canvas height. Slot 0 therefore hugs the far edge and the last slot hugs the Lens, by construction rather than by arithmetic that happens to work out. **A pane's position depends on its own width and nothing else's** — closing, opening, widening, or restacking any other card leaves it exactly where it was. A slot is a place in the arrangement, never a place in a queue, and that is worth more than the earlier rule of pooling all the slack in one margin beside the Lens (which made the arrangement re-pack whenever its membership changed). Slack now spreads evenly between the cards. That is the whole geometry: **the imposer never touches a pane's width, in any code path.** The width **presets** are no exception and were checked against this rule when they landed: a preset is applied by a *command* through `movePane`, the same door a drag-resize uses, and the imposer goes on passing whatever width it finds through untouched ([D128]). Widths stay the user's, which is what keeps the arrangement from re-creating the tab strip's fight for horizontal space — and it is why overlap needs no handling at all: when the assigned cards are wider than their share, the anchors still hold exactly and the cards simply overlap their neighbours, which is ordinary geometry, not a failure. **Slots are stacks:** any number of panes may hold one, clicking a number always assigns *and* raises (never a toggle, never a no-op), and the filterable, metadata-rich Lens list — not a tab strip — is the switching surface. Assigning a card that shares a pane with others **detaches it first** (`_detachCard`); the imposer slots cards, never tab groups. **The arrangement is also the keyboard's:** ⌘1..⌘N puts the selected card at slot N, where N is whatever the chosen kind has — the Layouts section configures the chords by configuring the arrangement, and there is no separate list of shortcuts to keep in step with it. DeckCanvas owns the `move-to-slot` handler because the canvas owns the layout tree, and it converges on the same `assign-slot` the SlotPicker dispatches, so a chord and a click do one thing. The whole digit row is bound even though six-up is the largest arrangement, and every number the current arrangement lacks — along with an empty selection and a selected Lens — is a **silent no-op**: a chord the user has not configured should do nothing, not beep. These chords replaced `jump-to-tab` (⌘1..⌘9 by tab index), which the tab strip's demotion to a switching surface of last resort had already left with little to switch; ⇧⌘[ / ⇧⌘] still walk a pane's tabs. Assignment is **only ever explicit**: nothing is auto-slotted, auto-rebalanced, or auto-evicted. Any manual move **or resize** of an imposed pane evicts it back to free geometry (an explicit option on the commit, never a "position changed" heuristic — a west-edge resize moves `position.x` too). The pinned Lens is the exception: its deck-facing edge resizes it in place, re-laying the arrangement live. `arrangeCards` (Window ▸ Cascade / Tile) skips a **pinned** Lens for the same reason — tiling writes a stored rect, and a pinned Lens paints from its pin; a Lens off the pin tiles with everything else. Turning the imposition off **freezes** every imposed pane at its live on-screen rect rather than scattering them back to stale coordinates. A kind change **clamps** out-of-range slots instead of dropping panes out of the arrangement. Geometry is **CSS-derived, not observed**: the span reaches CSS as `--tug-imposer-inset-left` / `--tug-imposer-inset-right` (the Lens's render width plus one gap) and an imposed frame's pins are `calc()` expressions over them, so a window resize or a Lens drag reflows every imposed pane with no JavaScript and no resize observation anywhere on the deck ([L06]). **Every horizontal pin is emitted as `left`**, including the ones measured from the right edge (as `100% - …`) — a frame positioned by one property can transition between two arrangements, while one that switches between `left` and `right` can only cut. That is what the settle is built on: a change to the `imposition` record marks the frames' container with `data-imposer-settling` for `IMPOSITION_SETTLE_MS`, and every derived frame crosses to its new place instead of teleporting. The duration is one number, written onto the container as `--tugx-imposer-settle-duration` and read back when timing the settle, so overriding it there retimes both. The vocabulary is the printing industry's, chosen deliberately: *imposition* is the arrangement of pages at their correct positions on the press sheet. It is also forced — "layout" is the layout tree and the tugbank domain, "position" is the pane rect's origin. `tugdeck/src/lib/layout-imposer.ts` (`resolvePlacement` / `travelFraction` / `imposeStyle` / `imposeSidebarStyle` / `imposeRect` / `resolveSpan` / `isSidebarPinned` / `allocateSidebarWidths`), `layout-tree.ts` (`imposition` / `slot?` / `widthPreset?`, invariant 6: at most one pane per sidebar componentId, and it carries no slot), `deck-store-selectors.ts` (`findSidebarPanes`), `deck-manager.ts` (`setImposition` / `setSidebarSide` / `setContentWidth` / `setPaneWidth` / `pinLens` / `_unpinSidebar` / `assignCardToSlot` / `movePane` `evictSlot` / `arrangeCards` / `centerPane`), `components/chrome/tug-pane.tsx` (the pinned and imposed style branches, DOM-seeded drag conversion), `components/chrome/deck-canvas.tsx` (the `move-to-slot` handler), `components/tugways/keybinding-map.ts` (⌘1..⌘9), `components/lens/sections/layouts-section.tsx`, `components/lens/layout-miniature.tsx`, `components/lens/slot-picker.tsx`. Full doctrine: [pane-model.md § Three geometry modes](pane-model.md). `roadmap/layout-imposition.md` [P01]–[P12]; `roadmap/lens-joins-imposition.md` [P01]–[P10]. [L02], [L06], [L09], [L11]

**D128.** **The layout system speaks two card roles — content and sidebar — and content cards have three named widths.** A registration declares `layoutRole: "content" | "sidebar"` (`card-registry.ts`), defaulting to content. **Content cards are the ones the deck arranges**: they take N-Up slots, answer ⌘1..⌘N, and follow the width presets. **Sidebar cards pin to a deck edge and inset the band the content cards live in** — the Lens and Jots, and whatever registers next. A sidebar takes no slot, is skipped by `arrangeCards`, is refused by `assignCardToSlot`, and carries a `{ side, pinned? }` entry in the imposition record ([D121]). The taxonomy **constrains the layout system; it does not sort every card into two boxes** — the utility cards (settings, keyboard, devtools, gallery, about) declare nothing and are arranged like anything else. This was a *promotion, not an invention*: every piece of it already existed as a Lens-shaped special case (`findLensPane` matching one componentId, `imposition.lens`, `imposeLensStyle`, an allocator with one unknown), and lifting Snippets out of the Lens as its own card is what forced the special case to become a declared trait. **Both edges can carry a rail, and one edge can carry two cards.** Same-side cards **share a rail and stand front-to-back in it**, exactly as two panes sharing a slot do: same pin, same width, same full vertical run, with z-order deciding which one you see and the title bar's stack badge and picker ([D123]) the way to the other. That makes a rail a *place* in the same sense a slot is, which is the whole of why the badge applies to it unchanged. Rails hold a z-band above the free panes (a rail is never occluded by a card) and below the overlay base (or it would bury its own menus); inside the band the members take the deck's array order, so `activateCard` raises one over the other. **Dividing the rail's height between the members was tried first and rejected**: it spends a rail to show two half-cards, which is what the Jots section was already doing *inside* the Lens, only less space-efficient — and the point of lifting Jots out was to give it a whole rail when it is the one you are looking at. The design that lost persisted a seam split and a per-card vertical order; the one that shipped persists neither, because front-to-back has nothing to remember but each card's side. This matters out of the box rather than in a corner: the shipped default puts both Lens and Jots on the right, so the stacked rail is the deck a user meets. **When imposition takes or grants rail space, every visible rail lands on ONE shared width** — never a per-rail solve, so the deck cannot answer with two rails at two widths; sidebars are a uniform class the way content cards are. The shared width may grow to the **slim** content width (675) and no further, whatever Card Width the deck is set to — a rail is a reading surface, and a comfy- or wide-sized sidebar is absurd on its face — and may shrink to a fifth under the width the user chose, floored by the tightest `sizePolicy.min` among each rail's stack members. The chosen width the shrink allowance hangs under is the card's **durable** stored width (`lensStore` for the Lens, `sidebarWidthStore` for every other sidebar), never the live pane width: the live width is where the allocator writes its own answers, and an allocator that reads its output back as the user's preference re-anchors on every solve and ratchets one rail wide. If the fit fails, nothing moves at all, the allocator's existing "never half-apply" discipline. **Every Layouts click re-solves**, Card Width included — restamping the content panes moves every seam, so `setContentWidth` runs the same retune the kind rows run. **The widths are slim 675 / comfy 800 / wide 1230**, named constants in `layout-imposer.ts`. Slim is the reading width the Session card's chrome was put on a diet to reach and is therefore also its floor — a card that could go narrower than the narrowest width the deck offers would have a floor no gesture can reach. A preset is applied by a command through `movePane` and held between the pane's own `sizePolicy.min` and `max`, because `movePane` writes the rect it is handed without clamping and a card can beat a preset in either direction (Settings' 720 floor, About's 320 lock). The stamp (`TugPaneState.widthPreset`) records what the user chose even where the card's bounds won, and **any manual resize clears it** — a settled control shows what the geometry holds. Two doors: the **title-bar popup** sets one pane, and **Card Width** in the Layouts section sets the deck's default and puts every content pane on it at once, overwriting per-pane deviations. That asymmetry is the design — you say how wide content reads here, then dissent on the one card you want narrow — and it is why re-choosing the width the deck is already at is a real gesture rather than a no-op. A card registered with `takesContentWidth` opens at the deck's width instead of a number frozen into its registration. The **window-shade collapse was removed outright** to make room for the popup in the title bar: it was pointer-only — no command, no chord, no menu item — and old blobs carrying `collapsed: true` deserialize with the field dropped and the pane expanded. `card-registry.ts` (`layoutRole` / `takesContentWidth` / `isSidebarCard`), `lib/layout-imposer.ts` (`sidebars` / `contentWidth` / `maxRailWidth` / `CONTENT_WIDTH_*_PX` / `resolveContentWidthPx` / `allocateSidebarWidths` / `imposeSidebarStyle`), `deck-manager.ts` (`setSidebarSide` / `setContentWidth` / `setPaneWidth` / `retuneSidebarAllocation`), `components/lens/sections/layouts-section.tsx` (the three axes, sidebar controls built from the registry), `components/chrome/tug-pane.tsx` (the width popup). Full doctrine: [pane-model.md § Three geometry modes](pane-model.md). `roadmap/layouts-rework.md`; `roadmap/layouts-rework-plan.md` [P02]–[P06], [P10], [P11]. [D121], [L02], [L06], [L09], [L23]

---

## Snap Sets

**D32.** Snap requires Option/Alt modifier during drag (`altKey`).

**D33.** Set-move is always active once a snap set is formed — dragging one member moves all.

**D53.** Set members get squared corners via CSS `data-in-set` attribute.

**D54.** Set perimeter flash via SVG hull polygon (`computeSetHullPolygon()`).

**D55.** Break-out restores rounded corners (CSS-driven) and flashes individual card perimeter.

**D56.** Border collapse: snap positions offset by border width so adjacent cards share a single visual line.

**D57.** Interior set shadows hidden via `clip-path: inset()` on `.tug-pane-chrome`. Exterior edges extend by `SHADOW_EXTEND_PX`.

**D58.** Active/inactive shadow tokens: `--tug-card-shadow-active` (focused) and `--tug-card-shadow-inactive` (unfocused).

**D59.** Command-key (`metaKey`) suppresses card activation on click, allowing multi-card operations.

**D60.** Resize click activates the card (brings to front).

---

## Selection

**D34.** Three-layer selection containment: CSS `user-select: none` baseline, `SelectionGuard` runtime clipping, `data-tug-select` developer API. [L12]

**D35.** `SelectionGuard` is a module-level singleton, not React state. [L01, L12]

**D36.** Pointer-clamped selection uses `caretPositionFromPoint` (with `caretRangeFromPoint` fallback). [L12]

**D37.** Four select modes via `data-tug-select` attribute: `default`, `none`, `all`, `custom`. [L12]

**D38.** Cmd+A scoped to focused card via responder chain `selectAll` action. [L12]

**D122.** **The element marks; the container does not — and the element mark rides Accent while the selection fill rides Key.** Two cross-component rules, recorded together because the second is what keeps the first safe. (a) An **item-group container** — `TugListView`, `TugAccordion`, `TugRadioGroup`, `TugChoiceGroup`, `TugOptionGroup`, `TugTabBar`, `TugTabView`'s sidebar — marks keyboard focus with **nothing on the container**. The whole treatment is the mark on the **cursor item** inside it: a **ring** at full accent on an inline chip, a **leading-edge bar** at full accent on a full-width row. The container is `data-key-view-kbd`, so the global leaf-ring rule would stroke it; each component suppresses that with `outline: none` on its own root at one-class-plus-one-attribute specificity, and names `data-key-within` in the same selector. **Three container marks were tried first, and the record is the point.** (1) A full-accent perimeter: a bright rectangle around a bright ring, two questions in identical vocabulary at two scales, and the eye reads the big rectangle before the small one — backwards, since the cursor is what the next keystroke acts on. (2) A background wash, the two-*kinds* answer, so neither question competed; better on paper and it lost to the surfaces, because an area mark is a colour laid over a whole region and at any strength that reads it also tints every pixel of content sitting on it — brown over a mid-tone sheet, grey-tan over a pale one, each correction toward one surface degrading the next. (3) A toned, thicker stroke against a brighter, tighter element one, separating by *degree*; it shipped and still read as two rings, because tone is not enough separation between two closed rectangles a few pixels apart. The fourth answer is to stop drawing it: what all three were buying was "which container holds the keyboard", and an element mark *inside* the container already answers that. Retiring it also retired the machinery — `TugListView`'s perimeter needed a sticky overlay child, a JS-measured scrollport height and scrollbar width, a two-layer band split by the scroller's clip, a z-index lift, an inner-overlap offset the cursor caret had to read, and a carry-suppression rule, none of which has a subject now. The shape of an element mark follows the shape of the item: full-width row containers (list, accordion) mark the cursor with the bar (`--tugx-focus-cursor-bar-*`, a language-level knob precisely because two components draw it — one component reading the other's slot would breach [L20]); inline chip groups mark it with a ring. The bar is the loudest mark on the surface by design, and its trailing edge comes to a shallow point at the item's mid-height (`-point`) so it reads as an indicator aimed at the row. **The element mark is now the sole focus indicator an item-group has, which makes one registration rule load-bearing:** a group with nothing to cursor must be refused the focus group (the Lens sections gate theirs on `navigable`, the post-filter row count), or it takes the keyboard and paints nothing — a WCAG 1.4.11 failure. That constraint got stricter with this decision, not looser. A selected row still paints its own opaque ground (`--tugx-list-row-ground`) even though nothing paints behind the rows any more — the rule that a selection wins the pixels it covers is general, and the next thing to paint behind a row will find it settled. (b) The focus mark resolves from the **Accent** axis and a committed selection fill from the **Key** axis, and any new theme must keep the two distinguishable. This was already true everywhere — accent is orange in all six themes, the "on" selection token is cobalt in `brio` and blue in `harmony` — but it needed writing down, because the natural move for someone tuning a focus mark is to reach for the selection hue "so it matches." A role-bearing group overrides both from its own role token, keeping them distinct within the role. Full doctrine and the engine-attribute contract: [focus-language.md](focus-language.md). `roadmap/archive/focus-language-wash.md` [P01]–[P10] records the superseded wash design. [L06], [L20]

---

## Motion & Animation

**D23.** Motion tokens are CSS custom properties: `--tug-timing` (duration scalar), `--tug-motion` (binary toggle).

**D24.** Reduced motion via `--tug-motion: 0` and `data-tug-motion` attribute. Durations scale; motion doesn't simply disappear. [L13]

**D76.** TugAnimator wraps WAAPI. Named animation slots (WeakMap-based), three cancellation modes (snap-to-end, hold-at-current, reverse), animation groups, reduced-motion awareness. [L13, L14]

**D77.** Inactive selection uses CSS Custom Highlight API for dimmed highlight in unfocused cards.

**D78.** Child-driven ready callback: parent triggers child `setState`, child signals DOM commit via its own `useLayoutEffect`. No inline measurement of child DOM from parent. [L04]

**D79.** `requestAnimationFrame` is never used for operations depending on React state commits. RAF timing relative to React's commit cycle is not a contract. [L05]

---

## Scroll Behavior

**D93.** Smart auto-scroll for dynamic content uses a six-phase state machine — not scroll-event guessing. `SmartScroll` (a standalone class, not a React hook) owns a scroll container element and tracks six mutually exclusive phases: `idle`, `tracking`, `dragging`, `settling`, `decelerating`, and `programmatic`. The phase is the guard: if the phase is `dragging`, `settling`, or `decelerating` and scroll direction is up, a user is scrolling — disengage follow-bottom. A scroll change is ours only while the phase is `programmatic`, or in `idle` with the post-programmatic one-shot suppression armed for that event; any other `idle` scroll belongs to the **user** — chiefly the native scrollbar, whose thumb drag delivers no pointer or wheel events — and an upward one outside the at-bottom band disengages follow-bottom, while one landing inside the band re-engages it. The full attribution inventory and the supersede rules live in [scroll-intent.md](scroll-intent.md).

**Amended: "any other `idle` scroll belongs to the user" excludes machine-authored displacement that the owning component has identified as its own.** The browser clamps `scrollTop` when the scroll maximum shrinks, and a clamp is shaped exactly like a user scrubbing up — one `scroll` event, no pointer, no wheel, phase `idle`. Where a component can *prove* it caused the movement, treating it as intent converts machine noise into a user decision, and intent is sticky: a single clamp used to end follow-bottom for the remainder of a session. `TugListView` brackets its own commits, and a displacement it detects is attributed via `SmartScroll.noteExternalWrite()` — which syncs the baseline and advances the write counter, so the still-undelivered `scroll` event arrives explained and both disengage rules pass it over. Where no component can prove authorship, the original rule is unchanged and the user still wins.

**Amended again: the clamp is prevented, not repaired, and the bracket is a pure witness.** The clamp fires synchronously at **renderer removal, inside React's mutation phase** — no reader forces it and no scroll API can witness it — which supersedes the earlier "forced layout in the commit-phase gap" model and with it the earlier remedy. The defense is constructive: an extent floor (`.tug-list-view-floor`, written by the commit bracket to the settled extent every commit, up or down) holds the scrollable extent across the mutation gap so there is nothing to clamp against, and a lowering is a *declared rebase* that pins its own clamp and records it. The bracket no longer repairs: an unexplained displacement is counted, traced, and logged at **warn**, and the position is left exactly where the browser put it — a record means the floor has a hole, which is a bug to fix rather than a symptom to counter-write. `notifyRepair`, the repair suppression, and the `repaired` / `priorRepairHeld` fields are deleted. See [scroll-intent.md](scroll-intent.md) for the evidence, the floor's rules, and the witness contract.

Growth pins remain **phase-invisible** by design — `pinToBottom` writes `scrollTop` directly and deliberately stays in `idle`, so the phase machine still reports the user's state rather than the pin's. They are now **counter-visible**: `pinToBottom` advances `programmaticWriteSeq` on the branch that actually assigns, which is what lets the displacement bracket exempt a pin whose deferred `scroll` event has not been delivered yet. The counter counts writes that moved the scroller, not calls — advancing it on `pinToBottom`'s already-at-max early return would hold the exemption open across most streaming commits and blind the bracket exactly when the transcript is most active.

Phase transitions are driven by six DOM listeners: (1) **`scroll`** on container — fires callbacks and drives re-engagement check; (2) **`scrollend`** on container — terminal signal for deceleration and programmatic animation, with 150ms timer fallback for browsers without native support; (3) **`pointerdown`** on container, **capture phase** — enters `tracking`; (4) **`pointerup`/`pointercancel`** on document — exits `dragging` into `settling` (a 50ms window to detect momentum); if scrolls arrive within 50ms, transitions to `decelerating`; if not, transitions to `idle`; (5) **`wheel`** on container, **capture phase** — skips `tracking`, enters `dragging` immediately, starts scrollend fallback timer, disengages follow-bottom on `deltaY < 0`; (6) **`keydown`** on container — all scroll keys (PageUp, PageDown, Home, End, ArrowUp, ArrowDown, Space) enter `dragging` immediately and start the scrollend fallback timer; only scroll-up keys (PageUp, Home, ArrowUp, Shift+Space) disengage follow-bottom.

The two intent listeners run at **capture** so a user disengage is recorded synchronously ahead of every descendant listener of any phase — one that stops propagation (the Cmd-wheel inner-scroller route, [D94]) or one that flushes a commit whose layout effects pin to the bottom. `keydown` deliberately stays at the bubble phase: a key a descendant control consumes is that control's key, not scroll intent, and bubble semantics are exactly that filter. Capture registrations take capture-matched removals in `dispose()` — an options mismatch leaves the listener attached.

The `settling` phase is explicit state: `isUserScrolling` returns true during `settling` (pointer released, momentum outcome pending). The `_scrolledAfterPointerUp` flag pattern is replaced by an actual phase.

Content auto-scroll is **controller-driven**: `TugMarkdownView.doSetRegion` checks `isFollowingBottom` and calls `scrollToBottom()` after content updates. `SmartScroll` has no `ResizeObserver` — it does not observe content changes. Architecture studied from `use-stick-to-bottom` (StackBlitz Labs, MIT — see THIRD_PARTY_NOTICES.md). [L03, L06, L07]

**Scroll anchoring is gated by follow-bottom.** (This replaces "`overflow-anchor` is not used" — WebKit does honor it, and the transcript's reading-position hold depends on it.) Anchoring holds mid-viewport content still when heights change above it, which is definitionally opposed to the pin: while following, the live edge *is* the position and the pin is the only writer entitled to hold the viewport anywhere. `SmartScroll` stamps `overflow-anchor: none` on its container at the follow-bottom transition chokepoint and clears it on disengage, so descendants' opt-in goes quiet while engaged and resumes the moment the user parks to read. An inline style, not a class — the gate belongs to following-the-bottom itself, so every consumer gets it without per-surface CSS. [L06]

**Follow-bottom's polarity**: engaged is the resting state; disengaging requires a direct user action or a user-requested machine scroll that declares itself; re-engaging requires attributed arrival at the bottom. A machine scope that disengaged (an inline dialog's) may not simply restore the flag it suspended at close — it consults `isSettledAtBottom` (the user's position judged against the live geometry *or* the geometry they were last shown, the same judgment behind `growth-at-bottom-reengage`) and engages only if the user never left.

**D94.** **Cmd/Ctrl-wheel bypasses inner block scrollers and routes the wheel delta to the outer scrollport.** Inner block scrollers (FileBlock's CM6 scrollport, DiffBlock's hunks region, TerminalBlock's virtualized scroller, future code-fence viewers, future JSON-tree blocks, future structured-result panels) capture wheel events while the cursor sits over them, which stutters the outer card-scrollport skim across long transcripts. The escape hatch is surface-wide: **holding Cmd (macOS) or Ctrl (Win/Linux) while wheeling routes the delta straight to the outer scrollport, regardless of cursor position.** Plain wheel (no modifier) preserves current behavior — inner captures until exhausted, then bubbles to outer.

Mechanism: `useOuterScrollOnModifierWheel({ innerRef, outerScrollportRef })` (and its imperative twin `attachOuterScrollOnModifierWheel(inner, getOuter)`) attaches a capture-phase, non-passive `wheel` listener on the inner scrollport. On a Cmd/Ctrl-modifier hit, the handler calls `preventDefault` + `stopPropagation` and forwards `event.deltaY` to the outer scrollport via `scrollBy({ top, behavior: "auto" })`. Outer scrollport is read on every event so context-driven node swaps land without re-attaching. The hook is registered in `useLayoutEffect` so wheel routing is live before the first paint a user could plausibly wheel against.

The contract is surface-wide, not body-kind-specific: any new inner scroller a body kind introduces opts in by calling the hook with its inner-scroller ref. The same mechanism applies to pane-scoped scrollers and modal scrollers when those grow into the pattern. Cmd-click (new-tab), Cmd-+/- (browser zoom), Shift-wheel (browser-native horizontal pan), and Alt-wheel are unaffected — the listener is wheel-modifier-specific and intercepts only the Cmd/Ctrl axis.

**Bash render-on-scroll-into-view recovery.** TerminalBlock's virtualized scroller occasionally paints empty when the outer card scrolls it into view — a WebKit layer-invalidation hiccup or an IntersectionObserver-style virtualizer missing the first paint while the outer's clip rect is settling. The fix is a symptom-targeted refit: TerminalBlock subscribes to its outer scrollport via `IntersectionObserver` (root = outer scrollport, threshold 0) and re-runs the virtualizer's `applyUpdate(scrollTop)` on every entering-view transition. A `scroll` listener on the outer (gated on `inView`) covers sub-IO-threshold deltas. Refit is idempotent when no enter/exit ranges change, so spurious fires are free. [L03, L05, L06, L22]

---

## Feed & Transport

**D19.** Transport is tugcast binary frame protocol, not a separate HTTP server. `FeedId` enum routes frames.

**D21.** Interface-first development: define TypeScript interfaces, mock backend, then implement frontend.

**D109.** `TugConnection.onFrame` returns an unsubscribe closure (mirroring `onDisconnectState`); short-lived subscribers (per-card stores) capture it and release on `dispose()`. The `_disposed` guard is retained as a secondary safety, not as the leak fix. [L27]

---

## Tugbank

**D46.** Tugbank is a SQLite-backed typed defaults store. `DefaultsStore` wraps `rusqlite::Connection`. Typed `Value` enum: `Null`, `Bool`, `I64`, `F64`, `String`, `Bytes`, `Json`.

**D47.** Per-domain key-value storage with CAS (compare-and-swap) concurrency via monotonic generation counters. `set_if_generation()` returns `Written` or `Conflict`.

**D48.** Frontend reads/writes tugbank via HTTP bridge endpoints.

---

## Observable Properties & Inspector

**D67.** `PropertyStore`: typed key-path property store per card. Schema defined via `PropertyDescriptor[]`.

**D68.** `PropertyStore.observe()` is directly compatible with `useSyncExternalStore`'s subscribe contract. [L02]

**D69.** Inspector panels participate in the responder chain.

**D74.** Dev cascade inspector: `Ctrl+Option + hover` shows token resolution chain for any element.

---

## Code Session & Transcript

**D96.** **Any code path that lands a `TurnEntry` on `state.transcript` must also seed the per-turn `streamingDocument` paths (`turn.${turnKey}.{assistant,thinking,tools}`) from the entry's payload.** This is the write-side counterpart to [L26]'s post-unification render contract: the assistant row's `AssistantTurnCell` observes `turn.${turnKey}.${channel}` exclusively (no fallback to `TurnEntry.*` fields), so a turn that exists on the snapshot but whose per-turn paths are empty renders blank — a textbook [L23] violation. Today the only such code path is `code-session-store/reducer.ts`'s `append-transcript` effect, paired with the `write-inflight` effects emitted by `handleTextDelta` / `handleToolUse` / `handleToolResult` / `handleToolUseStructured` for both live and replay events (the live↔replay symmetry restored by Step 18.9). Any future analogue — out-of-band ingestion, server-pushed transcript snapshot, debug-tool import, hot-reload state restore — must replicate the seeding using `serializeToolCalls` (`reducer.ts`) for the tools channel and the raw string for assistant/thinking. The reducer's pattern is the reference implementation; deviating from it without seeding strips the corresponding content from every rendered cell that comes through the alternate path. [L23], [L26]

**D97.** The session card is partitioned into **six placement zones, `Z0`-`Z5`, numbered spatially top-to-bottom**, with `Z1` subdivided into per-turn `Z1A` / `Z1B` and a per-row `Z1C` indicator zone, and `Z4` subdivided into two prompt-entry toolbar sub-slots, `Z4A` and `Z4B`. Each zone is an addressable region the telemetry renderers and the assistant-rendering registry target by ID. Most are `ReactNode` slot props on a host component; `Z5` is a state-coordinated interactive area rather than a content slot. `Z4A` and `Z4B` are *layout positions* — a leading-fixed slot and a centred-floating slot — whose occupants the toolbar assigns and is free to swap (below). Spatial numbering means any zone is findable by intuition, and a future zone inserts at its position with a clean downstream renumber — `Z0` is unambiguously "what you see first."

```
┌─ session card · transcript pane ────────────────────────────────────────────────┐
│ Z0   top of card — reserved (empty until its slot is filled)                 │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│ SessionTranscriptHost  →  TugListView          scrolls · flex 1 1 auto          │
│                                                                              │
│   ┌─ user row ───────────────────────────────────────────────────────────┐   │
│   │ "count lines of code..."                                Z1 user half │   │
│   │                                                           (reserved) │   │
│   └──────────────────────────────────────────────────────────────────────┘   │
│   ┌─ assistant row ──────────────────────────────────────────────────────┐   │
│   │ [ markdown body · tool-call blocks · thinking ]                      │   │
│   │                                               Z1A  model · timestamp │   │
│   │ ◌◌◌                                          Z1C  in-flight          │   │
│   │                                                    indicator         │   │
│   │                                                    (in-flight-tip    │   │
│   │                                                    row only;         │   │
│   │                                                    collapsed         │   │
│   │                                                    otherwise)        │   │
│   │ [copy]                                       Z1B  end-state (when    │   │
│   │                                                    a committed turn  │   │
│   │                                                    has assistant-    │   │
│   │                                                    side content;     │   │
│   │                                                    collapsed         │   │
│   │                                                    otherwise)        │   │
│   └──────────────────────────────────────────────────────────────────────┘   │
├──────────────────────────────────────────────────────────────────────────────┤
│ Z2  STATE · TIME · TOKENS · CONTEXT · WORK                                   │
│            (WORK cell per [D107], unifying [D100]/[D102]; it reads a         │
│             dimmed None when its feed is empty)                              │
│      flex 0 0 auto · never scrolls · sits outside TugListView                │
└──────────────────────────────────────────────────────────────────────────────┘
             ↑↓  split-pane sash — transcript / prompt-entry resize
┌─ session card · prompt-entry pane ──────────────────────────────────────────────┐
│ Z3   prompt-entry status row — reserved (collapses to zero height)           │
│                                                                              │
│   Ask Claude to build, fix, or explain                                       │
│                                                                              │
│ [Prompt|Changes]       [Project: /path] [Claude Code 2.1.148]          [ ↑ ] │
│ Z4A leading-fixed · Z4B centred-floating · Z5 trailing-fixed                 │
└──────────────────────────────────────────────────────────────────────────────┘
```

The find bar ([D122]) is a fourth row in the transcript pane when it is open, inserted between the view slot and `Z2`: transcript / find bar / `Z2` / PULSE. It is a flow sibling at `flex 0 0 auto`, so the view slot cedes the height exactly as it does for `Z2` telemetry growth and the scroller's `scrollTop` is untouched — a mid-scroll transcript keeps its top edge and only loses viewport height from the bottom. It is deliberately NOT a shade: it lives outside `ShadeViewController`'s `"none" | "changes" | "history"` union, and the History shade rises from the top of the *bar* while it is open. Being outside the union is a claim about **structure**, not about coexistence — ⌘F and Changes do exclude each other ([D122]), because both are modes that take over the bottom of the card. A commit message in progress is never lost to that: the exclusion routes through `CommitModeController.leave()`, which persists it first.

| Zone | Host · slot prop | Current occupant | Status |
|------|------------------|------------------|--------|
| `Z0` | `SessionCard` · `headerContent` | — | reserved — empty |
| `Z1` | transcript row · `renderTurnTrailing`, keyed by `half` | user half: —; assistant half: `Z1A` model · timestamp + `Z1B` end-state aggregate | user half reserved; assistant half shipped |
| `Z1B` | per-turn end-state aggregate in `TugTranscriptEntry`'s `controls` slot — the OK / interrupted / error / transport_lost badge, per-turn token count, per-turn wall-clock, whole-turn COPY. Rendered by `SessionZ1B`. The slot collapses to zero height (no `min-height`) when there is no end-state to show (in-flight rows). | rendered when the committed turn has an assistant-side Message (per Step 5.6 [Decision F](../roadmap/tugplan-tide-session-wake.md#step-5-6)). NOT rendered for a turn that committed with only a `user_message` (interrupted-before-response) — the user_message row's own static "OK" footer is the only chrome. NEVER multiplexes the in-flight indicator (Z1C owns that) | shipped under Step 5.6 |
| `Z1C` | per-row in-flight indicator zone in `TugTranscriptEntry`'s new `inflightFooter` slot — sandwiched between the body and the `controls` slot. *Every* `TugTranscriptEntry` carries the slot uniformly; the slot collapses to zero height when `inflightFooter={null}`. NOT a list row; adds nothing to `totalRows`; no scroll-to-row helper needs to skip it; no React key churn. | `SessionZ1C` — only the **in-flight-tip row** mounts an active Z1C with a `useSyncExternalStore` subscription to a memoized `{phase, interruptInFlight}` selector; all other rows pass `null` to the slot and stay subscription-free. Indicator content (visible glyph is the three-bar `TugThinkingIndicator` with `labelPosition="hidden"`; phase label rides on `aria-label`) resolves via `sessionZ1CContent(phase, interruptInFlight)` — returns the indicator for `submitting` / `awaiting_first_token` / `streaming` / `tool_work` / `waking`; returns `null` (slot collapses) for `awaiting_approval` (the pending dialog *is* the affordance), `interruptInFlight === true` (interrupt is instant from the user's POV — Z1B paints the end-state once the turn commits), and `idle` / `replaying` / `errored`. In Step 5.8 the in-flight tip is the in-flight code row (`!isCommitted`); in Step 5.9 it is the row carrying the Message flagged `isLastInflightMessage`. Position inherits from the row body's indent and footer placement — no absolute positioning. | shipped under Step 5.8 |
| `Z2` | `SessionCard` · `statusBarContent` | `SessionTelemetryStatusRow` — five cells, STATE · TIME · TOKENS · CONTEXT · WORK, and nothing flanking them (the sash grip and maximize toggle the diagram once showed are long gone; the BTW cell went with the Z2 diet that made the slim width preset fit — the `/btw` answer is a placard popping from the strip's top-right now, [D128]). The `WORK` cell (per [D107], unifying [D100]/[D102]) renders a pulsing-dot + merged headline label (`goal`, `finished/total`, a scheduled count, or dimmed `None`) driving a grouped popover — Goal / Running / Scheduled / Checklist / Finished — with stop / cancel / stop-loop / clear / clear-goal actions. Beneath the row sits the one-line `PULSE` strip (per [D103]) — ambient app-wide color commentary at fixed height, hidden when the `pulse/enabled` default is off. | shipped |
| `Z3` | `TugPromptEntry` · `statusContent` + `cautionContent` | — (the session card no longer fills it) | reserved — collapsed |
| `Z4A` | `TugPromptEntry` toolbar — leading, fixed | route choice-group — two routes, `Prompt` / `Changes`, per [D122]. Controlled by `CommitModeController`, so it holds no state of its own; absent entirely when the host passes no controller (the Component Gallery) | shipped |
| `Z4B` | `TugPromptEntry` toolbar — centred, floating | `indicatorsContent` — project-path + Claude Code badges | shipped |
| `Z5` | `TugPromptEntry` submit button (no slot) | lifecycle-driven Submit / Stop / Awaiting / Stopping / Reconnecting | shipped |

The transcript pane (the upper `TugSplitPanel`) is a flex column — `Z0` header, `TugListView` (`flex 1 1 auto`, scrolls), with `SessionJumpToBottomButton` as floating chrome anchored at the list's bottom edge, and `Z2` status bar — so a telemetry update in `Z2` grows into space the list cedes without repositioning the scroll, and `Z2`, living *outside* `TugListView`, never scrolls with the transcript. `Z1` is per-turn rather than card-level: one trailing slot wired twice per turn (`half: "user" | "assistant"`), spatially inside the transcript pane between `Z0` and the `Z2` row. `Z1B` (the per-turn end-state aggregate) and `Z1C` (the per-row in-flight indicator zone) are intentionally separate sub-zones — `Z1B` lives in `TugTranscriptEntry`'s `controls` slot (collapses to zero when empty); `Z1C` lives in `TugTranscriptEntry`'s new `inflightFooter` slot, sandwiched between the body and `controls` (every row carries the slot uniformly; only the in-flight-tip row mounts a Z1C that subscribes to phase and paints the indicator; all other rows pass `null` and the slot collapses). The pre-Step-5.6 `Z1B` multiplexed both responsibilities ("indicator ↔ end-state display") inside one component; under per-Message rows the multiplex is structurally awkward, so the two responsibilities split into two `TugTranscriptEntry` slots with disjoint domains and independent collapse rules. An earlier revision of `Z1C` placed it as transcript-level chrome below `TugListView`; that ended up marooned at the bottom of the pane instead of at the in-flight row's footer, so the design moved to a per-row slot that inherits position from the row body's indent for free. `Z5` is the submit button — a single DOM node whose label / `disabled` / `data-mode` are driven by the lifecycle state machine and which is never swapped across mode transitions, per [L26].

The prompt-entry toolbar lays out three zones in one flex row. `Z4A` sits at the leading edge and `Z5` (the submit button) at the trailing edge — both fixed and content-sized; `Z4B` floats centred between them, two equal flex spacers splitting the free width so `Z4B`'s centre lands at the midpoint of the `Z4A`–`Z5` gap. `Z4A` and `Z4B` are *positions*, not component assignments: the toolbar fills them with the route choice-group and the indicator cluster (the `indicatorsContent` slot — the project-path and Claude Code badges, the identity chrome naming the prompt entry's target), and which occupant takes which position is a layout decision free to change. Today the two-route choice-group sits in `Z4A` and the indicator cluster in `Z4B`. Both of `Z4A`'s segments are always mounted, so the group's width is constant and a route switch never shifts the centred `Z4B` occupant — which is what makes the Prompt↔Changes switch the honest stimulus for the flanking-geometry assertion in app-test `at0215`. The maximize toggle and the prompt-area sash grip are card chrome, not zone occupants — both sit in the `Z2` status bar (grip on the leading edge, maximize on the trailing edge), which is why the `Z3` prompt-entry status row, once home to the badges and the toggle, is now empty and collapsed.

A zone's *location* is contract; its *occupant* is not — every zone is a generic addressable slot. `Z0`, the `Z1` user half, and `Z3` are reserved: the slot exists in the API, default content is `null`, and the row collapses to zero height until something fills it (the session card leaves `Z3` empty today, so its row has no height). `Z1C` is a per-row `inflightFooter` content slot on `TugTranscriptEntry`: every row carries it uniformly and the slot collapses to zero when passed `null`; only the in-flight-tip row passes a live `SessionZ1C` instance. Established by the Step 20.x turn-surface work (archived as `roadmap/archive/tide-assistant-turns.md`); `Z3`'s `cautionContent` slot was added by the assistant-rendering drift-detection work; the `Z4A` / `Z4B` split and the `Z2` chrome relocations came from the prompt-entry-zones work (`roadmap/tugplan-tide-prompt-entry-zones.md`); the `Z1B` / `Z1C` split (separating per-turn end-state from per-row in-flight indicator) came from Step 5.6 of the tide-session-wake work (`roadmap/tugplan-tide-session-wake.md`) under the per-Message render-layout refactor and was reshaped in Step 5.8 from "transcript-level chrome below `TugListView`" into the per-row `inflightFooter` slot after the transcript-level placement marooned the indicator at the pane's bottom edge. [L06], [L26]

**D100.** The active task list surfaces as a `TASKS` cell on the [D97] `Z2` status bar — not inline in the assistant turn(s) that issued each call, and not as a separate sub-zone. Anthropic's `claude` ≥ `v2.1.148` replaces the old `TodoWrite` tool (single call carrying the canonical list) with a per-item CRUD family — `TaskCreate` (one call per task; the server assigns a monotonic `taskId`) and `TaskUpdate` (`{ taskId, status }` flips one task's status). A typical session produces ~3× more events than the old `TodoWrite` did (4 × `TaskCreate` up front + 8 × `TaskUpdate` interleaved for a 4-item list is representative), so inline-per-turn rendering would litter the transcript with a status-flip per row; the status-bar cell keeps the live picture one fixed glance away without claiming separate vertical real estate. The cell renders a `TugProgress` ring + `N/M` label (`done` over `total`) inside `SessionTelemetryStatusRow` alongside `STATE` / `TIME` / `TOKENS` / `CONTEXT`; clicking it opens a popover whose body is a vertical stack of `TugTaskItem` rows (one per task, status-driven indicator + `subject`, with the optional `description` surfacing in a `TugTooltip` on hover). When no task list is active the cell still occupies its reserved width but reads `None` (dimmed) and the ring is suppressed — the layout stays anchored so the surrounding cells never reflow. When the session is `idle`, the ring renders in its `stopped` state (closed outlined circle, no animation) in both the cell and the popover, so motion implies ongoing work and stillness implies it is over. The cell is fed by a pure reducer — `reduceTaskListState(toolCalls): TaskListState` in `select-task-list.ts` — that folds the event stream by walking `transcript[].toolCalls[]` then the in-flight `toolCallMap`'s values in order, appending one `TaskItem` per `TaskCreate` (with `status: "pending"` and `taskId` parsed from the `tool_result.content` "Task #N created successfully:" echo, falling back to monotonic count) and mutating-by-id for each `TaskUpdate`; only terminal (`status: "done"`) calls fold so an in-flight `TaskCreate` with no assigned id yet is skipped until its `tool_result` lands. The cell renderer reads through `useTaskListState` (a `useSyncExternalStore` hook per [L02]) and the popover composes `TugTaskItem` primitives directly, with role-driven accent colors (in-progress rows use `TugProgress role="action"` + `TugLabel role="action"`; completed rows use `--tugx-task-item-success-color` on the check glyph) so the surface picks up standard tone tokens instead of bespoke aliases. No wire-level clear event exists; the list lives for the session, and the active rule (non-empty list with at least one non-completed item) governs the `N/M` vs `None` reading. Session clear (`/clear`) resets the cell by way of resetting the store. The TASKS cell is the canonical surface for *current state* — the assembled list, one fixed glance away. The transcript carries per-call inline markers via `TaskInlineToolBlock` — a tiny `<ListChecks size=14>` + `<TugLabel size="sm" emphasis="calm">` row for each `TaskCreate` and `TaskUpdate` event (`"Created: <subject>"` / `"Started: <subject>"` / `"Completed: <subject>"`), so a reader scrolling the transcript can see *when in the conversation flow* each task action happened. The two surfaces never duplicate work: the cell answers "what's on the list now?"; the marker answers "when did this happen?". The marker uses the `calm` emphasis (italic + muted gray) so every row reads as ambient annotation subordinate to the TASKS cell — color is reserved for the rare error case (`role="danger"`, `emphasis="normal"`). The marker intentionally carries no `BlockChrome` — no frame, no status stripe, no error band — so it stays inline-flow, not another tool-call card competing with the cell for attention. The dead `registerToolBlock("todowrite", …)` entry is removed; the original Step 24's inline `TodoWriteToolBlock` was already silent on every `v2.1.148+` session — `TodoWrite` is no longer emitted — so the broader rework is also a correctness fix, not just a UX one. **Superseded (cell layout only):** the standalone `TASKS` cell merged into the unified `WORK` cell per [D107]; this decision's storage and grammar doctrine (the derived turn-scoped fold, idle demotion, the `N/M` reading, the inline markers) stands and is preserved inside the merged surface.

**History.** D100 originally specified a separate pinned sub-zone — `Z2A` (leading-fixed) paired with a renamed `Z2B` (trailing). That iteration shipped briefly (a `SessionCardPinnedTodo` renderer + `statusBarLeadingContent` prop), but produced too much vertical-layout shift and broke the cell-row rhythm of `Z2`. The decision was reverted to single `Z2` + the `TASKS` cell described above; the `Z2A` / `Z2B` split is no longer present in the code, and D97's zone table reflects single-`Z2`. The original artifacts (`SessionCardPinnedTodo`, `statusBarLeadingContent`, `--tugx-pinned-todo-*` tokens, `:has()`-based collapse) were removed. [D97], [L02], [L06], [L20]

**D101.** Tool visibility in the Dev transcript is classified by a single editable policy table — `TOOL_VISIBILITY_POLICY` in `tugdeck/src/components/tugways/cards/session-tool-visibility-policy.ts` — with three buckets and one source-of-truth rule per bucket. **Bespoke** is implicit: a tool has a bespoke wrapper iff it is registered in `TOOL_BLOCK_REGISTRY` via the dispatch's bottom-of-file `registerToolBlock(...)` calls (the `BESPOKE_REGISTRATIONS` array exported as `BESPOKE_TOOL_NAMES`). Bespoke names MUST NOT appear in the policy table — that would be a double-classification, caught by the governance test. **Hidden** is explicit: the policy table's `hidden` entries map to a shared exported `NullToolBlock` via two short-circuits — `resolveToolBlock` returns `NullToolBlock` for any hidden name ahead of the registry lookup, and `detectToolCallDrift` exempts hidden names from the `unknown_tool` check. Hidden is for control-channel machinery the user does not need to see (`ToolSearch`, `ScheduleWakeup`, `EnterPlanMode`, `ExitPlanMode`, `PushNotification`) or for tools whose surface lives elsewhere (`TaskCreate` / `TaskUpdate` per [D100] — the TASKS status-bar cell is the sole surface). **Default-intent** is explicit: the policy table's `default-intent` entries become `AUDIT_CONFIRMED_DEFAULT_TOOLS`, routing through `DefaultToolBlock` (the JsonTree fallback) with no `unknown_tool` caution. Every `default-intent` entry's `rationale` MUST cite the follow-on step's `#step-` anchor — the governance test enforces it, so a default-intent entry is always an *explicit TODO* with a planned bespoke wrapper, never a forever-bucket. The governance test (`__tests__/session-tool-visibility-policy.test.ts`) pins the two invariants that catch real mistakes code review would miss: (c) every `default-intent` entry's rationale contains `"Awaiting"` and `"#step-"` — the only mechanism preventing `default-intent` from becoming a forever-bucket; and (d) the union of bespoke + policy covers the v2.1.148 canonical tool set — so a new Claude Code tool in a future release fails CI until it is explicitly classified. Shape / parse / no-double-classification checks were considered and intentionally not landed: the entry shape is enforced by the `ToolVisibilityEntry` interface, and double-classification is rare-and-obvious enough that code review is the right gate. MCP (`mcp__*`) is intentionally excluded from the policy per the project's MCP non-goal — `mcp__*` names route through `DefaultToolBlock` and produce `unknown_tool` cautions; the caution count is the deferral's signal. Moving a tool between buckets is a one-line policy edit; promoting to bespoke means a new `registerToolBlock` call + removing the policy entry. [D04], [D11], [D100]

**D102.** AI-initiated background jobs surface as a `JOBS` cell on the [D97] `Z2` status bar, mirroring the `TASKS` grammar exactly ([D100]) so the row reads as one instrument: a `TugProgressIndicator` pulsing-dot + `finished/total` label (dimmed `None` when empty), a popover behind a click. The cell's pose is a pure function of the ledger only — `jobsCellPose` in `select-jobs.ts`: any running job → `running`; else any failed → `aborted` (the danger pose [D100] reserved gets its first consumer; it holds until the user clears it, a new job starts, or the session clears); else `completed`. **No idle demotion** — the one deliberate semantic divergence from TASKS: a background job genuinely runs *between* turns, so an idle session must not quiet a running dot. The feed is a **session-lifetime jobs ledger** held in `CodeSessionState.jobs` (not a pure fold over `toolCalls` — the lifecycle events are not all tool calls, and Clear must *forget* rows): claude ≥ 2.1.173 emits `system/task_started` / `system/task_updated` frames for background `Bash` / `Agent` (`run_in_background: true`), which tugcode forwards verbatim as `task_started` / `task_updated` IPC frames from **both** routing tiers (the terminal flip arrives mid-turn for a fast failure, inter-turn for an idle completion). Ledger updates are idempotent upserts/flips keyed by claude's `task_id`: inserts from the `task_started` frame AND the launch `tool_result` echo (`backgroundTaskId` / `agentId`), terminal flips from `task_updated.patch.status` (`completed` / `failed` / `killed`→stopped), the `wake_started` trigger fold, and a defensive `TaskStop`-tool fold — first terminal flip wins. **The wire carries no backgrounded discriminant**: a foreground subagent's `task_started` is shape-identical (the empirical contract lives in `stream-json-catalog/v2.1.173-jobs-spike/`), so inserts gate on the launching tool call via `tool_use_id` — `isJobLaunch`: explicitly backgrounded (`input.run_in_background === true`) **or a `Monitor` watcher**, which is background activity by nature (a watcher's `task_type` reads `local_bash`, so its `kind: "monitor"` derives from the tool name). The gate also keeps an async subagent's internal jobs out (their launching calls aren't in the deck's stream). **Monitor rows have their own flip rule**: a watcher reaches terminal only via `task_updated` — the wake-trigger fold exempts `kind: "monitor"`. On the captured wire this is defense in depth (mid-life monitor events wake claude via task-id-less re-inits, and a monitor's only `task_notification` is terminal with a status agreeing with `task_updated`: completed/completed on natural exit, killed/stopped on timeout or kill), but the exemption pins the invariant against future per-event notifications. Replay never populates the ledger (a replayed launch would pulse forever — its lifecycle frames don't replay), and a fresh `session_init` stale-marks `running` rows to `stopped` (a respawned claude cannot carry jobs across). The popover (`JobsPopoverContent`) lists per-job rows — status dot, end-truncated description (tooltip for the full text), elapsed (live 1Hz leaf that mounts only while the popover is open), and a stop button on running rows that fires the pre-existing `stop_task` verb (`CodeSessionStore.stopJob` → tugcode `handleStopTask` → control request); no optimistic flip — the capture confirmed claude answers with `task_updated{killed}` + a `stopped` notification. The footer carries the zero-bucket-drop summary — live watchers split out of the running bucket as "watching" (`1 running, 1 watching, 2 done`), while the cell's `finished/total` and pose treat all rows identically — and a **Clear** button: a deck-local wipe of terminal rows only — running rows always survive — disabled when nothing is clearable. Terminology guard: despite the wire's `task_*` spelling these frames are the *jobs* vocabulary, unrelated to the `TaskCreate`/`TaskUpdate` tool calls behind TASKS; deck-side names use the job vocabulary throughout. Layout: TIME / TOKENS narrowed 16ch→12ch to pay for the 14ch JOBS cell; container-query collapse order TIME → TOKENS → TASKS → JOBS (a running job is live signal; the task list is recoverable from the transcript). Originating plan: `roadmap/jobs-tracking.md`. **History.** As first shipped, the gate was flag-only (`run_in_background === true`) and monitors were invisible — an armed watcher had no surface for its entire lifetime until it fired, and no kill switch. The `Monitor` extension (gate, kind, flip exemption, the "watching" summary bucket) came from `roadmap/monitors-in-jobs.md`, pinned against `test-monitor-lifecycle-raw.jsonl`. **Superseded (cell layout only):** the standalone `JOBS` cell merged into the unified `WORK` cell per [D107]; the ledger, gates, folds, and no-idle-demotion doctrine stand. (The `session_init` stale-mark now flips `running` rows only — scheduled rows survive, since every tugcode respawn resumes and claude ≥ 2.1.204 re-fires pending scheduled work; probe: `tugcode/probes/goal-loop/FINDINGS.md#q02-loop`.) [D97], [D100], [L02], [L06], [L26]

**D107.** Every trackable unit of session work surfaces through ONE `WORK` cell on the [D97] `Z2` status bar, replacing the separate `TASKS` ([D100]) and `JOBS` ([D102]) cells — the row reads STATE · TIME · TOKENS · CONTEXT · WORK (freed width returned to TIME/TOKENS; collapse order TIME → TOKENS → WORK). **The surface unifies; the storage does not**: the task list stays [D100]'s derived turn-scoped fold, the jobs ledger stays [D102]'s session-lifetime state, and the `/goal` is its own snapshot field (`CodeSessionSnapshot.goal`, reduced in `select-goal.ts` from `/goal` submissions, `goal_feedback` frames, and the goal cycle's commit — a goal run is ONE result cycle, probe: `tugcode/probes/goal-loop/FINDINGS.md#q01-goal`). `select-work.ts` is pure projection: `WorkItem` rows grouped Goal / Running / Scheduled / Checklist / Finished, a merged pose (`workCellPose`: an active goal or any running/scheduled job → `running`, never idle-demoted per [D102]'s divergence; else a failed job holds `aborted`; else the checklist keeps [D100]'s idle demotion), and a headline label (`workCellLabel` precedence: active goal → jobs fraction → scheduled count → checklist fraction → dimmed `None`). The popover (`WorkPopoverContent`) composes the real row internals from the surfaces it merges and carries every management action: stop job (`stopJob`), cancel cron (`cancelScheduledWork`), stop a wakeup-paced loop (`stopLoop` — a real user message asking the assistant to end the loop with `ScheduleWakeup {stop:true}`), clear finished rows, and clear the goal (`clearGoal` sends `/goal clear`, idle-gated — a live goal run is stopped via the interrupt control). `/tasks` (alias `/bashes`) is a Tug-local command whose surface is this popover (`SessionTelemetryStatusRowHandle.openWorkPopover`, the `/context` imperative-open precedent). Vocabulary: `WorkItem` is the deliberately distinct umbrella term over the deck's three work-shaped nouns (`Task*` tools = checklist, `task_*` frames = jobs, scheduled work) — nothing on the wire spells "work". Originating plan: `roadmap/slash-command-plan.md` ([P02]/[P03]). [D97], [D100], [D102], [L02], [L06], [L26]


**D103.** PULSE — app-wide color commentary — **watches the wire**: the commentator narrates from the same session-tagged frames every deck receives, never from a hand-phrased re-telling, displayed as a one-line strip beneath the [D97] `Z2` status row. (The v1 architecture — a route-agnostic `pulse_fact` note-card bus fed by a tugcode producer observing outbound IPC — was retired by `roadmap/pulse-2.md`: every lossy hand-off was a place truth thinned out, and when the untyped observer silently mismatched the wire shape, the speak-pressured model fabricated. Generality was the *cause*: a narrator kept dumb about every route could only eat pre-chewed prose. A truthful Claude-Code-only PULSE beats a general one that lies; future routes integrate via their own tap + digester.) The architecture: **the tap** — tugcast's app-scoped pulse bridge (`feeds/pulse.rs`) subscribes to the shared `CODE_OUTPUT` broadcast and forwards an allowlisted frame subset (`tool_use`, `tool_result`, `assistant_text`, turn boundaries, task/job transitions, `api_retry`, `error`) verbatim to the daemon's stdin, spliced `tug_session_id` intact; `replay_started`/`replay_complete` are consumed bridge-side as a per-session mute set so reconnect floods never re-narrate history; a lagging broadcast receiver drops frames (commentary never backpressures work). The bridge lazily spawns/supervises the daemon, persists its lines in a capped (~200) `pulse_lines` ledger table, and broadcasts them on the `PULSE` feed (`0x80`). A reconnecting deck fetches the recent tail via the `list_pulse_lines` → `list_pulse_lines_ok` CONTROL pair (the `list_session_state_changes` shape — never feed replay), and the daemon re-seeds its inner session from the same tail at spawn. **The line machine** is `tugpulse`, a tugcode-codebase sibling binary that parses the frames with the REAL `OutboundMessage` types (shape drift is a compile error, not a silent starve) and runs a **deterministic per-scope ticker** (`pulse/ticker.ts`) — no model in the loop. (The model-based commentator was built, walked, and retired across nine prompt versions, all recorded in the spike README: when it spoke often it restated the visible transcript; when it spoke rarely it was unpredictable. Color commentary has no stable niche beside a transcript the reader follows natively.) The ticker makes the strip a live "what's happening now" readout — the gap the STATE cell's bare lifecycle word leaves during long turns: the newest started call as its label ("Bash: cargo nextest run"), elapsed refreshes for slow calls (5s/15s/30s/60s then every 30s), failures immediately and always with a real output excerpt, recoveries when a previously-failed call shape goes green ("ok after 1 failed try"), background-job transitions, API-retry strides, and a per-turn summary ("turn: 14 calls, edited reducer.ts, 1 failed — 2m 04s"; a no-tool turn reads "turn: direct reply"). Routine line changes are throttled (~1.5s) per scope so bursts don't strobe; failures, recoveries, jobs, and turn boundaries bypass the throttle. Every line covers exactly one session — attribution by construction. Behavior is rule-shaped and instantly predictable; there is nothing to fabricate and no per-line latency or cost. **One-way isolation is structural**: commentator output flows only deck-ward; no code path leads from the pulse bridge into any session bridge's stdin. **Default ON**, killed by the `dev.tugtool.pulse/enabled` tugbank default (consulted per spawn opportunity — a flip takes effect without a tugcast restart; absent reads enabled). Deck-side, the app-scoped `pulse-store` ([L02]) folds the tail + live frames (rolling cap 20, line-identity dedupe) and surfaces the toggle on its snapshot; `SessionPulseStrip` renders the newest line at fixed height with a fade keyed on line identity ([L06]), a dimmed placeholder before the first line, hidden entirely when disabled. Display is PER-CARD: the strip filters the app-wide log to the card's own session via `latestLineForScope` — a new session must never wear another session's commentary (one narrator, per-card display). Every v2 line carries exactly one scope; `"app"`-scoped (or scope-less) lines remain ambience for all cards (a future explicit feature on the same wire). The Shelf/Rack productionization (`roadmap/z2-status-redesign.md`) consumes this store for its PULSE lane and row item. Originating plans: `roadmap/pulse.md` (v1), `roadmap/pulse-2.md` (the wire-tap rework). [D97], [D102], [L02], [L06], [L19], [L20], [L26]
**D104.** Tool-block renderers share three consistency conventions, upheld by authorship and review rather than enforced as tuglaws (they are implementation facts, not locked-in project law). **(1) Status is the dot's; data is the badge's.** Lifecycle (`completed` / `running` / `failed` / `deleted`) is carried by the header's pulsing dot alone ([D02]); a renderer never paints lifecycle text in the detail column or a body field row. The one trailing `resultSummary` badge slot carries *data* — a compact token (`936 lines`, `19 calls`, `exit 0`, `deleted`) — never a sentence; sentence-shaped domain output stays in the body under an honest label (e.g. `result`, not `status`). **(2) One fold per block.** The chrome's whole-block chevron is the single fold whenever the block is collapsible; a body kind shows its own fold cue only where there is no chrome chevron (a nested tool dispatched at `depth + 1`, with no `ToolBlockHistoryCollapse` wrapper). The signal is `BlockFoldSuppressedContext` — truthy means the chrome owns the fold, so the body suppresses its cue. **(3) Streaming body is empty; the dot is the signal.** Per [D02] a streaming tool passes `body = null`; there is no shared `StreamingPlaceholder` (it was specified but never built, and the docstrings that named it were swept). The lone exception is the Agent block, whose `AgentWorkingBody` fills its uniquely long working window with calm, non-animated content (not a competing status signal). Originating plan: `roadmap/tool-block-renderers.md`. [D02], [D05], [L06], [L20]

**D108.** **`/btw` side questions are driven by claude's native `side_question` control-request, rendered overlay-only, and are invisible to the transcript and to replay.** A side question — ask claude something about the current work, answered from the full conversation with no tools — is *not* a text-expanding slash command (that path claude refuses headless, `num_turns:0`); it is a `control_request { subtype: "side_question", question }`, serviced by the same inbound handler as `initialize`/`interrupt`/`set_model`. Probe-verified serviced **idle AND mid-turn** on claude 2.1.204 (`tugcode/probes/btw/FINDINGS.md`); the answer is a single settled `control_response` (`response.response.{response,synthetic}`), preceded by one `system/control_request_progress{started}` frame — not token-streamed. Three rules make it work and keep it honest: **(1) Tug is transport + surface, not semantics ([P01]).** claude's `runSideQuestion` owns the system-reminder framing, tool suppression, cache reuse, and `threadHistory:false` history exclusion; tugcode only forwards the control-request and correlates the turn-free response into a `side_question_answer` frame (caught in `handleClaudeLine` *before* turn routing, beside `initialize`/rewind — which is why a mid-turn response never corrupts the streaming turn). **(2) Overlay-only, never transcript ink ([P02]/[P05]).** The answer renders in a non-modal `TugPopover` (never a pane-modal `TugSheet`, which would `inert` the transcript and defeat mid-turn) anchored at the prompt entry. `side_question_answer` is deliberately **absent from `KNOWN_CODE_OUTPUT_TYPES`**, so the code-session store drops it — it reaches only the dedicated per-session `SideQuestionStore` feed. Nothing about the exchange lands on `state.transcript`, so a Maker ▸ Reload (which re-resumes from JSONL, where side questions never appear) reconstructs an identical transcript. **(3) Ephemeral, session-scoped ([P03]).** `SideQuestionStore` holds the ask/answer history in memory only (no tugbank/JSONL/web storage), cleared on `/clear` and card teardown. `/btw` is `supported-local` and inherits mid-turn dispatch for free — local commands dispatch before the send-readiness gate, so no `performSubmit` change is needed ([P04]). Originating plan: `roadmap/add-btw.md`. [L02], [L06], [L26] **Addendum ([D110]):** `/btw` gains a second entry point — the `?` **route** in the Z4A choice group. A submission on the `?` route dispatches the same `/btw <text>` local command through the same `RUN_SLASH_COMMAND` responder path, so the overlay-only / never-transcript-ink / ephemeral rules above hold identically; the route is just a second door to this one surface.

**D110.** *(Superseded by [D122] — the three-recipient route model is retired; the `$` route's shell backend survives under [D111], reached now by the auto-router and `/shell`, and `?` btw survives as the `/btw` command. Kept for the history of how the group got here.)* The Session card's Z4A route choice group carries **three routes — `❯` Code, `$` Shell, `?` btw — modelled as *recipients*** of a submission: Code = Claude on the record (the transcript), Shell = the machine, btw = Claude off the record (a native side question, [D108]). Each route owns its Return semantics (`RETURN_ACTION_BY_ROUTE`) and a `SELECT_ROUTE` keybinding (⇧⌘C / ⇧⌘S / ⇧⌘B). (The prefix-alias trigger — typing the route char at offset 0 flipped the route — was later removed; route characters are ordinary text.) ⇧⌘C required freeing the chord from `Copy as Plain Text` (moved to ⌥⇧⌘C, paired with `Paste as Plain Text` at ⌥⇧⌘V) in **both** the web keymap (`keybinding-map.ts`, whose exact-modifier matcher then lets a bare ⇧⌘C fall through to the route) and the Swift Edit menu (`AppDelegate.swift`, which owns the chord at the macOS menu bar). **Per-route submit dispatch** lives in `performSubmit` (`tug-prompt-entry.tsx`): `❯` → `codeSessionStore.send` (unchanged); `?` → intercept BEFORE the local-command split (the route IS the recipient, not the leading token) and dispatch `/btw <question>` via `RUN_SLASH_COMMAND`, recording the RAW question in history (not the synthesized `/btw` line) — inherits mid-turn dispatch for free ([D108] [P04]); `$` → an interim `SHOW_SLASH_COMMAND_NOTICE` (`shell-pending`) that preserves the draft rather than silently sending the command to Claude (the shell backend lands in a later phase, `roadmap/route-enhancements.md` Phase 3). **Per-route Z4B chrome** is a declarative manifest (`SessionRouteChromeManifest`, Table T01) replacing the old `SessionRouteShellGate` disable-scatter: `❯` shows identity(Claude Code) · Mode · Model · Effort; `$` shows identity(Shell) · Project · Cwd; `?` shows identity(Claude Code) · Session · Project. (The code route dropped its Session and Project chips when the card was put on a diet for the slim width preset: both names already stand in the pane title bar via `sessionCardTitleOverride`, so the row was saying them twice, and the code route is the one that runs out of width. Shell and commit are not space-challenged and keep theirs — [D128].) Chips a route drops **unmount** (show/hide, not disabled), and the focus cycle skips them for free; the identity badge never unmounts ([L26]) and stable keys preserve mount identity for chips that survive a flip (Project on every route, Session on code + btw). Swapping the centred-floating Z4B occupant on an explicit route gesture is the slot working as designed ([D97] — Z4B occupancy is a layout decision, not contract); the leading Z4A group and trailing Z5 submit stay edge-pinned across the swap (app-test `at0215`). The `Cwd` chip (a shipped path chip alongside `Project`) shows the shell session's working directory; until the shell backend exists it falls back to the project dir. Originating plan: `roadmap/route-enhancements.md` ([P01]/[P02]/[P03]). [D97], [D108], [L02], [L06], [L26] **Addendum ([D111]):** Phase 3 replaced the `$`-route interim notice with the real block-oriented shell backend and bound the `Cwd` chip live to the shell session — the recipient model and Z4B manifest above stand unchanged; the recipient is now wired to a running shell.

**D111.** *(The `$` **route** is retired by [D122]; everything below about the shell BACKEND stands unchanged — only the door moved, from a route to the auto-router plus an explicit `/shell`.)* **The `$` route runs a real block-oriented shell whose command exchanges thread into the transcript as durable, visually-distinct *non-context ink* — the transcript is the session's record of what happened, not a strict mirror of Claude's context ([P11]).** A `$`-route submission is executed by a per-session persistent POSIX-shell child in tugcast (`feeds/shell.rs`): **the user's own `$SHELL` (or `/bin/zsh`), spawned interactive-login (`-il`)** so it sources their startup files — PATH, exports, aliases, functions — and the route feels like their terminal, not a bare shell; spawned in pipe mode under `setsid()` (no controlling TTY, so `/dev/tty` grabs fail fast and TUIs decline), running each command as `{ …; } </dev/null` with `exec 2>&1` so stdout+stderr merge, framed by a sentinel (`printf '\n<sentinel>\t%d\t%s\n' "$?" "$PWD"`) read until it appears — settled-whole output for v1, with a per-exchange timeout backstop that reaps a wedged group (`kill(-pgid)`) and respawns fresh in the project dir. Interactive mode brings baggage a block shell must not leak, all neutralized by a **post-startup preamble** (run after rc sourcing so it wins): `trap - TERM QUIT` restores the SIGTERM disposition an interactive shell ignores (else the reap can't kill it), the precmd/preexec hooks and `promptsp` marker are cleared, and every prompt parameter is emptied; a **discarded warmup exchange** then absorbs any rc-file chatter so real exchanges start clean. A profile that hangs or fails the warmup can never brick the route — the child is reaped and a plain rc-less shell takes its place, flagged once in the next exchange's output. **No TTY emulator**: the block model is deliberate, not a terminal. The child persists across exchanges, so `cd` is stateful and the live `Cwd` chip ([D110], [P10]) tracks it. **The deck side is a clean split of ownership ([P12]):** `ShellSessionStore` (one per card) is the sole consumer of the `SHELL_OUTPUT` feed and owns *session* state (`live` / `cwd` / the in-flight exchange that drives the route-aware Z5 `stop` pose, [P13]); it does NOT own the rows — each exchange's transcript presence lives in `CodeSessionStore`, fed via `ingestShellExchange` on `exchange_started` (mint an in-flight `shell`-origin turn) and `exchange_complete` (settle it in place). `SHELL_OUTPUT` is deliberately absent from the code-session feed filter, so there is one consumer chain and no dual-ingest race; the `exchange_complete` frame is self-contained (it re-carries command/cwd/started_at/settled_at) so the settle — live and on restore — needs no correlation with an earlier frame. A `shell`-origin `TurnEntry` holds a single `shell_exchange` message and is disjoint from the Claude phase / activeTurn / scratch machinery — it renders as `#s` (`turn-metric.md` S01/S02) inside a `participant="shell"` transcript entry, styled as subdued non-context ink so it never reads as Claude context. **The exchange body reuses the standard tool-block chrome for family consistency, not a bespoke layout:** `ShellExchangeBlock` composes the shared `BlockChrome` with the command in the header (the `$` sigil + command, exactly where `BashToolBlock` puts its command) and a right-aligned Add-to-context + Copy cluster, over an `embedded` `TerminalBlock` for the output; the exit status + duration are NOT drawn inside the block but on a Z1B end-state row beneath it (`SessionZ1B participant="shell"` — the exit badge where a Claude turn shows OK/Error, reusing `endStateBadgeFor`'s grammar via `shellEndStateBadge`). The block carries no tool NAME (the `Shell` row identity already names it; a repeat would be redundant, unlike a `Bash` block under the shared `Code` identity). **Persistence + restore ([P07]):** every settled exchange is recorded to a per-session `ShellLedger` (`shell_exchanges.db`, capped 500/session, oldest evicted) beside `sessions.db`; a Maker ▸ Reload / relaunch re-fetches via the `list_shell_exchanges` → `list_shell_exchanges_ok` CONTROL pair (never feed replay) and mints each row keyed `restored-<sqlite-id>`, interleaving by the exchange's `started_at_ms`. **Interleave is one-directional by design, and the reason is empirical:** shell turns upsert into the append-ordered Claude spine by timestamp (`upsertShellTurn`), and a Claude append slides past trailing shell turns the restore seated ahead of it (`appendTurnInterleavingShell`) — but the Claude stream itself is NEVER timestamp-sorted, because the real on-disk JSONL corpus is ~39% non-monotonic in entry timestamp (sidechains, `--continue`/`--resume` merges, and compaction re-insert older stamps mid-stream). JSONL arrival order is the source of truth for Claude turns; timestamp is a reliable key only for shell turns (a ledger `started_at_ms`) and genuine user submissions (a `submitAt` from the JSONL) — exactly the pair the interleave sorts on. **The interleave also holds for submissions that have not committed yet:** an in-flight turn and the queued-send ghosts live outside the committed transcript, so the row layout merges the transcript's trailing shell rows INTO that pending block by timestamp (`composeRowLayout`), under the same rule the commit will apply — otherwise an exchange the user ran *after* hitting submit paints above their own message for the length of the turn and then jumps. A queued send is stamped when the user submits it, not when the queue flushes (`QueuedSend.queuedAt`), so the shell rows run during its wait stay below it permanently. **Claude never sees a shell exchange implicitly ([P08]):** the only path into Claude's context is the on-demand **Add to context** toggle on a settled exchange row — it composes a fenced `command + output + exit` block (oversized output truncated at a line boundary; the fence lengthens past any backtick run) and stages it on the pending-context queue, where it rides the next `❯` submission as attributed `#s{n}` context. Never auto-sent, and a second click un-stages it. **One gesture, one glyph:** `cloud-upload` marks add-to-context everywhere it appears (shell rows and the `/btw` overlay alike), going `check` while staged. The earlier **Share** affordance — a second, redundant path that seeded the composer directly — is retired along with its `requestShare`/`consumePendingShare` slot; two ways to do one thing is what made the header read as a cluster of near-synonyms. **Richness accretes without a rewrite:** shell rows resolve their renderer through a command-block registry on the [D101] grammar (`session-command-block-registry.ts` — `registerCommandBlock(name, matcher, renderer)` / `resolveCommandBlock(command)`, total default to the generic exchange block, throw on duplicate name); no bespoke renderers ship (`git status` / `ls` / build-progress blocks are follow-ons, [P05]). App-tests: `at0215` (route chrome), `at0216` (exchange e2e + restore interleave). Probe: `tugrust/crates/tugcast/probes/shell-exec/FINDINGS.md`. Originating plan: `roadmap/route-enhancements.md` ([P04]–[P13], Milestone M03). [D97], [D101], [D108], [D110], [L02], [L06], [L26]

---

## Dev Prompt Entry

**D98.** Host facts — the backend's network `hostname` and the basename of its login shell — are resolved by tugcast and served at a read-only `GET /api/host` endpoint, then read into tugdeck's `HostFactsStore` through `useSyncExternalStore`. The browser cannot know the backend's real hostname or `$SHELL` (`window.location.hostname` is only the URL host), and the facts are static for a server's lifetime, so the store fires the fetch exactly once and caches the result. The endpoint is loopback-restricted like the other `/api` handlers; the response shape `{ hostname, shell }` is a cross-stack contract, pinned by a Rust serialization test and a `bun:test` parser test. A failed or pending fetch leaves the store empty, and consumers treat an empty snapshot as "not yet known." [L02]

**D99.** Each Dev prompt entry owns a `RouteLifecycle` — a per-prompt-entry pipe that holds the authoritative command route (`❯` Code / `$` Shell) and announces every change. It offers two surfaces over one fire path: a store surface (`subscribe` + `getRoute`) that renderers read through `useSyncExternalStore` ([L02]), and a synchronous delegate/observer surface (`observeRouteWillChange` / `observeRouteDidChange`, `useRouteDelegate`) for imperative reactors. The route is no longer component `useState`: once it has a consumer outside the component that owns it, it is external state and must enter React through `useSyncExternalStore`. Unlike the deck-level `CardLifecycle`, `RouteLifecycle` is scoped per prompt entry, provided through `RouteLifecycleContext`, surfaces a single `(prev, next)` will/did pair, and dispatches synchronously — route consumers re-render content and have no gesture-focus-lock hazard, so there is no `MessageChannel` drain. See [route-lifecycle.md](route-lifecycle.md). [L02], [L03]

---

## Onboarding & Setup

**D106.** ConfigureTug — the app-modal onboarding wizard (`configure-tug.tsx`, [#step-9] of `roadmap/onboarding-and-install.md`) — renders as an ordered list of **step rows**, each a **bespoke row** (not a transcript `BlockChrome` [D104]): a left-hand `TugProgressIndicator variant="pulsing-dot"` whose role+state encode the step's lifecycle ([D02]), a requirement/direction **label**, a **detail** line carrying state/progress/completion, and an optional **CTA**. The block shell was evaluated and rejected for this surface — its tool-name/result-summary/copy/chevron affordances are transcript baggage a setup step does not want; the bespoke row keeps the wizard's own rhythm. A step's status is one of `pending | active | busy | error | done`, mapping to the dot as: `pending`→`inherit/stopped` (row dimmed), `active`→`action/running`, `busy`→`agent/running`, `error`→`danger/aborted`, `done`→`success/completed`. The pulsing dot is the *only* role tone on the row; the label/detail inherit panel text ([D104] convention 1 — status is the dot's). The design spike that fixes this surface's copy and rhythm without a clean guest is the **`gallery-configure-tug`** card (`gallery-configure-tug.tsx`), which simulates every state below from local state — it never touches the real `authStore`.

**Flow.** Three steps, each gated on the one before, fed by the app-level `authStore` ([L02]) plus the deck card count:

```
   (first launch) ──▶ PROBING  "Checking your setup…"
                          │
   STEP 1 ── Install Claude Code   (label flips to "Claude Code installed" on done)
     active (Install) ─▶ busy (Installing…) ─▶ done ✓
                    └─▶ error (Retry) ─┘
                          │ done
   STEP 2 ── Log in to Claude
     active (Log In) ─▶ busy (Logging in…) ─▶ done ✓
                    └─▶ error (Try Again) ─┘
                          │ done
   STEP 3 ── Start a Claude Code session   (success/transition button)
     active (Open a Session Card) ─▶ opens first card ─▶ wizard dismisses

   Transport down (replaces body): a "Reconnecting…" step row
   Sibling app-modal (wins):       VERSION TOO OLD → TugVersionGate ([#step-7])
```

**Per-state copy (what we show & say).** Label is the requirement/direction; detail is the state/progress/completion line. A `busy` step keeps a **disabled** CTA (not hidden) so the row doesn't empty out; a `done` step swaps the CTA for a green success check (✓):

| Step | Status | Dot (role/state) | Label | Detail | CTA |
|---|---|---|---|---|---|
| — | probing | agent / running | Install Claude Code | "Looking for Claude Code…" | — |
| 1 | active | action / running | Install Claude Code | "Tug will install it for you." | **Install** |
| 1 | busy | agent / running | Install Claude Code | "This can take a moment." | *Installing…* (disabled) |
| 1 | error | danger / aborted | Install Claude Code | "Install failed: \<error\>" | **Retry** |
| 1 | done | success / completed | Claude Code installed | "Claude Code is ready." | ✓ |
| 2 | active | action / running | Log in to Claude | "Tug runs sessions with your Claude subscription." | **Log In** |
| 2 | busy | agent / running | Log in to Claude | "Use your browser to log in…" | *Logging in…* (disabled) |
| 2 | error | danger / aborted | Log in to Claude | "Log-in didn't finish. The browser may have been closed." | **Try Again** |
| 2 | done | success / completed | Logged in as \<email\> | "Claude \<Tier\> plan" | ✓ |
| 3 | pending (no cards) | inherit / stopped | Start a Claude Code session | — | — |
| 3 | pending (cards open) | inherit / stopped | Continue working | "You'll return to your \<N\> open cards." | — |
| 3 | active | action / running | Start a Claude Code session | "Open a Session card to get started" | **Open a Session Card** |
| 3 | done | success / completed | Start a Claude Code session | "Opening Session card…" | ✓ |
| — | transport down | agent / running | Reconnecting… | "Lost the connection to Tug. Setup will resume automatically." | — |

The step-3 pending row previews the return to work while the user is still logged out (the "Continue working" case, [P04] of `roadmap/logout-consolidation.md`): with cards open, re-login auto-closes the wizard straight back to them (the `open` derivation is `notReady || needsFirstSession`, which goes false the instant a logged-in deck has cards), so the preview lives in the logged-out window rather than as an active step. The `pendingOpenStepCopy` helper (`configure-tug-copy.ts`, unit-tested) owns the label/detail branch on card count.

**Verbiage.** Tug uses the consistent **"Log in" / "Log out"** pair for the account action (the `claude` CLI itself says "Sign in"/"Log out" — inconsistent; Tug does not follow it). The `subscriptionLabel` helper (`configure-tug-copy.ts`, unit-tested) formalizes the tier as "Claude Max plan" etc. **Logout** reopens this same wizard: an app-level "Log out…" (File menu + `/logout`) confirms via TugAlert, interrupts every in-progress turn **first** (each tagged `interrupt("logout")` so its Z1B end-state reads "Stopped — logged out"), then runs `claude_logout`, and flips `authStore` logged-out so ConfigureTug returns to the Log-in step (a failed/timed-out logout surfaces a "Couldn't log out" alert and leaves the user logged in). The `TugLogout` orchestrator (deck-root sibling) owns that flow.

**One login surface.** ConfigureTug is the *only* login UI. There is no per-card in-card sign-in: when a card's session hits the auth gate (`auth_required` / `claude_missing`), the observer re-probes `check_auth` (flipping `authStore` logged-out → ConfigureTug opens app-modal) and unbinds the card to its picker with a `signed_out` notice, so the user re-logs-in once in the wizard and resumes per card from the picker. See `roadmap/logout-consolidation.md`.

**Resolved design questions.** **(1) "Open your first session" is a success/transition button, not a probed step** — its CTA opens the first Session card and the wizard dismisses. That first card seeds its **Project Path to the user's home directory** (a sensible from-the-drop default; the user re-points it afterward). **(2) The step row is bespoke** (above) — `BlockChrome` was evaluated and rejected. **(3) Probing / first-launch:** on a user's *first* launch ConfigureTug shows **up front and immediately** (it does not wait to probe behind a blank deck). This is governed by a persisted **first-launch flag** stored via tugbank defaults (`/api/defaults/…`, never `localStorage`); the flag is set once the user has launched the first time, so subsequent launches fall through to the normal probe-then-decide path. **(4) Transport-down** replaces the wizard body with a calm "Reconnecting…" takeover rather than a dead wizard; **version-too-old** is the sibling app-modal `TugVersionGate` ([#step-7], Spec S02) which takes precedence over ConfigureTug. The unhappy-path states (install-fail, sign-in cancel/timeout, transport-down) are designed in [#step-10]; this decision fixes their copy and visual grammar. [D02], [D104], [L02], [L06]

---

## Changesets & tugdash

**D112.** **Attribution is recorded at the point of change, never reconstructed — and it records provenance only, never judgments.** File authorship for a session is captured in a `file_events` table in `sessions.db` from tugcast's `agent_bridge` relay loop — the one supervised point every tool frame already flows through. Write/Edit/NotebookEdit attribute exactly from tool inputs on a successful `tool_result` — with the row's `project_dir` resolved **per file** from the file's own directory (worktree-aware `file_repo_root`: repo membership is a per-file fact, never the session's project dir, which misfiled every dash session's worktree edits as `.tug/worktrees/…` under the main checkout, invisible to every reader — and a file belonging to **no** repo is not recorded at all, since the fold matches only git's repo-relative dirty paths and such a row could never surface anywhere); `Bash` needs working-tree fingerprint bracketing (snapshot on `tool_use`, delta on `tool_result`); and the **turn-scoped fallback bracket** (`origin='turn'`, `tool_name='Turn'`) snapshots on the `user_message` forward and takes the delta on that turn's non-replayed `turn_complete`, attributing any path no exact/per-call-Bash row already covered — closing the capture gaps a per-call Bash bracket structurally cannot (its pre-snapshot racing a fast command, or a relay crash mid-Bash within a live turn). **All brackets are relay-local**; there is no cross-session bracket registry, and no capture path ever observes or marks another session. The retired alternative — a shared registry cross-marking rows `ambiguous` on repo-root + wall-clock overlap between sessions' brackets — was wrong twice over: unrelated concurrent commands on one checkout false-positived constantly (with several session cards open, essentially everything went ambiguous), and the hazard it aimed at (two sessions writing the same file, each bracket catching the other's delta) *always* manifests as both sessions holding rows for that path anyway. The correct signal is therefore **per-file contention, computed at read time**, and gated on proof: a file is `shared` iff two or more sessions hold **live proof rows** (`exact`/`replay` — the tool input names the file) for that exact repo-relative path. Proof decides ownership; `bash`/`turn` bracket rows are a whole-tree fingerprint *delta* that cannot separate this session's writes from a concurrent save, build churn, **or the user's own hand-save in their editor** (which belongs to no session and can never be claimed back) — so correlation never decides any bucket, **not even for the bracket's own session**: a bracket-only file stays `unattributed`, tagged `likely this session's (bash bracket)` as a hint, and inclusion is the same explicit disposition as any other unattributed file. (Each weaker rule reintroduced the bug through a side door: "any row = authorship" let two sessions' `cargo`/`git` brackets sweep up each other's saves so everything went `shared`; "a bracket attributes its own session when no exact owner exists" would let an agent's bracket auto-commit the user's inflight hand-saves.) A row is live only while it postdates the last commit that touched its path (a commit *spends* the rows it absorbs — a fossil row neither attributes nor contends when the file is re-dirtied later; ties break toward spent, degrading to `unattributed`, visible). The legacy `ambiguous` column remains in the schema but is always written 0 and read by nothing. The ledger itself is **machine-global**: one `changes.db` (`tugcore::instance::changes_db_path()`, independent of `TUG_INSTANCE_ID`; `TUG_CHANGES_DB` is a test-isolation override) attached by every instance's `SessionLedger` — the working tree is machine-global, so per-instance attribution splits the truth (a second app instance on the same checkout saw the first's work as ownerless and its card pre-selected the lot; both halves are fixed — shared ledger + unattributed-defaults-off). All three source rules (exact, `bash`, `turn`) hold the same invariants: point-of-change capture, an upsert keyed `(tug_session_id, tool_use_id, file_path)` so replay/resume converges idempotently, and attribution failures never affecting frame delivery (degrade to "forward unchanged"). Shared rows are excluded from every default commit set (the card's selection and `tugutil commit` alike, opt-in via `--all`/`--tree`/`--paths`) — one session's commit never silently sweeps another's file. The commit *read* side treats no row as a gate: `tugchanges-core`'s join is inverted so `git status` is the universe and the ledger annotates it (a dirty file is never invisible; see `roadmap/commit-tool-fixes.md` [P01]–[P05]). Full doctrine: [tracking-changes.md](tracking-changes.md). `roadmap/changesets-plan.md` [P01]–[P06], [P15].

**D113.** **The changeset surface is a process-level aggregate feed, not per-workspace.** `CHANGESET_ALL` (a single process-level snapshot watch, the `spawn_stats_feeds`/`defaults` pattern) aggregates every open project into one `WorkspacesChangesetSnapshot` delivered to every deck, because tugdeck's `FeedStore` holds one value per feed id — a second project's frame on the same id would overwrite the first, so aggregation must be server-side. The project set is exactly the current `WorkspaceRegistry` entries (one per project with a live session card); non-repo dirs surface with an "Initialize git" affordance rather than being skipped. The retired per-workspace `CHANGESET` (0x23) stays reserved, never reused. The card retires the old Git card entirely. `roadmap/changesets-plan.md` [P09], [P16], [P17].

**D114.** **Dash is its own tool, `tugdash`, not a `tugutil` subcommand.** The worktree-isolated work-unit engine lives in `tugdash-core` (library, callable from tugcast) with a `tugdash` CLI over it (`create`/`commit`/`join`/`release`/`list`/`show`), so the Changeset card drives create/commit/join through the same code as the CLI. A dash *is* a `tugdash/<name>` git branch plus a worktree under `<repo>/.tug/worktrees/<name>` (auto-migrated from the legacy `.tugtree/tugdash__<name>` when clean and unheld); its base branch and description live in git config, its activity in the per-project append-only dash-log — there is no database. `tugutil dash` is deleted; the tugplug skills and docs speak `tugdash`. `roadmap/changesets-plan.md` [P12], [P13].

**D115.** **A conflicted join is a graded resolution ladder ending in a review-gated AI rung, never a dead end or a hand-off.** `tugdash-core`'s `join` builds the integration commit **off to the side** (`merge-tree --write-tree` stages → resolved blobs → `commit-tree` on the base → staleness-guarded fast-forward), so no checkout is ever half-merged and abort is free. Conflicts escalate: replay probe (round-by-round in-memory replay; a clean replay lands as linear rounds, shape change accepted) → `rerere` (enabled by default; replays recorded resolutions, and the ladder records its own driver/AI resolutions) → per-file `merge-file` → a PATH-discovered structured-merge driver (mergiraf by default, never bundled — AGPL) → the AI rung. The AI rung ([P32]) is a headless per-file scribe merge (base/ours/theirs + the dash's maintained draft as intent) on the existing `ScribeSpawner` seam, deterministically validated marker-free, streamed to the card as a `/btw`-style overlay — **never a Session card, never auto-committed**. The card reviews the pre-built candidate (resolved files badged by rung) and lands it on explicit confirm; non-content conflicts (delete/modify, binary, mode) short-circuit to honest "unresolved". `roadmap/changesets-plan.md` [P14], [P31], [P32]. [D114]

**D116.** **Every change lands through one lifecycle — Preflight → Draft → Land — and landing is the user's act, from the prompt entry.** The consolidation over [D112]/[D113]: buckets and rounds gather truth; an editable, durable, **machine-global** Draft (message + selection dispositions + edited-provenance in `changes.changeset_drafts` — the same scope axiom as `file_events`) proposes the landing; and the prompt-entry verbs **`/commit`** (main lane, two-beat: draft on the first beat, land on the second) and **`/join`** (dash lane, preview → squash with the join draft as message) execute it. The Changes shade — a modal `TugSheet` presentation over the transcript region with the prompt entry live beneath it — is where drafts are reviewed and edited (`TugMessageEditor` on the `TugTextEditor` substrate; the composer IS the display); an `edited` draft is never machine-clobbered (the shade's confirmed Regenerate is the only overwrite path). Landings are idle-gated at dispatch AND at the affordance; drafting stays live mid-turn. Skills draft, humans land: the `draft` skill (né `commit`) authors via `tugutil draft set` and never commits; `implement` ends by writing the dash's join draft and pointing at `/join <name>`; Claude's built-in `/commit` is shadowed dead by the local verb. Squash is the only join strategy in the UI (merge/rebase stay CLI-expert paths); release is shade-only with a discard preflight, and both join and release clear the dash's draft row so a reused dash name never inherits a dead dash's message. Every landing leaves a non-context receipt in the transcript; History badges joins by the `Tug-Dash:` trailer. The gathering verb is `tugutil preflight` (né `context` — "context" is AI-reserved vocabulary; a hidden alias holds for one release). Full doctrine: [tracking-changes.md](tracking-changes.md) § The landing workflow. `roadmap/changes-commit-dash-consolidation.md` [P01]–[P17].

**D117.** **The commit route is a `!changes` chip in the composer, orthogonal to the changes glance — the composer IS the message editor.** The realization of [D116]'s "the composer IS the display", refined to a prefix-chip model. There is **one bottom-anchored passive changes sheet** (`TugSheetContent presentation="shade" shadeAnchor="bottom" shadeAutoSize shadePassive`) that rises from the top of Z2 over the transcript (scrim + transcript-`inert`, no focus trap — focus stays in the composer below, [P17]); it is the read-only glance, driven purely by the `ShadeViewController`'s `changes` view. **Two orthogonal axes:** (1) the sheet's *visibility* (card chrome), and (2) the *commit route* — "the composer leads with a `!changes` command chip." The chip IS the route: while present, the prompt entry is the commit-message editor (the payload after the chip is the message), the slash/bang/mention machinery stands down, Z5 swaps to a cancel / auto-message / commit icon rail (the pencil-sparkles Auto-Message guarded by the Replace-message `TugConfirmPopover`), and submit lands the commit (`extractCommitMessage` strips the chip; `matchBangCommandLine` recognizes the same line) instead of sending to Claude. **⇧⌘C** (and `/commit`, `!changes`, Session ▸ Commit…) is the entry: on an empty composer it shows the sheet **and** enters the route (inserts the chip); on a non-empty composer it shows the sheet as a **glance only**, leaving the in-progress prompt untouched; toggled again it hides the sheet and, if the route was active, drops the chip. The session card owns the coupling (`commit-route-controller.ts` no longer knows about the shade); History and the route are mutually exclusive. The intermediate transcript-resident `TugCommitDialog` (an inline dialog with its own embedded editor + button rail) is **retired** — it duplicated the composer, blocked nothing, reserved transcript space, and left `!changes` inert; this model deletes all four problems and makes `!changes` a real routing. The Commit button's turn/pending/empty-changeset gate is JS `disabled`; the empty-message half is CSS-gated on the entry's `data-commit-empty` (the message with the chip stripped — `data-empty` can't be used, the chip makes the doc non-empty) so no per-keystroke React state is introduced ([L22]). **Uncommitted edits are durable:** every genuine user edit debounce-persists via `persistMessage` (backend maintained-draft engine → `entry.draft.message`, the source of truth), so edits survive card deactivation / reload; a cancel/re-enter resumes them. `tug-sheet.tsx` (`shadeAnchor` / `shadePassive`), `commit-route-controller.ts`, `tug-prompt-entry.tsx` (`commitRoute`, `buildCommitRouteState` / `extractCommitMessage`), `session-card.tsx` (route↔sheet coupling). **Superseded (chip + routing spelling only):** the `!changes` prefix chip and the routing framing are retired by [D119] — committing is the composer's one secondary resting mode, the editor holds the message alone, and `!changes` only teaches. The two-axis doctrine (sheet visibility vs. the active commit state), the composer-as-editor, the Z5 rail, the land gate, and the durable-draft persistence all stand and carry forward. [D116], [P17], [L22]

**Auto-Message feedback ([P06]).** Generating a commit message is a legible, cancellable, non-leaking act — the scribe is a separate headless `claude -p` child, wholly isolated from the interactive session's turn. While it streams: the composer goes read-only (`disabled` → `editable(false)`, so manual typing can't interfere), an **untinted wave caret** (the AI-thinking `TugProgressWave` glyph in the prose text color, a CM6 `Decoration.widget` rebuilt at `doc.length` every transaction — `wave-caret.ts`) rides the stream's tail in place of the suppressed native caret while the view scroll-follows it, and the **Auto-Message button stays lit** for the whole composition (a real filled-accent button via `emphasis`, pointer-neutralized on `data-drafting`). On settle the view scrolls back to the top so the message reads from its start. **Auto-Message is repeatable** — an empty field always FORCES regeneration (`requestDraft(true)`), so the [P03] edited-gate never swallows a repeat after the first edit — and the whole generation is **one undo step**: deltas stream with `addToHistory: false` (`restoreState`'s new opt) and the settle reverts-then-applies so ⌘Z removes the generated message and ⌘⇧Z restores it. **Cancel** is the X immediately left of the button, Escape, or Cmd-. — all three abort only the scribe child and **never reach the session-turn interrupt**: the leak is closed at two points because the window-level keybinding claims `CANCEL_DIALOG` before the entry-root listener when a turn is in flight, so the `CANCEL_DIALOG` responder cancels the draft first (turn-running case) and the entry-root capture listener handles Escape/Cmd-. directly (no-turn case). Cancel reverts the composer to its persisted pre-draft message (no half-stream kept). The abort is a `changeset_draft_cancel` CONTROL verb → a JoinHandle registry keyed by entry identity (`draft_engine.rs`); aborting drops the `kill_on_drop` child. The scribe prompt (`scribe.rs draft_ask`) forbids hard-wrapping — no column width, one unbroken line per subject/bullet. `wave-caret.ts` (`setWaveCaretActive`), `changeset-draft-store.ts` (`cancelDraft` + the `cancelled` → idle fold), `commit-route-controller.ts` (`cancelDraft`), `draft_engine.rs` (`DraftTaskRegistry` / `cancel_draft`), `agent_supervisor.rs` (`changeset_draft_cancel`). [P06], [L06], [L22]

**D118.** **One row grammar for changed files: `TugChangesList` rows are compact mono `TugListRow`s, every row expands into its diff, and the `/commit` receipt's file list IS a changes list — sha-backed.** The live glance, commit mode, and the durable receipt render the identical row (status letter leading; path + badges content; the monochrome `+N −M` pair, pop-out, and fold cue trailing) — the only differences are the diff source and which affordances are live. Three moves make this hold. (1) **The row layer moved off `BlockChrome` onto `TugListRow`** — a changed file has no lifecycle, so the tool-block header's lifecycle dot (and its header padding) had no business on a file row; the whole row is the fold's click target, the diff body mounts under it on expand ([L26] unmount-collapse), and the path link / trailing controls own their gestures. `TugListRow`/`TugListView` grew a **`density="compact"`** knob (context-published `rowDensity`; hairline block padding via the existing `--tugx-list-row-padding-*` tokens) and the changes list rides it by default — hundreds-of-files commits stay scannable. (2) **Every row is expandable because every file now diffs:** the head-flavor backend synthesizes a new-file unified diff per untracked path (`git diff --no-index -- /dev/null <path>` appended to `git diff HEAD` — `fetch_git_diff_with_untracked`), which also feeds the Auto-Message scribe (created-but-never-committed content reaches the prompt and the fingerprint; the old `(size, mtime)` side-band fold-in is retired). The old behavior — untracked rows silently rendering a disabled chevron — was status-dependent mystery, not design. (3) **The receipt composes `CommitChangesList`** over a third `DiffDescriptor` flavor, **`{kind:"commit", root, sha, paths}`** (`git diff-tree --no-commit-id --root -M -p <sha>` server-side — first-parent diff, root commit against the empty tree), replacing the static `FrozenChangesList`: counts render instantly from the frozen record, each row's hunks fetch lazily on first expand (a thousand-file commit costs nothing until you look), the pop-out opens a `Commit <sha>` Diff card, and a vanished sha (rebase, gc) degrades to an in-body notice while the rows stay intact. `root` is the exchange row's ledger-persisted `cwd`, so live and restored receipts resolve the same workspace with no side-band data. Diff counts everywhere are the shared `DiffSummaryBadges` atom — ghost `inherit` badges, **no green/red tint** (the monochrome doctrine [P27]; chromostereopsis made per-theme tinted counts unsolvable) — while the status mark keeps its tone. The status mark itself is a **colored single letter, not a glyph** (`statusMark`): green **N** (new — untracked or added), yellow **M** (modified or moved — renamed folds in), red **D** (deleted); a letter reads more legibly than a lucide file-icon at row size, and three letters cover every state the list surfaces. `tug-changes-list.tsx` (`ChangesFileRow` / `CommitChangesList` / `StatusMark`), `tug-list-row.tsx` (`density`), `diff-summary-badges.tsx`, `git-diff-store.ts` (`commit` flavor), `feeds/git.rs` (`fetch_git_diff_with_untracked` / `build_commit_diff_snapshot`), `session-commit-receipt-block.tsx`. [D117], [P01], [P08], [P27], [L06], [L26]
**D119.** **Commit is a composer mode, not a bang routing — the mode lives in the chrome, never in the document.** [D117]'s prefix-chip model made committing a cousin of `!shell`/`!btw`/`!find`/`!history`, and every seam showed: the chip did double duty (an inert pre-submit routing token AND in-mode dressing — you could hold a `!changes` chip with normal Z5 and not be in the mode), Z4A hid the routing picker behind a dead icon, and Z4B kept the Claude-session chips (CLAUDE CODE / MODE / MODEL / EFFORT) that describe *sending a prompt* while you were doing something else entirely. The repair separates three ideas that had been conflated: **bang commands** are per-submission routings (four of them — shell, btw, find, history); **the Changes glance** is a view (the read-only bottom sheet); **commit is the prompt entry's one secondary resting mode** (`commit-mode-controller.ts`). In the mode the editor holds **only the message** — no chip, no `extractCommitMessage`, submit lands the document verbatim — which deletes the ambiguous half-state outright: there is no way to wear commit dressing without being in the mode, because the dressing is chrome. **Z4A** keeps the `!` routing picker in its slot but **disabled** — routings are meaningless while composing a commit message, and the Z5 ✕ is the mode's single way out, so Z4A carries no exit of its own (an interactive Commit chip was tried first and cut: a second exit was redundant, and the slot must not shift). **Z4B swaps** (the Table T01 move shell/find already made): the Claude chips unmount; the commit cluster is **Project** (where it lands) + **Changes** (`N files`, click toggles the changes sheet without leaving the mode — the first in-mode sheet control). Z5's cancel / auto-message / commit rail stands unchanged. **Entry points unify on `enter(seed)`**: ⇧⌘C on an empty composer, `/commit [message]`, Session ▸ Commit…; ⌃⌘C is a muscle-memory alias for the ⇧⌘C toggle (it no longer seeds a chip). **`!changes` is retired to a plain unknown bang**: the registry drops to four and the `!` picker lists four; a submitted `!changes` is no longer special-cased — it falls to the shell escape hatch exactly like any unregistered `!foobar` (this app has a single user who will never meet a teaching notice for a feature they never shipped). Deliberately NOT built: a generalized composer-modes framework — prompt and commit are the only resting modes ([P01] stands; everything else is per-submission), and one honest special case beats a framework with two tenants. `commit-mode-controller.ts`, `tug-prompt-entry.tsx` (`commitMode`, `buildCommitModeState`, the disabled-in-mode Z4A picker), `session-card.tsx` (commit cluster, `TOGGLE_CHANGES_VIEW`), `bang-commands.ts` (registry drops `changes`), `keybinding-map.ts` (⌃⌘C alias). [D117], [P01], [L22], [L26]

---

**D120.** **A session can *claim* a file — an explicit, intentional attribution act, decoupled from editing.** Changeset attribution has two tiers, and only one claims a file: a **proof** row (`origin: exact`/`replay`) means a tool call named the exact file (Write/Edit/MultiEdit/NotebookEdit); a **correlation** row (`origin: bash`/`turn`) is a whole-tree fingerprint delta around a Bash command, recorded as a "likely" *hint* on the unattributed bucket, never proof (a tree-delta can't tell the session's own write from the user's hand-save during the command, or from another session on the same checkout — `changes.rs::compute_changes`). The gap this exposed: **claiming was welded to editing** — the only way for a session to own a file it touched via `perl`/`sed` (bracket-only) or by hand was to *re-edit it with a proof tool*. `origin: "claim"` fixes that: a third proof origin (`origin_is_proof` on both the writer `attribution.rs` and reader `tugchanges-core/ledger.rs`) that promotes a hinted file straight into the session's entry, written by an explicit claim rather than a side effect of editing. **tugcast owns the write** (tugchanges-core opens the ledger read-only; the aggregate recomputes only on tugcast's process-global bump): the `changeset_claim` CONTROL verb in `agent_supervisor.rs` records one `claim` file-event per repo-relative path via `record_file_event`, then fires `changeset_all_bump`. **Two surfaces, one handler.** The deck sends a `changeset_claim` WS control frame (a **Claim** affordance on each unattributed "likely" row → `ChangesRouteController.claim` → `ChangesetVerbStore.claim`). The CLI (`tugutil claim <paths>`) POSTs `/api/tell {action:"changeset_claim", …}`; `tell_handler` bridges `changeset_*` actions into the same `handle_control` (the single source of truth shared with the WebSocket ingress) under a synthetic client id, since a tell has no connection — gated to the `changeset_` prefix so client-scoped verbs (`spawn_session`) stay off the HTTP surface. Idempotent (re-claiming writes another proof row, composed identically); a self-claim never marks contention. This is the intentional, first-class counterpart to editing — never "write the file to claim it". `feeds/attribution.rs` + `tugchanges-core/ledger.rs` (`origin_is_proof`), `agent_supervisor.rs` (`do_changeset_claim`), `server.rs` (`tell_handler` bridge), `tugutil` (`claim` verb), `changeset-verb-store.ts` / `changes-route-controller.ts` / `tug-changes-list.tsx` (`ChangesFileRow` Claim). [D118], [P03]

**Orphan surfacing — the open-card path is fresh-on-look.** The aggregate composes only projects **open in the `WorkspaceRegistry`** and recomposes only on the process-global bump (the per-workspace FS watcher, ledger writes) — so a working-tree change that lands while no FS event fires can leave the cached snapshot stale ("No changes" while the tree is dirty). Hardening: a side-effect-free **`changeset_refresh`** CONTROL verb fires the same bump (no payload, no reply; emission is diff-suppressed, so a client sees a frame only when the tree actually drifted), and the Session card fires it on **Changes-shade open** (`ChangesRouteController.refresh` → `ChangesetVerbStore.refresh`, wired at the shade-open effect in `session-card.tsx`) — so opening Changes always reflects live working-tree truth. Orphans in a project with **no** open card stay invisible to the UI by design (unwatched, uncomposed); `tugutil claim <paths> --project <dir>` reclaims those headlessly.

**Orphaned bucket — a file stranded on a dead session.** Diagnosis (via the observability endpoint below) corrected the earlier "zero-row/trashed" theory: closing a session does **not** delete its rows (only *trashing* cascades a delete). So a file its author proof-owned stays *owned* after the session closes (`live: false`) — never entering the unattributed bucket — yet no live card surfaces another session's changeset entry, so it falls into a gap and reads as "No changes." Fix: `compose_snapshot` **lifts** a file whose every proof-owner is non-live into a dedicated `orphaned` bucket (`OrphanedFile` carries the dead `prior_owner_name`/`id`); a file any *live* session co-owns stays in that live entry, so a file orphans only when all its owners are dead. The Changes shade renders `orphaned` as a claimable section ("orphaned — claim to bring into this session") with the same per-row **Claim** affordance the unattributed bucket uses. Semantics ([D120] extension, user-locked): closing keeps rows, so re-opening the originating session **auto-re-owns** its untouched orphans (plain composition — a live owner's proof-dirty files show in its entry; a committed/reverted file is spent by the row-liveness rule and won't silently re-attach). But **claim severs the prior owner** — `do_changeset_claim` calls `SessionLedger::sever_file_ownership_except(project_dir, paths, claimant)` to delete other sessions' rows for the claimed paths, so a claimed file is "no longer an orphan, nor owned by the originator." `changeset.rs` (lift + unattributed exclusion), `tugcast-core/types.rs` (`OrphanedFile`, `ChangesetSnapshot.orphaned`), `session_ledger.rs` (`sever_file_ownership_except`), `agent_supervisor.rs` (`do_changeset_claim` sever), `changeset-types.ts` / `changes-route-controller.ts` / `tug-changes-list.tsx` / `session-changes-view.tsx` (orphaned entry kind + Claim).

**Changeset observability.** `GET /api/changesets` (loopback, read-only; `server.rs` → `AgentSupervisor::compose_changeset_aggregate`) returns the live aggregate composed fresh over the registry + ledger — ground truth for "what compose produces right now," independent of emission/diff-suppression. `tugutil host changesets` (`--json` for the full snapshot) prints per-project `changesets`/`unattributed`/`orphaned` counts with each owner's liveness — the tool that pinned the dead-owner gap. `compose_aggregate` also traces per-project counts at debug level. NB: a throwaway `tugcast --source-tree` shares the machine-global `changes.db`, so a live claim through it mutates the real ledger — verify the claim/sever path at the unit layer, not by driving the shared ledger.

**D122.** **The composer has exactly two routes — Prompt and Changes — and the `!` layer is retired with no vestige.** A **route** is defined as *a mode that owns the composer's whole document*. Exactly two things do: typing a prompt, and authoring a commit message. Everything else is either a **one-shot verb** (a slash command — it consumes one submission and leaves you where you were) or lives **outside the composer entirely** (find). The four things previously called "bang routings" — `!shell`, `!btw`, `!find`, `!history` — were verbs wearing a routing's sigil: none ever swapped the document, and calling them routes was the original error. This decision replaces [D110]'s three-recipient group and [D119]'s "commit is a mode, not a bang routing" framing (which was right about commit and wrong only in the company it kept).

**The Z4A group is a view, not a second home for the selection.** `CommitModeController` already swapped the document on enter/exit (stashing the prompt draft in `preCommitDraftRef`, restoring it verbatim, persisting the message through the changeset draft store), so the two-item `TugChoiceGroup` renders `commitActive ? "changes" : "prompt"` and dispatches `SELECT_VALUE` into `enter()` / `exitCommitMode()`. It holds no state, which is what makes every existing entry and exit path — ⇧⌘C, `/commit`, Session ▸ Show Changes, a successful land, Cancel ✕, Escape, the shade's self-close — move the visible tab with zero new code. It is deliberately **not disabled in commit mode** ([D119] disabled the `!` button there): the group is how you leave Changes. **No controller means no group** — `TugPromptEntry`'s `commitMode` prop is optional and the Component Gallery mounts the entry without one, so `Z4A` renders empty there rather than a Changes segment that cannot act. Consequence worth writing down: this quietly makes `CommitModeController` the route selector, which is right for two routes and wrong for three — a third route means lifting the selection into a small route controller with commit mode as one of its values.

**The bang layer retires with no vestige.** `lib/bang-commands.ts` is deleted along with every consumer: the `!` button and its `TugPopupMenu`, the `bangCommandCompletionProvider` and its `"!"` trigger, the ⌃⌘S/B/G/H chip-seed chords and the ⌃⌘C `!changes` alias, `bangCommandSurfaces`, the submit-path `matchBangCommandLine` call, the `isBangCommand` sigil branches in `command-atom.ts` and `slash-commands.ts`, and the `SHOW_SLASH_COMMAND_NOTICE` branch that taught `/shell` → `!shell` (a redirect that pointed the wrong way by the end). Two namespaces with two sigils was a tax paid on every surface — picker, completion popup, chip label, reconstructed command line, help text, unknown-command notice each carried a bang branch. **A leading `!` is ordinary prose again**: `!ls` goes to Claude as text. That is the intended behavior, not a regression — the shell auto-router (`shell-line-classifier`) carries a bare `ls` to the shell, which is a better path than the bang ever was. No notice, no redirect, no deprecation shim: a `!`-specific notice would itself be the vestige.

**`/shell` and `/btw` demote to arg-taking local slash commands**, their surfaces moved verbatim. `/shell <cmd>` is the deliberate override under the auto-router — the classifier decides by default, and a user who knows better can force it, which costs one registry entry and removes the "what if it misroutes" objection to deleting `!shell`. `/btw`'s duplication resolves itself: with the `!` menu gone there is one *place* BTW lives and `/btw` is merely how you *ask* — one feature with two organs, not two doors ([D108] stands unchanged). That place is now the **placard**: the Z2 diet dropped the BTW cell, and the answer surface pops from the top of the Z2 strip, right-aligned to the host card, rather than from a cell of its own. `SideQuestionStore` and the placard body are unchanged — only the anchor moved, and the cell it was anchored to was the widest thing on the row that nothing else needed. **Neither route has a typeable name.** A slash command is a one-shot verb that returns you where you were; a route is a mode you stay in, and the two do not belong in one namespace. `/prompt` could not have worked at all — the Changes route owns the document, so `performSubmit` lands the message verbatim and returns before slash interception, which left it reachable only from the route it already names — and the plan's `/changes`, shipped without a twin, was a half-namespace whose behavior nobody could predict from the other half. Both are gone: the route gestures are the Z4A tabs, ⇧⌘P / ⇧⌘C, and Escape, and `/commit [message]` remains the *verb* (it enters with a seed and is an act of committing, not an act of looking). Every slash command therefore leaves the route alone, which is what keeps "`/` is a typing accelerator" true without making the `/` key itself a route gesture. **`!history` is deleted as a route only** — the History shade, ⇧⌘H, Session ▸ Show History, and the `/tugplug:history` skill are untouched; the route was the unused part, and `/tugplug:history <question>` is a real Claude command that replaces the `!history <question>` variant directly.

**Find lifts out of the composer into a ⌘F bar** (`TugFindBar`, extracted from `TextCardFindBar` — one find face across the deck, over a host-supplied `FindSession`). Routing find through the prompt editor's submit path was a category error: it made find a *submission*, which is why it needed a dissolve-on-next-submit rule and an Escape-when-empty rule living inside the composer. All three vestiges of that are deleted — the submit-path dissolve, the empty-editor Escape branch, and the `findActive`-gated `TugFindCluster` in `Z4B`. ⌘F needed no Swift work: the chord was already plumbed end to end (Edit ▸ Find… → `sendControl("find")` → `action-dispatch` → the chain), and `host-menu-state` derives the menu item's enablement from `chain.validateAction(FIND)` — so the *only* missing piece was one responder registration on the session card, and registering it is simultaneously what un-greys the menu item. **Closing the bar ends the search** (`findSession.clear()`), but the **query survives** to the next ⌘F, pre-filled and selected — standard macOS behavior, and what makes clear-on-close painless. There is deliberately **no `/find`**: a `/find <query>` command would reintroduce the composer-as-search-box shape through a different sigil, and ⌘F is more discoverable than any slash command. See [D97] for the bar's geometry and its non-shade status.

**⌘F toggles, with no carve-out.** The chord that summons a mode is the chord that dismisses it — the shape ⇧⌘C already had on Changes. It must fire *from inside the bar too*, and that is the load-bearing half rather than an edge case: ⌘F lands the caret in the query field, so the second press of any real toggle attempt comes from inside. A bar that answered `FIND` with its own responder — re-summoning the field instead of closing — would make the toggle unreachable by the only gesture anyone makes. The bar therefore registers no `FIND` handler at all; the host's is the only one.

**Find and Changes are one slot.** Both are modes that take over the bottom of the card, and neither is a shade the other can rise over: opening the bar leaves Changes (through `CommitModeController.leave()`, which persists a typed message the way Cancel and the Z4A tab do), and entering Changes dismisses the bar. The exclusion is wired **off the mode's active flag**, not per door, which is what makes every entrance agree — ⇧⌘C, `/commit`, the tab, the Session menu, the failed-land re-entry.

**The bar joins the card's focus cycle; it does not bring a walk of its own.** Its four controls — query, options, Previous, Next — are stops in `SESSION_CYCLE_GROUP` at orders **8…11**, between the Z4 toolbar (0…7) and the Z2 status cells, which is exactly where the bar sits on screen (the cycle reads the card upward from its bottom edge). They register through the *same* `CycleScope` the prompt entry uses — a second `CycleScope` around the bar, which is what that primitive was always for — and they take a row in `cycleSpatialOrder`, so the arrow plane covers them like every other row. The stops exist only while the bar does; the walk skips what is not rendered, and the spatial row drops out empty.

This is worth stating as a rule, because the first attempt got it wrong: **a new surface on a card joins the card's authored order — it never declares a parallel one.** A private group in the base mode "worked" in the sense that Tab moved within it, and was wrong in every way that matters: two orders on one card, no spatial order at all on the new one, and ⌥⇥ walking a set of stops the user could see but not reach.

**An empty text field spends Tab on movement, and the movement is the cycle.** Indenting nothing is not an edit, so an empty composer or an empty query field has no use for the key — but the field keeps *owning* it (the `data-tug-tab-consume` marker stands) and simply spends it differently: a `Prec.high` binding calls the host, which enters the cycle **at that field's own seat** and takes one step (`useCycleMode`'s `enterAt`). So Tab out of an empty field is the walk it would have been if the user had already pressed ⌥⇥ — not a second mechanism, and not a jump to the top of the ring. The first typed character takes Tab back. `enterAt` is distinct from `toggle` precisely here: ⌥⇥ is a request for the walk itself and seeds the commit-home ([P10]); Tab is a request for the *next stop from here*.

Return and ⇧Return follow the caret for free: the bar's find keys live in its substrate's `Prec.high` keymap, so they are the ↓ / ↑ buttons' twins in the query field and are submit again in the composer. Dismissing the bar returns the keyboard to the composer **only when the bar was holding it** (`TugFindBarHandle.holdsKeyboard`, which reads both registers — DOM focus for the caret, the projected `data-key-view` mark for an engine-routed ring), and it returns it *the way it got there*: cycling exits the cycle and lets `restingFocus` land the caret, because a raw editor claim under a pushed mode is the focus-language bug that leaves ring and caret disagreeing. Escape at a bar control belongs to the **cycle** (it ascends), which is the language working; the bar's own `cancelDialog` handler covers the pointer path, where the chain has promoted to the bar and nothing else would answer.

**The cluster sits at the trailing edge, count first.** Count, then Case / Word / Grep, then ↑ / ↓ — result, then the controls that shape it, in one group. Centring the cluster (the shell's `toolbarCenter`) put a card's width of empty toolbar between the count and the buttons that change it, and needed an absolute-centring override to look right at all; the trailing group needs no CSS of its own.

**Chords.** ⌘F toggles the find bar; ⌘G / ⇧⌘G are unchanged (they stay registered on the prompt entry so ⌘G cycles with the caret in the composer, gated on the session holding matches — true exactly while the bar is open). ⇧⌘C keeps toggling Changes; with two routes a toggle is also a complete round-trip, so no Prompt-specific Swift menu item is needed. **⇧⌘P selects Prompt** and `CYCLE_PERMISSION_MODE` moves to **⌃⌘P** in *both* doors — the `keybinding-map.ts` entry and the Swift Session ▸ Permission Mode ▸ Cycle key equivalent — because either alone leaves the menu bar swallowing the chord. ⌘/ keeps `OPEN_COMMAND_PICKER` but now means "open the `/` completion popup," implemented as a plain editor insert of a leading `/`; the old Radix `focus()`-then-synthetic-Enter workaround left with the popup it worked around. ⌃⌘S / ⌃⌘B / ⌃⌘C / ⌃⌘G / ⌃⌘H are deleted, which is what leaves the ⌃⌘ band free for the permission cycle rather than making it a graveyard.

Originating plan: `roadmap/routes-rework.md`. Supersedes [D110] (three recipients) and amends [D119] (the "four bang routings" it contrasts commit against no longer exist). [D97], [D108], [D111], [L02], [L03], [L06], [L11], [L23], [L24], [L26]

**D123.** **A slot's stack gets a local switching surface — a title-bar badge, a picker built from the store, and ⌘R — and the stack itself is derived, never stored.** [D121] settled the geometry: any number of panes may hold one slot, assignment always raises, and the top one is what you see. What it left was a deck that hid its own evidence — `pane-occlusion-controller.ts` stamps a fully covered pane `data-occluded="true"` and CSS turns that into `visibility: hidden`, so a buried card contributed not one pixel to say it existed, and the Lens list, a remote surface, was the only way to reach it. D123 is the interaction decision that sits on D121's geometry. **The stack is derived from `DeckState.panes` and never stored** (`slotStackOf(state, slot)` beside `findLensPane`): membership and z-order are both already fully determined by state the deck owns, so a stored copy could only ever disagree with it — and deriving keeps the surface automatically correct across `assignCardToSlot`, drag/resize eviction, kind changes that clamp slots, and pane close. No field is added to `TugPaneState`, no serialization version moves, and the collapse case needs no notification: when a peer leaves and the depth drops to 1 the badge stops rendering, and the prop the picker already reads is the signal that closes it (a stale-open bit would otherwise unmount the trigger with the menu up, stranding keyboard focus on a removed node and re-mounting already-open). `DeckCanvas` resolves it — the same vantage point that resolves `placement`, for the same reason: a pane cannot work out chain-wide facts from its own state — and hands each pane a display-resolved `SlotStackEntry[]` rather than raw pane records, so `CardTitleBar` renders its menu from props alone and never reads a store ([L10]). **The badge shows on every pane in the stack, not only the front one.** It describes the *slot*, and panes in one slot may differ in width (the imposer never touches width), so a wider buried pane peeks out title bar and all — suppressing its badge would make the one title bar the user can see the one that stays silent. Occlusion already solves the common case: a fully covered pane is hidden, badge included, so a same-width stack shows exactly one. The render condition is therefore `slotStack.length > 1` and nothing else — no "am I the top?" test, which would need a second cross-pane fact. **The picker is built from store data, never from revealed DOM.** Showing the buried title bars as live DOM would mean bracketing the gesture with `paneOcclusionGesture.begin()`/`.end()`, un-hiding every pane on the deck for its duration and re-hiding them on a settle timer — a large, timing-sensitive cost for a list of strings the store already holds, and a pile of peeking title bars is a worse answer to "let me pick one of these" than a menu is. **A row is a miniature of the title bar it stands for**: it draws that pane's own `CardMeta.icon` ahead of that pane's own title-bar text, in the title bar's order, so the menu reads as a stack of small title bars rather than as a list of strings — both ride `SlotStackEntry`, resolved by `DeckCanvas` because the title bar renders its picker from props alone and reaches for neither a registry nor a store. **And a pane's name is produced in exactly one place, `lib/pane-title.ts`.** A pane's title is not a stored field: it composes the active card's registry title, the group name a multi-tab pane carries, and the live override a card publishes on `cardTitleStore` when its identity resolves at runtime. Two rules for that existed before this — the title bar's composition, and a simpler `CardState.title` fallback chain in the menu-state projection — and they disagreed exactly where it mattered most: a Session card bound to a project read `test-repo/petit-thaw` on its title bar and `Untitled` in every list that named it, because only the title bar consulted the override store. The module now holds the rule (`composePaneTitleBarText`, pure over the three parts, called by `CardTitleBar` which already holds them) and its resolver (`paneTitleBarTextFor`, which does the registry lookup and the override read for callers holding deck state), and all three naming surfaces adopt it — the title bar, the picker, and the Window menu's pane list, whose `Untitled` entries this also fixes. The override is not deck state, so each consumer subscribes to `cardTitleStore` in its own idiom: `DeckCanvas` through `useSyncExternalStore` on a new monotonic `version()` (a per-card `get()` cannot be a stable snapshot, and several panes' names are needed at once), and `initHostMenuState` by re-projecting from a plain subscription. That revision must be bumped *before* the listeners run, or a `useSyncExternalStore` subscriber reads the value it already had and nothing recomputes — a picker that notifies and does not repaint. **The badge stays lit while its menu is down**, in the pressed pose it already owns rather than a new colour, because a menu being open is a control being held — the macOS menu-bar reading, where the title that opened the menu keeps its highlight until it closes. Radix stamps `data-state="open"` on the trigger, so one CSS rule covers all three doors, including the two that never touch the badge. Its inline padding is derived from the square icon buttons beside it (`(height − icon-size) / 2`, their optical inset) rather than taking the stock `sm` 1rem, which set the one non-square control in the cluster 9px further from its neighbour than they sit from each other and split the row into two visual groups; the count is `tabular-nums` so a raise cannot jog the cluster, and nothing is reserved for a second digit — a ten-deep slot is rare enough that parking its width beside every badge would just restore the gap. **Three doors, one opener, one raise.** The badge is a ghost `TugButton` acting as a `TugPopupMenu` trigger, so the pointer path needs no open plumbing at all; the keyboard and Cmd-click paths drive the menu through a new optional controlled `open`/`onOpenChange` pair on `TugPopupMenu` (absent, every existing call site keeps its exact current uncontrolled behavior), because synthesizing a click on the trigger would be a guess at whichever DOM event Radix listens on and a lie about what the user did. All three converge on one `CardTitleBarHandle.revealStack()`, reached the way `CLOSE_PANE` already reaches `requestClose()`. Choosing a row raises through `transferFocusForActivation` + `activateCard` — what `assignCardToSlot` does, and for its stated reason: a bare `activateCard` flips the first responder but skips the focus transfer, so the outgoing card never saves its focus bag and the incoming one gets no caret until clicked. **Cmd-click resolves in the drag machine's own no-travel branch, so Cmd-drag survives.** Cmd-drag on a title bar is meaningful twice over — the Mac convention of moving a background window without raising it, and the gesture that *evicts* an imposed pane from its slot — so opening a menu on Cmd-*press* would kill both. `handleDragStart` records `event.metaKey` beside the origin it already snapshots, and `onPointerUp`'s existing `if (!dragMoved.current)` branch (already commented "the pointer never travelled, so this was a click on the title bar") acts on it; a drag past `DRAG_MOVE_THRESHOLD_PX` latches `dragMoved` and never arrives. No second gesture handler exists to fight the frame's pointer capture — one could not see the release anyway, since `handleDragStart` captures on the *frame* and every pointer event retargets there — and deferring `onDragStart` until travel would run `captureFocusForDragStart` after the mousedown blur it exists to beat. `gesture-interpreter.ts` is untouched: its `metaKey` branch already suppresses activation, which is exactly the posture wanted, and the title bar already carries `data-tug-fr-preserve`. **The chord is menu-owned and depth-gated.** ⌘R is `Window ▸ Reveal Stack`, not a `keybinding-map.ts` entry, because AppKit resolves a menu key equivalent before the `WKWebView` sees the keydown; freeing it moved `Maker ▸ Reload` to ⇧⌘R. Enablement pulls from a new `stackDepth` on the menu-state payload — panes sharing the *focused* pane's slot, 0 when it holds no slot or nothing is selected — and the item validates enabled iff that exceeds 1, deliberately without the deselected-deck escape hatch its neighbours take, because Reveal Stack acts on a specific pane's stack and picking one for the user is the non-obvious-target failure. **The handle toggles, and that is ordering rather than a race:** an open `TugPopupMenu` closes on any chain dispatch that is not its own selection blink, `REVEAL_STACK` travels the chain, and `sendToFirstResponder` runs the responder action *before* notifying observers — while a closed menu has no observer registered at all. So open → the toggle queues false and the observer queues false again; closed → the toggle simply opens. Both single-valued. What this decision does **not** do: cycle the stack (⌃\` stays deck-wide; the picker plus arrow keys is the keyboard path — superseded by [D124], which adds a no-look cycle and makes ⌘R's owner a preference), give free panes any behavior (no slot, no stack, no badge, no chord, and no "most recently active stack" fallback), repurpose Cmd-click in a pane's *content* area, revive buried panes' DOM, change how panes enter or leave a slot, or add a tab strip. `deck-store-selectors.ts` (`slotStackOf` / `SlotStackEntry`), `lib/pane-title.ts` (`composePaneTitleBarText` / `paneTitleBarTextFor`), `lib/card-title-store.ts` (`version`), `components/chrome/deck-canvas.tsx` (the derivation and the raise), `components/chrome/tug-pane.tsx` (badge, picker, `revealStack`, the `REVEAL_STACK` responder entry, the meta-click branch), `components/tugways/internal/tug-popup-menu.tsx` (controlled open), `components/tugways/action-vocabulary.ts` + `action-dispatch.ts` (`reveal-stack`), `lib/host-menu-state.ts` (`stackDepth`), `tugapp/Sources/AppDelegate.swift` (the item, its selector, the parse, the validation, the Reload move). App-tests: `at0347` (badge, picker, raise, Cmd-click, occlusion), `at0348` (the gate and the chord's toggle), `at0349` (Cmd-drag still drags — foreground, because `dragMoved` latches only inside a rAF callback). Originating plan: `roadmap/stacks-interaction.md` [P01]–[P10]. Full doctrine: [pane-model.md § Three geometry modes](pane-model.md). [D121], [L02], [L06], [L09], [L10], [L11], [L24]

**D124.** **A slot's stack gets a second, no-look switching command, and ⌘R's owner becomes a preference rather than a verdict.** [D123] gave the stack a picker and put ⌘R on it. That is the right gesture while you are still learning what is in a slot and the wrong one once you know: the picker has to be read, so switching costs a look even when the user already knows the card they want is the one behind. `Window ▸ Cycle Stack` is the counterpart — it brings a buried pane forward and puts **nothing** on screen, which is the entire point. Nothing to read means nothing to aim at, which means the press can be repeated blind. **Cycling raises the bottom-most pane in the slot, not the one below the front, and that is what makes it a ring.** Each raise sends the outgoing front pane exactly one place back, so a depth-N slot returns to its starting order after N presses and the user can count instead of look. Raising the next-one-down is the obvious rule and it is broken: with three panes it ping-pongs between the top two forever and the third is unreachable, because every raise rewrites the order the "next" was computed from. Raising the buried-longest is also what ⌘-backtick does with windows, so the behavior is borrowed rather than invented. It needs no new store mutation either — it is the raise [D123] already defined, aimed at the last `SlotStackEntry` — and the pane answers `CYCLE_STACK` itself rather than delegating to its title bar, because there is no transient UI to own; the title bar is [D123]'s route only because the picker's open bit lives there. The raise moves the first responder to the pane that came up, so the *next* press is answered by that pane reading its own freshly-ordered stack, which is why one press is stateless and N presses still compose. **Neither command is right for everyone, so the chord is a setting.** Both items always exist in the Window menu and both gate identically on `stackDepth > 1`; the preference (Settings ▸ General, `stack-chord-store.ts`, persisted at `dev.tugtool.app` / `stackChord` like the theme) moves only the key equivalent. It has to move on the host side — AppKit resolves a menu key equivalent before the `WKWebView` sees the keydown, so the frontend cannot claim or release ⌘R by any route — which is why the value rides the menu-state payload and `applyStackChordKeyEquivalent()` assigns it in `updateMenuState`, outside AppKit's key-equivalent scan, exactly as the dynamic ⇧⌘S for Save As… already does. The preference is not deck state, so `initHostMenuState` subscribes to its store directly and re-posts, the same way it subscribes to `cardTitleStore`. The default is **cycle**: the picker keeps two doors that need no chord at all (the badge, and Cmd-click on the title bar), while the cycle has none, so defaulting the chord to the surface that already has doors would leave the new gesture unreachable without a settings trip. What this decision does **not** do: add a reverse direction (a ring returns home on its own, and ⇧⌘R is `Maker ▸ Reload`), show any HUD or transient overlay during a cycle (the raised pane's own title bar is the feedback — a HUD would put back the thing the user is trying to skip), cycle anything but the focused pane's slot, or give free panes and the Lens any behavior. `stack-chord-store.ts`, `settings-api.ts` (`readStackChord` / `putStackChord`), `main.tsx` (boot seed + DEFAULTS push), `components/chrome/tug-pane.tsx` (the `CYCLE_STACK` responder entry), `components/tugways/action-vocabulary.ts` + `action-dispatch.ts` (`cycle-stack`), `lib/host-menu-state.ts` (`stackChord`), `components/tugways/cards/settings-general-body.tsx` (the choice), `tugapp/Sources/AppDelegate.swift` (the item, its selector, the parse, the shared validation, `applyStackChordKeyEquivalent`). App-tests: `at0350` (the ring across three presses, and that handing the chord away takes it from the other item), `at0348` (which now sets the preference before pressing), `at0168` (the chord table at its default). [D123], [D121], [L02], [L11], [L24]

**D125.** **A card's own name replaces its type name in the title bar; it does not extend it.** [D123] put a Pane's name in one place (`lib/pane-title.ts`) and made every naming surface — title bar, slot-stack picker, Window menu — go through it. That fixed the disagreement between them but preserved the composition each had inherited, which joined the registry title to the live override: a Text card holding `changes-rework.md` was called **`File : changes-rework.md`**, on its title bar and in every list. The first word is redundant twice over. The filename already says it is a file, and the row draws that card's own `FileText` icon immediately to its left — [D123]'s own "a row is a miniature of the title bar" is what makes the word provably surplus, because the glyph carries the type and the text no longer has to. **So an override REPLACES the registry title.** The registry title is the FALLBACK — what a card of that type is called before it has a name of its own, which is what an unopened Text card ("File") or a Diff card ("Diff") legitimately shows — rather than a category stamped in front of every name. A **group name still prefixes**, because it is redundant with nothing: it is the name the user gave a multi-tab Pane, and no other part of the title bar carries it, so a named group of open files reads `Notes : draft.md`. This also retires a workaround: a card whose identity is entirely dynamic used to declare an EMPTY registry title so that its override would stand alone (the About and Session cards both do this), and that trick is now simply the rule — the empty declarations are harmless but no longer load-bearing, and a new dynamic card needs no such trick. One function moved (`composePaneTitleBarText`), and because [D123] had already collapsed the three surfaces onto it, the title bar, the picker, and the Window menu all changed together with no second edit — which is the argument for consolidating a rule before changing it. `lib/pane-title.ts`. Unit tests: `lib/__tests__/pane-title.test.ts`. [D123], [L10]

## Commands & Chords

**D126.** **Which chord a command gets is derived from a modifier algebra, not chosen from what happens to be free.** [L30] made every chord a registry fact, which settled *where* chords live and said nothing about *which* chord a command should get — so the chords accreted, each one taking whatever was near a mnemonic letter and climbing the modifier stack whenever plain ⌘ was already spoken for. That produces a keymap that is a list of memorized facts: unpredictable for a user, and offering an author no way to tell a good grant from a bad one. **Two base tiers and two operators now generate every modifier set in use** — ⌘ the universal tier (verbs any Mac user knows, plus the highest-frequency Tug verbs; digits are places), ⌃⌘ the Tug tier (surfaces, shades, modes, themes, app-specific machinery, anchored by the macOS-conventional ⌃⌘F Full Screen), ⇧ the counterpart operator (reverse/widen/undo), ⌥ the variant operator (same verb, altered object or form) — and every remaining set is a composition with a derived reading: ⌥⇧⌘ both twists ("…as Plain Text"), ⌃⇧⌘ the counterpart of a Tug-tier command, ⌃⌥⌘ the advanced form of one. Plain ⌥ letters (glyph typing) and plain ⌃ letters (the caret's emacs set) are **closed forever**, with ⌥⇥ and ⌃\` grandfathered as non-printing exceptions that would not be granted today. The argument for adopting this rather than inventing something is that it was already half-latent in shipped bindings — ⌘V → ⌥⌘V → ⌥⇧⌘V, ⌘Z → ⇧⌘Z, ⌘W → ⌥⌘W — so writing it down moved nine chords and explained the rest. Six rules ride with it, and the load-bearing two are **pairing** (a ⇧/⌥-composed chord shares its key with the base it twists; a chord with no base to vary is a plain-⌘ or ⌃⌘ candidate, never a taller stack chosen because the short one was taken) and **scarcity** (plain ⌘'s free slots go only to commands hit many times an hour, so the frequency claim is part of the grant). The rest: the arrows exemption (⌥⌘↑/↓ has no ⌘ base because ⌘↑/↓ is composer currency; ⇧ still means "to the extreme"), closed sets stay closed, ⌘. parity, and **menu placement is a chord decision** — a `menuEligible` chord preempts every scoped binding regardless of focus ([commands.md](commands.md), four-layer resolution), so one decision records both the tier and the `menuEligible`/`scope` choice.

**The algebra spends exactly the band [D124] cleared, and re-homes both chords [D124] placed.** That entry deleted ⌃⌘S/B/C/G/H "which is what leaves the ⌃⌘ band free", gave ⇧⌘P to the composer's Prompt route, and moved `cycle-permission-mode` to ⌃⌘P *in both doors* because either alone leaves the menu bar swallowing the chord. The continuity is the point rather than an accident: **Prompt Route moves ⇧⌘P → ⌃⌘P** (route selection is Tug machinery; ⇧ was carrying nothing, since there is no ⌘P base for Prompt to be the counterpart of) and **Cycle Permission Mode moves ⌃⌘P → ⌃⌥⌘P**, debuting the advanced tier — permission modes govern agent autonomy, the quintessential expert feature — and vacating ⌃⌘P is precisely what lets Prompt Route land on its own tier. Both doors move again, for [D124]'s reason. **Show/Hide Changes ⇧⌘C → ⌃⌘C** and **Show/Hide History ⇧⌘H → ⌃⌘H** (shade toggles are machinery; the twins move together), **Generate a Commit Message ⇧⌘M → ⌃⌘M** (still composer-scoped, now joining one mnemonic cluster: ⌃⌘C the shade, ⌃⌘M the message, ⌃⌘A the claim), **Next Theme ⌥⌘T → ⌃⌘T**, and **Insert File ⌘I → ⌃⌘I**, which frees ⌘I from its italic baggage under scarcity. Freed and returned to the pool: ⇧⌘P, ⇧⌘C, ⇧⌘H, ⇧⌘M, ⌥⌘T, ⌘I. Every move is a registry-default edit; **user overrides are untouched and no migration exists**, because an override is a per-command binding list in tugbank where *absent means registry default* — a moved default is exactly the case the override design already absorbs.

**Next Theme's chord enters the registry, because a chord that exists only as a Swift construction literal is invisible to the app that owns it.** `next-theme` carried `menuItemId: "view.nextTheme"` with **no bindings at all** — ⌥⌘T lived solely in `buildMenuBar()`, so the keymap pane could not show it, `resolveChord` could not reason about it, and no user could rebind it. It gains a real binding (⌃⌘T, `menuEligible`, `preventDefault`), which is sufficient on its own: `host-menu-state.ts` publishes a chord claim for a **non-`mirrored`** entry too, since presence in `menuChords()` is what the push tests. Construction literals for all four moved menu items are edited in the same change — they are pre-first-push defaults only, but a cold-start menu bar must never flash a chord the registry no longer holds.

**Claim All ⌃⌘A and Disclaim All ⌃⇧⌘A become registry commands rather than buttons the keyboard cannot reach.** Both are `routing: "first-responder"` with composer-responder-scoped bindings, registered in `tug-prompt-entry.tsx` beside the existing `commit-auto-message` registration under the *same* `commitActive` gate — because raising the Changes shade **is** entering commit mode, and the shade is a **passive** sheet that leaves the composer holding focus, so the composer's responder is where the chords are genuinely live. **Registration and handling live in different components, unlike `commit-auto-message`** (which does both in the composer): these two are handled in `session-card.tsx`, because the card is the only component holding `changesController`, and the chain makes the split work — the composer's responder sits below the card's, so a first-responder action the composer does not claim falls through. The gate is where the surface is; the semantics are where the controller is. **⌃⌘A is deliberately stronger than either shade button.** The shade wires two bulk buttons (claim-all-unattributed and claim-all-orphaned); the chord claims **both buckets at once**, because a single keyboard verb meaning "make everything claimable in front of me mine" is worth more than one that mirrors one button and sends the user to the mouse for the other. The buttons remain the granular path. **The pair is not an inverse on one set** — ⌃⌘A acts on what is *not yet* this session's, ⌃⇧⌘A on what *is* — so ⇧-as-counterpart is carrying "the opposite bulk verb of this shade", not "the same set, reversed"; the algebra promises a shared key and an opposite sense, never set inversion, and this pair is the reason to say so out loud. Both no-op on an empty path list or a pending claim/disclaim round-trip, read non-reactively from `changeset-verb-store`. Bindings are the door, so `lintCommandTable` passes with no `internal` declaration; the keymap pane shows them as scoped, non-rebindable rows.

**⌘. parity is a matcher change, not a routing change.** Five components hand-matched `metaKey && key === "."` even though `cancel-dialog` owns both ⎋ and ⌘. in the registry — `tug-alert.tsx`, `tug-confirm-popover.tsx`, `tug-sheet.tsx`, `tug-placard.tsx`, `tug-editor-context-menu.tsx`. They are **not naive bypasses**: each documents why chain routing is unreliable for its surface (a portaled popover whose buttons never promote; a placard outside any focus mode), so the [L30] defect is only the authored chord match — which also mis-matches by `event.key` label instead of `code` identity. One exported helper, `isCancelChordEvent(event)` in `keymap-registry.ts`, returns true when the event matches any `cancel-dialog` binding **except Escape**, and the five surfaces call it in place of their literals while keeping every dismissal path and propagation posture exactly as documented. **Excluding Escape is load-bearing**: the engine owns ⎋'s ladder for alerts, sheets, and context menus, and a helper that matched it would let five conversions quietly steal it. `chordMatchesEvent`'s parameter widens to a five-field `ChordEventFields` structural type rather than each call site casting — the cast would be a lie repeated five times, and the widening is what lets one predicate serve native listeners and React synthetic events alike. The context menu's extra `ctrlKey` alternative is dropped: the registry chord is the one truth. **Parity extends to dismissal, not to field-revert.** ⌘. mirrors ⎋ wherever ⎋ dismisses a surface or backs out of a mode, so the sweep adds it to the dev error overlay, both image-lightbox paths, and the editor's completion popup. It is deliberately withheld where ⎋ *reverts an in-field edit* — `tug-filter-field`, `tug-value-input`, `tug-slider`, and the gallery's in-flight hue scrub — because that ⎋ is form-control currency inside a typing surface, editing the value rather than leaving the surface, and ⌘. there would be both surprising and outside macOS convention.

The doctrine doc is [chord-tiers.md](chord-tiers.md) — the algebra, the six rules, the plain-⌘ free-pool ledger, the macOS never-bind list, and the known anomalies, including the deferred Cascade ⌃⌥C / Tile ⌃⌥T re-home and the shade toggles' un-detachable menu claim. `tugdeck/src/components/tugways/command-registry.ts`, `keymap-registry.ts`, `chord-format.ts`, `action-vocabulary.ts`, `tug-prompt-entry.tsx`, `cards/session-card.tsx`, the five converted surfaces; `tugapp/Sources/AppDelegate.swift` (`buildMenuBar` literals). Originating plan: `roadmap/keyboard-command-cleanup.md`. This supersedes the chord spellings embedded in the prose of [D122]–[D125] without editing those entries, and most directly [D124], whose ⌃⌘ band it spends and whose two P-chord placements it re-homes; it also removes the stale comment left by [D125]'s retirement of the ⌃⌘C `!changes` alias, and makes [D117]'s ⇧⌘C collision note in `at0253` obsolete. [L30], [L02], [L11], [D117], [D122], [D124], [D125]

---

## Model Work

**D127.** **Auxiliary model work runs on SharedAgents over the user's own subscription, never on a model Tug ships.** Tug's non-conversational model work — pulse intent headlines, shell-command arbitration, and whatever comes next — runs on an app-scoped pool of persistent, job-constrained headless `claude` workers authenticated by the Claude Code subscription the app already requires. **Tug does not ship, download, host, or update model weights.** The retired arrangement was an on-device MLX pack (qwen3-4b): ~2.4 GB resident and 2.28 GB on disk to run a model markedly weaker than the one the user is already paying for, behind a catalog, a downloader, a progress UI, a Configure Tug wizard step, and a second Swift inference backend that existed only to feed it. The subscription is a hard prerequisite of the whole app, so the remote path adds no dependency that was not already load-bearing — which is the argument that makes this a subtraction rather than a trade. **Cost accepted, explicitly:** these features stop working offline, degrading to the absent posture they were already designed to have.

**A SharedAgent is job-constrained by construction, not by convention.** `AgentSpec` names a model and a fixed `&'static [JobSpec]` table; `JobSpec` carries a job's instructions, its timeout, and its slow-mark. **No API anywhere accepts an arbitrary prompt** — `run(job_name, input)` looks the job up in that table, and a caller holding the pool cannot ask it for anything the table does not already name. That is what keeps a shared pool from becoming a general-purpose model endpoint inside the app, and it is why adding a second agent (the Sonnet one) is a second `AgentSpec` over the same machinery rather than new machinery. **Workers are persistent because a cold turn cannot meet the latency contract**: measured against CLI 2.1.222, a cold turn is 2327 ms — more than classify's entire 2 s budget — against 867–989 ms warm. Warmth is the feature; the pool exists to hold it — so **nobody ever waits on a cold start**. A job whose ceiling is shorter than one is never handed a worker that has not answered yet: its turn becomes that worker's warmup and the caller degrades on the spot. Lanes are otherwise warmed ahead of any caller (`ensure_warm`, called on traffic that precedes the job — a card asking for the login-PATH set warms the classify lane), and stay warm across a recycle because the replacement is warmed before the old worker stops answering. Workers recycle at `MAX_TURNS_PER_WORKER` (40) so a transcript cannot grow without bound, reap on idle per class (30 min for classify, whose ceiling cannot pay a respawn; 5 min for summarize, whose can), and respawn no faster than `RESPAWN_MIN_INTERVAL` (5 s) so a persistently failing spawn cannot become a fork bomb.

**Workers carry a job class, and classify never queues behind summarize.** The two jobs have contracts an order of magnitude apart (`CLASSIFY_TIMEOUT` 2 s against a user waiting on a keystroke; `SUMMARIZE_TIMEOUT` 6 s against a background cadence), so a single FIFO would let one in-flight summarize spend a classify's whole budget before it was even dispatched. `JobClass` splits the pool: a classify arriving during a slow summarize gets a same-class worker, never the summarize worker's queue. The rule is one `match` and it is the one thing in the pool a unit test caught wrong — `classify_with_grammar` fell into the summarize lane, which is precisely the collision the affinity exists to prevent, and which nothing but a test would have surfaced.

**A worker is isolated because it runs `claude`, and `claude` is an agent.** `--disallowedTools '*'`, `--strict-mcp-config`, `MAX_THINKING_TOKENS=0`, and a neutral Tug-owned cwd at `base_data_dir()/shared-agent`. The isolation is not precautionary: the unisolated posture *actively misbehaved* in the bringup spike — the worker invoked Bash on a tool-baiting prompt, and with the cwd left in a repo it read that repo's `CLAUDE.md` and appended a decoy token to its answer. A summarizer that inherits project instructions is a summarizer whose output is a function of whatever directory it happened to start in. The neutral cwd also parks worker transcripts under one encoded directory rather than scattering them through the user's projects.

**Every failure shape produces one degraded answer.** No agent configured, dead worker, timeout, malformed or refused verdict — all of it collapses to the same thing at the boundary: classify emits `ok:false, verdict:null` and the line goes to Claude with no routing chips; summarize emits no headline and arms backoff. The deck never learns *which* failure happened, because there is no action it could take that differs. **And under app-test the pool refuses to spawn at all** (`TUGAPP_APP_TEST=1` ⇒ `run` returns `Err` before any process starts), which makes the absent posture the *only* posture an app-test can observe — so `at0280` needs no tugbank seeding to force it, and no app-test can cover the positive path by construction. That limitation is stated in the test's own docblock rather than papered over; the positive path is covered by an on-demand real-`claude` test and by fake-spawner unit suites. Model prose is never asserted anywhere, per the scribe convention.

**The timeout triad is a cross-file invariant, so each of the three names the other two.** `CLASSIFY_TIMEOUT` (`shared_agent.rs`), `CLASSIFY_REQUEST_TIMEOUT_MS` (`shell-classify-store.ts`), and `VERDICT_SUBMIT_WAIT_MS` (`tug-prompt-entry.tsx`) are all 2000 ms and have to move together — a submit that gives up before the transport does would route a line to Claude while the verdict was still in flight. `CLASSIFY_SLOW` is **1500 ms, not the 1 s the plan proposed**: at a measured ~900 ms warm, a 1 s mark would fire on roughly half of all calls, which does not raise an alarm so much as retire one.

**Readiness is a tenant switch and a transport check, deliberately not a probe.** `useSharedAgentReady` is true when the `dev.tugtool.shared-agent` switch is enabled (absent reads as enabled) and the connection lifecycle is `open`. It does **not** ask the pool whether a worker is alive, because a probe would cost a turn to answer a question whose wrong answer is already free: the fallback lattice makes an unavailable agent indistinguishable from a slow one at every call site, so the only thing a probe could buy is suppressing a request that already degrades correctly.

What this decision does **not** do: put conversational model work on the pool (sessions are the user's own `claude`, unchanged), permit any caller to compose a prompt, promise these features work offline, or claim the pool is a general inference service. `tugrust/crates/tugcast/src/shared_agent.rs` (the pool, `HAIKU_AGENT_JOBS`, `AgentWorkerSpawner`, the `SHARED_AGENT_DOMAIN` reads), `feeds/session_overview.rs`, `feeds/shell.rs` (`shell_classify`), `actions.rs` (`shared_agent_*` observability verbs); `tugdeck/src/lib/shell-classify-store.ts`, `lib/shared-agent-store.ts`, `components/tugways/tug-prompt-entry.tsx`, `configure-tug.tsx` (the wizard is four steps with the on-device-AI step gone). App-test: `at0280-shared-agent-absent`. Originating plan: `roadmap/shared-agent-bringup.md`. [L02], [L22], [L24], [L27]

**D129.** **Card navigation is one relationship with two axes — lateral and depth — and the Window menu's ad-hoc arrangement commands are retired.** The Window menu accreted five movement commands with four unrelated shapes: Previous/Next Card walked one pane's tabs and stopped at its edges, Cycle Panes (⌃`) hopped panes with no inverse, Cycle Stack rotated a slot one way under a ⌘R it had to share by preference, and Cascade/Tile rearranged geometry the imposer had long since superseded. D129 replaces all of it with a quartet that reads as one sentence: **Previous/Next Card move side to side, Previous/Next Card in Stack move front to back, and together they reach every open card.** The **lateral pair keeps ⇧⌘[ / ⇧⌘]** — untouchable macOS tab convention — and now walks the deck's whole *lateral ring*: every tab of every **visible** pane (the front of each slot and rail, plus the free panes), so at a pane's last tab the step crosses into the next pane instead of wrapping. **The sidebars are on the ring, the Lens included** — they are cards the user reads and types in, standing in plain sight, and a lateral walk that skipped them would leave the card that is always on screen as the one the keyboard could not reach. The first cut excluded the Lens by analogy to `move-to-slot`; that exclusion is a fact about *slots* — a sidebar takes none — and reading it as a general rule about sidebars is what left the Lens off the ring while Jots rode it. A rail is a place exactly as a slot is ([D128]), so a rail's front is a ring position exactly as a slot's front is. That is what retires Cycle Panes rather than merely deleting it, and it restores the deselected-deck keyboard escape too (both directions re-activate the topmost card when nothing is selected). The ring's order is **structural, never measured** — left rail, slotted fronts by slot number, free panes by stored position, right rail (`lib/card-ring.ts`) — so the same function gates the menu item (`visibleCardCount` on the menu-state projection) and computes the step, and the gate cannot disagree with the gesture. The handler moved from the pane to the **deck canvas**, the one responder that can see every pane; `activateCard` both raises the target's pane and makes it the active tab, so one call serves the within-pane and cross-pane steps alike, wrapped in `transferFocusForActivation` as every activation is. The **depth pair takes ⌥⌘[ / ⌥⌘]** — the ⌥-variant of the lateral pair on the same keys, exactly the composition [D126]'s pairing rule generates: same motion, other axis. NEXT is [D124]'s raise-the-buried-longest ring, renamed; **PREVIOUS is its exact inverse, and needed the one new store mutation in the change** (`sendPaneBehind`): the front pane goes all the way to the back and the one beneath it fronts, because the tempting rule — raise the second-from-top — is [D124]'s ping-pong failure wearing the other label. This supersedes [D124]'s "no reverse direction" posture and dissolves its chord preference whole: **⌘R belongs to Reveal Stack outright**, stated as an ordinary registry binding, and the arbitration machinery — `stack-chord-store.ts`, the `dev.tugtool.app/stackChord` tugbank key, the Settings ▸ General choice, `applyStackChordPreference`, the payload's `stackChord` field — is deleted, not parked. Reveal Stack keeps its depth gate and its `disabledChord: "detach"`. **Cascade and Tile are removed altogether**, command and code (`arrangeCards`, the `arrange-cards` wire, both menu items): the Layouts system and the imposer are how Tug arranges the deck, period — which also resolves [chord-tiers.md](chord-tiers.md)'s ⌃⌥ anomaly by deletion rather than re-homing. ⌃` returns to the closed ⌃ set, leaving **⌥⇥ the only grandfathered exception**. `tugdeck/src/lib/card-ring.ts` (the ring), `deck-manager.ts` (`sendPaneBehind`; `arrangeCards` gone), `components/chrome/deck-canvas.tsx` (the lateral pair), `components/chrome/tug-pane.tsx` (the depth pair), `components/tugways/command-registry.ts` + `action-vocabulary.ts` (`previous-stack-card` / `next-stack-card`; `cycle-card` / `cycle-stack` / `arrange-cards` retired), `lib/host-menu-state.ts` (`visibleCardCount`; `stackChord` gone), `tugapp/Sources/AppDelegate.swift` (the quartet, the removals). App-tests: `at0350` (both directions of the depth ring, and that they are inverses), `at0169` (the lateral ring's gate crossing panes, and shrinking when a stack buries a card), `at0348`, `at0168`, `at0193`. [D121], [D123], [D124], [D126], [L02], [L11], [L30]

**D130.** **A card's width is a keyboard gesture on the Tug tier, and the digit row means "index into an ordered set" rather than "place on the deck."** Width was reachable by exactly one door — the title bar's width popup — which is a mouse trip for a property you retune constantly while reading. D130 adds two more doors onto the same act: **⌃⌘1 / ⌃⌘2 / ⌃⌘3** for Slim / Comfy / Wide, and a three-row check-marked group in the **Window** menu, both landing on the pane the selection is in. The proposal that started it was ⌥⌘1–3, and **R1 is why it was rejected**: ⌥ is the variant operator, so ⌥⌘n has to read as a variant of ⌘n — a variant of Move Card to Slot *n* — and width is a different verb on a different property, so the composed set had no base to compose from. Landing on ⌃⌘ digits instead required amending [chord-tiers.md](chord-tiers.md)'s "digits are places," which was only ever true of the tier that had digits and, stated flatly, made the ⌃⌘ digit row underivable. The wider rule — **a digit indexes an ordered set; the tier says which set** — keeps the old reading inside it (⌘n *is* a place) and comes with the constraint that makes it a rule rather than a pool: the family must be an order **the user already sees**, which slots (left to right) and `CONTENT_WIDTH_PRESETS` (narrow to wide, the order every picker offers) both are. **Three commands, not one cycling command**, though ⌃⌘T Next Theme would have licensed a cycle: the set is static and small, the gesture is meant to be no-look, and a cycle you must know your place in is one you have to look at — the same argument that makes Next Card in Stack a ring ([D129]) rather than a swap. It also gives the menu three check-markable rows instead of a verb whose current value is invisible until you fire it. **Promotion to the menu is the half of the grant R6 asks for**, and it is safe here exactly where it was not for the slot family: ⌘1–9 stay chord-only because surfaces like the PDF viewer decline them by hand to leave the digits with the deck, and a menu item would take that choice from every surface after — while nothing in the app claims ⌃⌘ digits. **No new state, and no second opinion about width.** The chord's handler lives on the **deck canvas** (the one responder that can name which pane the selection is in, as with ⌘1–9) and dispatches the existing `set-card-width`, so the clamp to the stack's bounds and the `widthPreset` stamp are identical on all three doors; the enablement and the radio mark ride one new menu fact, `cardWidth`, gated on the same two conditions the title bar hides its popup behind — a pane is selected, and it is not a rail. A pane dragged to a width of its own has no preset stamped, so **no row is checked** rather than the nearest one — the settled control shows what the geometry holds. The Swift items carry **empty key equivalents** so `applyCommandChords` writes the chords from the table and all three stay rebindable, the discipline the sidebar toggles follow and the shade toggles do not. `components/tugways/command-registry.ts` + `action-vocabulary.ts` (`set-pane-width`, the three parameterized entries, the `cardWidth` fact), `components/chrome/deck-canvas.tsx` (the handler), `action-dispatch.ts` (the wire bridge), `lib/host-menu-state.ts` (the projection), `tugapp/Sources/AppDelegate.swift` (the Window group). [D126], [D128], [D129], [L11], [L30]

**D131.** **Bullseye is a temporary *presentation* layered over whichever geometry mode a pane already holds — never a fourth stored mode.** The deck could place a card four ways and size it four ways, and still had no gesture for the thing a writing app calls a focus mode: *put the thing I am working on in front of me, at a comfortable column, and push everything else back — for now.* **⌃⌘B** and **Window ▸ Bullseye** are that gesture. The name is deliberately not "focus mode": *focus* already names the responder-chain concept in this codebase ([focus-language.md](focus-language.md), `transferFocusForActivation`, `data-focused`), and a second meaning for it would be a permanent tax on every reader. **The obvious implementation is a trap, and avoiding it is most of the decision.** "Remember the geometry, call `setPaneWidth` and `centerPane`, restore on exit" routes through `movePane`, which calls `scheduleSave()` unconditionally *and* rewrites the `widthPreset` stamp on any width change — so within one save debounce the temporary posture is in the persisted v4 blob and the user's stamp (or their hand-dragged width, which is recorded by the *absence* of a stamp) is gone, and a crash or quit mid-bullseye strands them in a stance they never asked to keep. That is a silent, durable [L23] violation. So **bullseye writes no geometry at all**, and exit therefore restores nothing: there is nothing to restore because nothing was disturbed, which is strictly stronger than restoring four fields correctly across every interleaving a window resize or an allocator retune could produce. The one state change is `bullseyePaneId` on `DeckState` — in the state because `DeckState` *is* the `useSyncExternalStore` snapshot ([L02]) and the input to `arrangementSignature`, and deliberately absent from `serialize()` for the reason above, following the precedent `hasFocus` sets on the same interface. **The frame is the imposer's existing one-up placement at comfy** — `imposeStyle({ slot: 0, count: 1 }, comfyWidth, pinnedFrame)` — because `travelFraction` already returns `0.5` for `count < 2`: "centred in the band, slack split evenly" *is* what one-up means, so writing centring math would be writing a second copy of it, and the width is `comfy` rather than a fourth number ([D130]'s "no second opinion about width"). It is the **first** branch of `TugPane`'s geometry ternary: precedence over imposed is the feature (a slotted card bullseyes to the centre and returns to its slot), precedence over pinned is defensive only, since a rail can never hold the posture. Entry and exit **cross rather than cut** for free, because every horizontal pin is a `left` calc ([D121]) and the FLIP settle interpolates width as well as position. **Every other content pane leaves the canvas; the rails stay standing and recede.** A card that is merely dimmed is still a card you can read, and the first cut of this decision — recede everything, move nothing — was wrong for exactly that reason: it produced a deck where nothing had got out of the way. So each other content pane slides off the canvas and back on exit, and **which side it leaves by is read against the bullseyed card, not against the canvas**: panes left of it go left, panes right of it go right. Sorting around the canvas centre was tried first and it let cards cross the bullseyed one — bullseye the leftmost card of a three-up and the middle card, still left of the canvas centre, slid left THROUGH the card arriving there. Sorting around the bullseyed card's own former place makes a crossing impossible by construction, because every pane keeps the side it was already on and relative order is preserved throughout the move. The reference is the bullseyed pane's PRE-bullseye centre, which is available for free: bullseye writes nothing, so the pane still carries the slot and position it will return to. The cost is that travel is no longer a short hop to the nearest edge — a pane just right of the bullseyed one but near the left of the deck crosses the whole canvas to leave rightward. It writes no geometry either: the displacement is one `left` override at render, so the pane returns to the pixel. **The edge is read from the pane's RESOLVED pin, never from its stored `position.x`** — for an imposed pane the stored position is a last-known value the imposer superseded, and a three-up deck can hold all three panes at nearly the same stored x while they sit left, centre and right on screen, so deciding from it sent every card out the same side. The comparison itself is then made in CSS rather than in JS: both terms are length expressions over the live insets, and resolving them at render would mean measuring, which is the observation [L06] keeps out of the geometry path; instead a clamp saturates the signed difference between the two centres into one bound or the other, and re-resolves on a window resize for free. **Both bounds park the pane just past the canvas edge rather than a whole canvas beyond it**, and that is about the motion rather than the resting place: the frames animate out on the existing FLIP settle, and a left bound of `-100%` — a full canvas-width out — made a left-leaving pane travel some 1400px further than a right-leaving one over the same 300ms. The settle's spring is critically damped, so it spends most of its distance early; over that much travel the card was gone inside ~50ms and read as having simply vanished, while the return, which decelerates *into* view, looked correct. Same tween, opposite direction, only one of them legible — and the failure is invisible to every resting-rect assertion. The motion is the meaning here: a card that disappears reads as closed, a card you watch slide off the edge reads as moved aside. **Rails are excluded and keep their pins** ([D128]): a rail that left would take the band's insets with it and the bullseyed card would jump the moment the posture began — and collapsing one would run the space allocator on both edges of a gesture meant to be instant and repeatable. They take the deep recede in place instead. **The recede is the deck's existing inactive-content dim, deepened** — the two `.tug-pane-chrome::before` / `::after` layers at deeper token values under a `data-bullseye` hook — and emphatically **not** a scrim: `.tug-pane-scrim` is the *pane-modal* layer and blocks pointer events, while bullseye must stay non-modal, because clicking another pane is one of its exit doors and a blocked click is a dead gesture instead of an exit. Nothing may put `filter`, `transform`, `backdrop-filter`, `contain`, or `will-change` on `.tug-pane` for this: any of them makes every receded frame a containing block for its `position: fixed` descendants, and `TugSheet` portals into the frame while completion popups and banners position from viewport coordinates. **The exits come in two shapes, and both are stated over the mechanism rather than over the caller.** The geometry-shaped exits say *any path that writes a pane's `position`, `size`, or `slot` releases that pane* — one helper, `_clearBullseyeFor`, called from the three paths that do (`movePane`, gated on the `positionChanged || sizeChanged` locals it already computes and **not** on `evictSlot`, which the width doors never pass; `setContentWidth` and `assignCardToSlot`, which bypass `movePane`). Stating it over the mutation is what makes the audit question checkable — *does this path write geometry?* — where a rule written over call sites goes stale the next time someone adds a mutator; it is the same rule the imposer already lives by, that a manual gesture releases a derived pane ([D121]). The focus-shaped exits are **one clear from `_flipFirstResponder`**, the single entry point for first-responder transitions, so the click, the ⌘R picker, both rings, every sidebar chord, and the canvas-background deselect are covered at no per-path cost — and so is every focus path added later, which is the property an event handler per door cannot offer. The read accessor derives on top of that (`bullseyePaneIdOf`: the id holds only while its pane exists *and* hosts the first responder), which makes a stale id unreadable in the window before a flip commits. **Deriving alone was tried first and is not sufficient**, and the correction is recorded rather than quietly applied: a hidden-but-present id starts matching again the moment focus returns to its pane, so clicking away and back resurrected a posture the user never re-asked for. Bullseye is meant to be left, not parked. The posture does survive a **tab switch inside** the bullseyed pane, because the question asked is whether the *pane* hosts the responder — the user is still working in the card they bullseyed. **⌃⌘B is derived, not chosen** ([chord-tiers.md](chord-tiers.md)): a card's posture on the deck is Tug's own layout machinery, so it takes the Tug tier beside ⌃⌘L, ⌃⌘T and the ⌃⌘1/2/3 width row; plain ⌘ is out under R3 (a deliberate posture change is not a many-times-an-hour verb) and the composed sets are out under R1 (there is no ⌘B base for it to be a variant of — ⌘B is held in reserve for bold). Promotion to the Window menu is R6's half of the grant and preempts every scoped binding, which is intended: a deck-level posture is not a surface's to decline. One action, not [D130]'s selection-relative / pane-addressed pair, because bullseye's two doors mean the same pane; if a title-bar control is ever added it should introduce a pane-addressed `set-bullseye` and have `toggle-bullseye` dispatch to it. `layout-tree.ts` (the field, invariant 8), `deck-store-selectors.ts` (`bullseyePaneIdOf`), `deck-manager.ts` (`toggleBullseye`, `_clearBullseyeFor`, `_clearBullseyeOnFocusFlip`), `serialization.ts` (the documented omission), `components/chrome/tug-pane.tsx` (the branch, `derivedRef`, `data-bullseye`), `components/chrome/deck-canvas.tsx` (the handler, the signature term, the container hook), `components/tugways/command-registry.ts` + `action-vocabulary.ts` (`toggle-bullseye`, the `bullseye` fact), `lib/host-menu-state.ts` (the projection), `components/tugways/tug-pane.css` + the six themes (the deepened dim — and it has to be deep: the inactive dim it builds on moves the card surface about 4%, so a timid multiple of it is indistinguishable from nothing, which is how the first cut shipped a recede nobody could see), `tugapp/Sources/AppDelegate.swift` (`window.bullseye`). App-tests: `at0372` (entry, the no-write claim read off the live store, every exit door, rail inertness, the imposed pane's return). [D121], [D128], [D130], [L02], [L06], [L09], [L23], [L24], [L30]

---

## Session Identity

**D132.** **A session has one identity, produced by one resolver and rendered by one component family at a declared density tier — led by a live pulsing dot, titled `<name> : <callsign>`, and said in two lines beneath.** Tug named a session seven ways across fifteen surfaces, under five near-parallel precedence rules no two of which agreed: `sessionChipDisplay`, `sessionRowTitle`, `sessionEntryTitle` (a hash-equality *sniff*, which is what a rule looks like once nobody remembers what it was for), and two more in Rust. The 8-char short id was computed independently in three places and stored in none. D132 is [D123] — *a pane's name is one string produced in one place* — applied to the session, and the five rules are deleted rather than reconciled. **`resolveSessionIdentity(sessionId) → SessionIdentity`** is the one producer; **`TugSessionIdentity`** is the one renderer family — `chip` and `line` are its two tier values, and the row and masthead densities are compositions of them (`TugSessionRow`, `SessionMasthead`) rather than further tiers on the component; **no surface composes a `project/tag` string of its own**, which is a grep gate rather than a convention because that is the only form the discipline survives in.

**Two registers, not two formats.** A surface that *is* the session — the masthead, a picker row, a Lens row — renders the identity as typography: no enclosure. A surface that *refers* to a session from foreign context — a Gazette ref, a History commit line, the Changes card's orphan hint — wraps the same identity in the **session atom**, a rounded pill. An enclosed chip reads as a link to the thing elsewhere; on a surface that *is* the thing, that reading is a lie. The tier implies the register, so a caller picks a density and never a format.

**The mark is a live pulsing dot, and it is the same mark on both registers.** It says what the session is *doing* rather than what kind of thing it is, which is the one thing a static glyph could never do. `useSessionPhase(sessionId)` is the session-keyed door onto it and returns the flattened phase **key** rather than the snapshot, which is what makes it affordable on a Changes card carrying one citation per row: React bails out unless the *reading* changed. **Unknown liveness reads `idle`, never danger** — no bound card, services not yet constructed, a closed or external row — because red is the error channel and spending it on "we do not know" makes every ordinary closed session look broken. A card that exists whose transport is genuinely offline still reads danger; that is a real failure rather than doubt. Retired with the dot, *do not re-propose*: the **chatbox icon** (`MessageSquareText`/`MessageSquareOff`), which spent a second mark on a job the dot does better, and the **`OFFLINE_PHASE_INPUT` fallback** that painted doubt as danger.

**The title is `<name> : <callsign>` in TWO runs, and which run gives way depends on the register.** A user-supplied name is the user explicitly saying what the session is called; it cannot rank below a callsign Tug minted for itself. The callsign stays visible beside it because it is the permanent citable handle a rename never changes. Under a width squeeze the two registers truncate **differently, by design**: on a *title* surface (the line tier — the masthead, the Lens rows, the picker rows) the callsign is the handle a reader scans a list by, so it survives and the user's name elides; on the *atom* (the chip tier) the user's own words survive and the minted handle elides — the tooltip and every copy path still carry it whole either way. An unnamed session renders the bare callsign, which may then elide because it is the only run there is. `sessionTitleParts` produces the pair; two runs rather than one joined string is what lets them be sized separately, and a filter highlight is painted **inside each run** — joining first would put the ` : ` separator inside a mark and defeat the truncation rule. **No `project/` prefix in title ink.** That prefix is `sessionIdentityLine`'s, and that string is a different channel: the pane-title channel the tab strip, the Window menu, and the slot-stack picker read, which is not a notification path and is documented as **constant for the life of a binding** — changing it would make the tab strip stale on rename. It survives unchanged, and so does the prefix, in the tooltip, the citation, and the telemetry popover. Retired: **`<project>/<callsign>` as one text node**, which was right while the two halves were a path and wrong the moment one of them became the user's own words. **Monospace still belongs to flat text only** — never to session chrome.

**The callsign is immutable and the UUID never leads.** A legacy tagless row degrades to its 8-char short id and **never** to the full UUID. Permanence is not aspirational: `sessions` rows are hard-`DELETE`d by trash, three cascade paths, and cap/age eviction, so a unique index over that table frees a tag when its row dies — and a recycled tag makes a two-year-old commit's citation resolve to a *different* session, which is a confidently wrong answer and strictly worse than an unresolvable one. So an **append-only `minted_tags` table is the all-time arbiter**: every mint path — spawn, fork, external backfill — inserts into it in the same transaction as the row it names, its `PRIMARY KEY` violation is the collision signal a mint retries against, and **nothing anywhere may delete from it** (`no DELETE FROM minted_tags` is a grep gate). `sessions_tag` survives as the live-row invariant only. On a genuine collision the server **rerolls a complete fresh `adjective-noun`** — the bare `-N` suffix backstop is retired, along with the silent NULL tag it landed on at exhaustion. *Do not re-propose either.* The guarantee is honestly **per-ledger** (`sessions.db` is per-instance): a trailer written on one machine and read against another's ledger simply misses, which renders as the slashed atom and is safe.

**A fork's callsign says where it came from.** `<root>-<Letter><Number>`: the letter names the rewind point (the first point ever forked from within a lineage is `A`), the number sequences forks from that point, and a fork of a fork extends the chain (`stocky-pixie-A1-B2`). Allocation is a table, `tag_lineage_points`, not a query over sibling names — a query would have to re-derive letters from parsed display strings, and a trashed fork would take its number with it. That table is append-only for the same reason `minted_tags` is: a reissued letter or number would make two unrelated forks share a callsign. `-<Letter><Number>` is the **only** sanctioned suffix, and a colliding lineage candidate *errors* rather than rerolling, because a reroll would write an unrelated word pair into `tag` while `root_tag`/`tag_lineage` still named the lineage — a contradiction the resolver would render straight to the user.

**The citation is the only sanctioned flat-text form**, and the only place monospace appears: `<tag> (<shortid8>)`, project-prefixed when the context does not supply the project, degrading to the bare short id for a legacy tagless row (a doubled hash is noise). Every Tug commit carries **two** trailers — `Tug-Session:` (the citation, for a human) and `Tug-Session-Id:` (the full uuid, for a machine, never displayed) — which tugcast parses server-side into typed `GitLogCommit` fields exactly as it already did for `Tug-Dash`, **and strips from the displayed body**, because `%b` retains trailer lines and that is why the History card used to show raw trailer ink. Reading a citation is two steps with a wire between them: `resolveCitedSession` is **pure grammar** over the trailer text — machine field first, then the parenthesized token (36 chars = a legacy one-line trailer, which therefore still resolves exactly; 8 hex = a short id) — and turning that id into a session is the **ledger's** job, over the `resolve_sessions` verb. Legacy commits live in history forever and had to keep working; they do. Transcript exports become `tug-session-<callsign>-<shortid>` — a file named after a hash is a file nobody finds again.

**Resolvability is a ledger answer, and a short id is expanded server-side.** *Do not re-propose* deciding either from the client's caches. The tag store and the binding store hold only the sessions some listing, push, or card binding happened to mention this run, so a chip judged against them is resolvable or slashed depending on whether the picker had been opened — which is not a fact about the reference. `resolve_sessions { ids }` answers `{ sessions: [{ queried, session }], unknown }`: keyed by the spelling asked (a short id and its row's full id are different strings), and **naming its misses**, which is what lets `session-citation-store.ts` cache a negative instead of re-asking on every repaint. Expansion belongs there for a second reason — only the ledger can see that two sessions share eight hex characters, and an ambiguous prefix therefore **resolves to nothing** rather than to the first row found, the same "wrong-but-resolvable is worse than unresolvable" rule the callsign's permanence rests on. A resolved row also seeds the name / callsign / description stores, which is the fourth such seed path beside the spawn ack, `list_sessions_ok`, and the card-binding rows, and it is how a citation names a session no card is bound to. The resolver answers from `sessions` first and falls back to `external_scan_cache`: cap and age eviction hard-delete `sessions` rows while the transcript stays on disk and the picker keeps listing it, and a citation must not go dark on a session the picker can still resume — the fallback row is synthesized exactly as the picker union synthesizes an external row.

**Liveness is not identity.** The record carries no phase field, and that survives the dot becoming the mark. Phase lives on a per-**card** `codeSessionStore` whose snapshot is the whole `CodeSessionSnapshot`, and an identity hook that subscribed to it would wake the chip, the line, the row, and the masthead on every transcript event; the dot is isolated in a leaf subscription so a reducer wake repaints the dot alone. `useSessionPhase` is the join — session → card → services → snapshot → key — and it lives beside the resolver rather than inside it precisely so the two stay separable. Identity and liveness are two subscriptions with two keys, and **the component is where they meet**: the dot arrives as a child, never as a field.

**The React entry point is the hook, and that is part of the spec rather than an implementation note.** `resolveSessionIdentity` reads three stores imperatively; called from a render body it hands back a snapshot with **no subscription**, so a `/rename` updates the stores while the surface repaints never. That failure passes every unit test and appears only as a stale name in the running app, so `useSessionIdentity` — `useSyncExternalStore` over each backing store **by id**, never by whole-store version token, which would wake every identity surface in the app on any session's rename — is the only door a component may use ([L02]). *No component calls the bare resolver*: grep gate.

**The Session card's chrome is a fixed 72px masthead, and the pane owns the slot while the session owns what is in it.** Three lines — the dot-led title with the pane controls, the description, the activity with its tape — plus a trailing telemetry **popover** carrying branch, stamps, the rendered atom, and the citation on a `TugCopyBadge`.

**One row shape, three mounts.** The masthead, the Lens rows, and the new-session picker rows show the same thing, so `TugSessionRow` authors the shape once and all three compose it — the component library is the consistency mechanism rather than a convention three files are asked to remember. The masthead is seated in chrome through the knobs the row's own primitive publishes (block and inline padding, the indicator gutter, the flush hover, the stack's block pad) rather than by reaching into its rules, and it raises the sub-line indent from the rail's to the dot's full advance because a card-wide tier can afford to hang its two lines off the *title* where a rail cannot. Retired with the shape, *do not re-propose*: the **three-level stack** (a standing goal beside the description read as a second goal-shaped line — the intent survives in the recent-pulses popover, where it heads its run of beats), the **four-line row** (its metadata facts are the activity line's rest form now), and the four **audition fits** `gutter`/`reveal`/`wash`/`duplex` with their comparison meter — only `inset` ships. **The stack's type is stated once, on the row, and no mount restates it**: title, description, and activity are the ambient sans at the row size, one size down, one size down again, and the step off the title is small enough that the pair beneath still reads as one group. The activity line therefore **drops `TugPulse`'s condensed face** on this stack — condensed is right for a PULSE that is a bar of its own, where width is scarce, and wrong for the third line of a paragraph whose other two lines are sans. Three mounts that each named their own sizes is exactly how the three surfaces came to differ from one another and from the gallery.

**The activity line says something true at rest.** `<turns> turns, <size>. Last updated: <stamp>. Ready.` — the turns segment **always prints** (a fresh session's line is `No turns, 8 KB. Ready.`; "no conversation yet" is a fact, not an absence, and the zero is spelled as prose rather than as a digit, which reads as a measurement that came back empty), an unknown size drops its segment, the labeled stamp appears only for a session with turns to have been updated by, and **`Ready.` always closes the line** — the session is on disk and one gesture from another turn wherever the row is read. `Last updated:` is labeled because a bare date-time beside a size and a count is ambiguous about which of the three it dates, which is also why the relative-age form (`3h ago`) went with the metadata line it opened. During a turn the live beat replaces the sentence — on every mount of the row, the picker's included; the picker's state lines survive only for rows this app cannot see into (a terminal's) and for failed rows. **The bare turn-end marker `Done` is not a beat and is never shown** (`sessionActivityBeat` filters it at every mount): it is the *absence* of one, it says a run ended and nothing about when, it sits there unchanged for however long the session then stays quiet, and it is precisely the state the rest sentence describes with the facts in it. The marker itself is untouched on the wire — the ledger, the recent-pulses popover, and a copied line all still carry it; only this reading changes. That grammar is what makes [P10]'s server work load-bearing: both facts are scan-derived, so tugcast re-derives them **at turn end** from the same incremental entry points the picker scan uses, and `build_session_updated_frame` reads the `(file_size, turn_count)` pair from the scan cache on **every** push — because the client replaces its cached row wholesale, so a push that omitted a fact would blank a known size or knock a scan-derived count to a sparse `0`.

**`PULSE` is an internal name and never user-facing ink.** Stores, modules, feeds, class names, and constants keep it; labels, legends, and stand-ins lose it. With the activity line always carrying something true to say, nothing needs a placeholder word — and a band that names itself is furniture where a sentence belongs. A grep gate (`lib/__tests__/pulse-ink-gate.test.ts`) keeps it out, stripping comments first and exempting gallery cards, which are where the vocabulary is discussed.

**The width control does not render on a masthead-bearing pane, and nothing else in the cluster changes.** The masthead's first row is identity plus its own telemetry wave, and width is the one member of the controls cluster that competes with the name for a row that is now three lines of ink; it stays reachable by its command and by the Lens's width presets, so the affordance is relocated rather than removed. The **slot-stack badge**, the **`…` section menu**, and the **close X** all keep rendering exactly as they do: the badge describes the *slot* rather than the card, and a Session card standing in a stack is entitled to say so — it is the only way into the panes behind it. The condition is the **active** card's masthead, so a pane holding a Session card and a non-Session card in one stack shows the button on one tab and not the other. **That flip is accepted, not a bug**: suppressing on "any card in this stack wears a masthead" would take a cross-card fact the title bar does not hold, to buy consistency for a control whose whole point is that it acts on the pane you are looking at. Every assertion about this is an *absence* assertion on the width button's testid — never an equality check on the cluster's list, which is legitimately longer or shorter depending on whether the card stands in a stack. (It shipped as a `TugPopover`, not a `TugPlacard`: a hover-dismissed surface puts its own copy button out of the pointer's reach, and `TugPlacard` renders in place, which a clipping 72px chrome tier cannot host.) The height is **ratified and fixed, never content-driven**; the masthead never reflows and overflow truncates. Chrome remains the Pane's ([L09]), so `TugPane` opens the taller slot when its **active** card publishes a masthead sidecar and mounts a tugways `SessionMasthead` into it — which is what keeps the PULSE feeder, the dwell queue, the compaction pin and the telemetry popover out of chrome code. In a multi-tab pane the chrome follows the **frontmost** tab, swapping 36↔72 on tab switch: a pinned 72px chrome over a Text tab would caption one card with another card's identity. The height travels as a pane-scoped `--tugx-pane-chrome-height` so the four chrome-offset consumers (the title bar, the pane scrim, the sheet clip's top, the pane banner) follow it while the four that are really measuring *a tab row* or *a square button* stay on `--tug-chrome-height` — the masthead and the tab bar **stack** (72 + 36), they do not merge. Once the masthead carries the voice the Z2 `SessionPulseStrip` comes out entirely, with the dwell queue moved *as code* rather than rewritten; its focus stop is retired rather than re-homed, since the masthead is chrome and takes no card-cycle stop.

**The description is a rolling synopsis, and the incipit is retired as its source.** A first prompt describes where a session *started*; a description has to be current, which is the whole complaint. A third job on the SharedAgent's Summarize lane composes a standing line from its own session-lifetime digest — `compose_synopsis_digest`, the opening ask plus the arc of finished stretches, built from the same emit loop's already-cached `PromptCache` rather than the headline's per-stretch cut (the headline summarizes the current stretch; the description summarizes the session) — on a much slower clock (once a minute against the headline's eight seconds — the headline is meant to move, the description to stand), and persists to a `synopsis` column on the session's ledger row. The register rules are the headline's verbatim; only the subject differs.

**The name and the description are two fields on two lines, and the rename freeze is gone.** `SessionIdentity` carries `customName` (the user's `/rename`) and `description` (the agent's synopsis) as independent fields — the merged `title` is **removed**, not deprecated, so the compiler enumerated every reader. The freeze existed because they competed for one line of chrome and a generated line must not speak over the user's word; they occupy different lines now, so both enforcement points are gone — the `AND name_user_set = 0` clause in `record_synopsis` and the pre-model early return in `session_overview.rs`. Leaving it would have given every renamed session a permanently dead description line on its most-visible surface. *Do not restore it as a "fix".* The description's own precedence is its three rungs: the synopsis, else the session's **own first prompt** (load-bearing at the picker, where a freshly-scanned external session has never been summarized and its first prompt is the only human-meaningful text the row holds), else `Created <stamp>`. The lower two are marked `data-stamp` and painted a step quieter, to say they are facts standing in. **The stamp rung has to work on a session with no turns**, which is the only state in which it is the *only* rung available — and both of the deck's sources were structurally blind to exactly that session: the card's replay anchor reads the first turn's timestamp out of a JSONL a zero-turn session has not written, and the ledger's workspace listing deliberately omits a content-empty row so nobody is offered an abandoned session to resume into. The listing rule stays; the spawn-time `session_updated` push is **kept rather than discarded** (`SessionLedgerStore.detachedRows`) and answered by id through `findRow` / `useSessionLedgerRow`. A card asking about *its own* session is not the picker browsing a workspace, and only one of those two questions the empty-row filter was written for. The line **always occupies its height**, so a description arriving does not move the line below it.

Two consequences of riding the headline's *machinery*, both deliberate. **It inherits the headline's gates:** with PULSE or the `pulse-overview` tenant switched off, no digest is composed and no description is written, so those sessions read as honestly empty. The synopsis digest is a different *cut* of the same evidence, never a second collection: it is composed inside the same emit loop from the `PromptCache` the emit already assembled, so running it re-reads no JSONL and bypasses no gate. A standalone description pipeline — its own JSONL read, its own schedule, alive while PULSE is off — is what was rejected: *do not re-propose*. **And the write has no fallback row key:** ledger rows are keyed by *claude's* session id, the resolver answers it, and a resolver that cannot answer means the ask is **skipped entirely** rather than aimed at the tug id. The resolver returns `None` both for "no such entry" and for a contended lock, and after a rewind fork the tug id names the *parent's* row — so guessing it would write one session's description onto another's, and the session that waits a minute for its line has lost nothing.

**The atom paints in text ink, and the dot is its only color channel.** The pill keeps its rounded shape and drops the theme's session tone: the run takes the ordinary text color and the border a `currentcolor` mix. A colored pill around a colored dot was two tints saying one thing, and the dot's tint is the one carrying information. The theme-authored `session` tone on the `atom` slot — four `--tug-color(violet, …)` recipes per theme, aliased in one hop to `--tugx-session-identity-*` ([L17]) with `ELEMENT_SURFACE_PAIRING_MAP` entries — **stays authored** and is simply not consumed by this component; retiring the tokens is a separate question. Still rejected, *do not re-propose*: a **per-session hashed tint** (color is a semantic channel; a hashed hue reads as meaning while meaning nothing), and a bespoke `--tug-session-color` role token with the component deriving three of four values by `color-mix` — not a `--tug7-*` target, moves per-theme tunability into component CSS against [L20], and is invisible to the contrast audit, which cannot see through `color-mix`.

**Wherever an atom is a mounted React chip, it is a subscribed one.** Its dot reads the session's phase this second and its name tracks renames — the Gazette ref, the History line, the Changes list row (one chip *per row*), and the masthead's telemetry popover, all four for the price of the hook, because all four compose `TugSessionCitation`. **The composer's pasted atom is explicitly excluded and stays baked ink**: an atom in the prompt editor is a CodeMirror `AtomWidget` whose `toDOM` returns an `HTMLImageElement`, and the bake is not a styling choice but how an atom survives being *editor ink* — one `root.render` for the app's life ([L01]), a replaced inline element whose caret and selection semantics CodeMirror owns, and a copy/paste round trip through document text. A pasted atom's dot is a snapshot at paste time and its name the name as of paste, which is honest for a thing that is now text in a draft. *Do not propose mounting a live subtree there* without first solving the second-React-root problem.

**An unresolvable citation keeps its shape and says so.** Same pill, dashed border, muted ink, a forced idle dot, fully inert, tooltip *Session not found* — and it still shows whatever callsign the commit recorded, because a reference that cannot be followed is more useful naming what it named than showing a bare hash. The **slashed mark is retired** with the icon it slashed: there is nothing left to slash, and shape alone states the failure, which it already did alongside the slash. **Liveness is never a property of a reference**: sessions are never dead, only unfindable.

**Right-clicking the masthead's title copies the atom.** The full flavor set, through the same `useCopyableText` every other Tug chip uses — the atom should be reachable without opening anything, and right-click is the idiom already in place. The telemetry popover additionally *displays* the rendered atom above the flat citation, so the two copyable forms sit side by side, each labeled by what a paste of it yields.

Those two halves fight, and the record is where they are kept apart. A `recordedTag` — the callsign inside a commit's own trailer — fills `SessionIdentity.tag`, so a reader of `tag` alone cannot tell the ledger's word from the commit's copy of it, and one that tried would call every foreign commit's session findable. So resolvability is **its own field**, `SessionIdentity.resolved`, true only on ledger-fed evidence (a callsign in the tag store, a card binding supplying the project, or a `resolve_sessions` hit) and conspicuously indifferent to `recordedTag`. A third rendering falls out and is required: while the round trip is in flight the chip is **inert but not slashed** — claiming *not found* before asking is the same lie pointing the other way. And the click belongs to `TugSessionCitation` rather than to each surface, offered only when a card is actually bound (a subscribed read, so it appears and disappears with the card): every citation means one thing by a click, and *looking clickable while doing nothing* is the failure the Gazette's disabled refs were written to avoid.

Also retired, *do not re-propose*: the **`(branch)` title suffix** — the branch left identity and lives only in the telemetry popover; and **`/retag`** — the callsign is immutable, which is the property every commit trailer now rests on.

**And what was KEPT, so it is not re-litigated as an oversight.** The slot-stack badge and the `…` section menu stay in a masthead pane's title bar; the masthead's **two popovers** stay — the tape's expanded Activity card and the activity run's recent-pulses history, whose intent groupings are where the retired standing-goal line survives; the composer's pasted atom stays baked ink; `sessionIdentityLine` and `sessionCitation` are unchanged, as are the atom's clipboard flavors, the commit trailer pair, and the fork-lineage grammar. The atom's *face* changed; its *payloads* did not.

Two open questions were deliberately deferred rather than answered: whether the atom's sidecar segment should carry the short id (nothing needs exact re-resolution yet — the callsign is already a unique permanent key), and whether the Changes card's server-side `session_row_title` should adopt this grammar (it is a sixth surface naming sessions and currently gives a different answer; the change is independent of everything here).

`tugdeck/src/lib/session-identity.ts` (the resolver, the hook, `sessionTitleParts`, the citation and line formatters, `resolveCitedSession`), `lib/code-session-store/use-session-phase.ts` (the session-keyed phase door and the doubt rule), `lib/session-activity-line.ts` (the rest grammar), `lib/session-created-at.ts` (the shared creation-date resolver), `lib/session-tag-store.ts` (the by-id cache and the reverse `tag → session_id` index behind `/resume <callsign>`), `lib/session-synopsis-store.ts`, `lib/session-atom.ts` (the clipboard flavors; the chip's raise dispatches the registry's `focus-session-card`, the same funnel the Lens rows use), `lib/pane-title.ts` (`cardTitleTextFor`), `components/tugways/tug-session-identity.tsx` (the family and `TugSessionCitation`), `components/tugways/session-phase-dot.tsx`, `components/tugways/tug-session-row.tsx` (the one row shape), `components/tugways/tug-pulse.tsx`, `components/tugways/session-masthead.tsx`, `components/lens/sections/cards-session-cell.tsx`, `components/tugways/cards/session-picker-cells.tsx`, `components/chrome/tug-pane.tsx` (the chrome tier and the width control's suppression); `tugrust/crates/tugcast/src/session_ledger.rs` (`minted_tags`, `tag_lineage_points`, the mint reroll, `record_synopsis`, `scan_metrics_for`), `external_sessions.rs` (`refresh_session_metrics`), `feeds/agent_supervisor.rs` (`build_session_updated_frame`, the turn-end refresh), `shared_agent.rs` (the `synopsis` job), `feeds/session_overview.rs` (the synopsis's cadence), `feeds/changeset.rs` (the tag arm), `feeds/git.rs` + `tugcast-core/src/types.rs` (the typed trailers), `tugdash-core/src/ops.rs` (`session_trailer`). Grep gates: `lib/__tests__/pulse-ink-gate.test.ts`, and the no-bare-resolver-in-components gate. App-tests: `at0282`, `at0373`–`at0381`. Originating plans: `roadmap/session-reference-plan.md`, then `roadmap/session-identity.md` (the content rollout). [D121], [D123], [D127], [L01], [L02], [L06], [L09], [L13], [L17], [L19], [L20], [L25]

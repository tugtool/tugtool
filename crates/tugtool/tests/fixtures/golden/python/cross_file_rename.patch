--- a/utils.py
+++ b/utils.py
@@ -2,12 +2,12 @@


-def calculate_sum(numbers):
+def compute_total(numbers):
     """Calculate the sum of a list of numbers."""
     return sum(numbers)


 def calculate_average(numbers):
     """Calculate the average of a list of numbers."""
     if not numbers:
         return 0
-    return calculate_sum(numbers) / len(numbers)
+    return compute_total(numbers) / len(numbers)

--- a/main.py
+++ b/main.py
@@ -1,12 +1,12 @@
 """Main module that uses utility functions."""

-from utils import calculate_sum, calculate_average, format_result
+from utils import compute_total, calculate_average, format_result


 def process_data(data):
     """Process data and print statistics."""
-    total = calculate_sum(data)
+    total = compute_total(data)
     avg = calculate_average(data)

